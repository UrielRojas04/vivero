# --- Entornos / secretos ---
.env
.env.*
!.env.example

# --- Backend (Java / Maven) ---
backend/target/
backend/*.log
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# --- Frontend (Node / Vite) ---
frontend/node_modules/
frontend/dist/
frontend/*.log
frontend/*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Logs / residuos de pruebas locales ---
*.log
test_put.py
frontend/test_ui.cjs

# --- Directorios de configuración de agentes (no se commitean) ---
.agent/
.agents/
.claude/

# --- Skills OPSX del proyecto (SÍ se versionan) ---
# .opencode/ contiene las skills openspec-*.md del flujo OPSX local — NO ignorar.

# --- IDE / OS ---
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# --- Base de datos local / volúmenes docker ---
*.db
*.sqlite
docker-data/

# --- Imágenes y adjuntos ---
img/

# --- Scripts y dumps de prueba locales ---
test_*.py
login.json
*_logs*.txt

# --- Scripts SQL destructivos de sesiones de trabajo (borrado masivo por unidad, resets, etc.) ---
# NO son secretos, pero son peligrosos: nadie debería poder cloneaer el repo y correr un DELETE
# masivo contra la base equivocada por accidente. test_seed.sql (fixtures inofensivos) SÍ se
# versiona a propósito, no lo cubre este patrón.
wipe_*.sql
reset_*.sql

# --- Específicos del Stack ---
frontend/coverage/
frontend/.eslintcache
docker-compose.override.yml
.settings/
.project
.classpath
.factorypath
venv/
.venv/