> ### ⚠️ Gobernanza: **MEDIA-ALTA** — Stock e insumo de costos para rentabilidad
>
> Este change escribe stock real (`Producto.stock`) y el costo histórico congelado (`MovimientoStock.costoUnitario`) que va a consumir el dashboard de rentabilidad de `us-017-finanzas-ui`. No toca autenticación, cuentas corrientes de dinero ni cheques, así que **no** es CRÍTICO como `cheques-rebote-endosado` — pero tampoco corresponde autonomía plena.
>
> **El grupo 6 (confirmación de recepción e ingreso de stock) requiere checkpoint explícito del usuario ANTES de escribir el código, no después.** Se le muestra la secuencia exacta —qué cantidad se suma, qué costo se congela, qué estado resulta— y se obtiene confirmación. No alcanza con aprobar al pasar. El **grupo 5** (sobrecarga de `registrarMovimiento`) lleva checkpoint liviano por la misma razón: es donde se decide de dónde sale el costo.
>
> Motivo concreto: `Producto.costoUnitarioHistorico` es una `@Formula` que lee el `costo_unitario` del **último** movimiento `INGRESO`/`AJUSTE_INICIAL`. Un costo mal escrito no rompe ninguna pantalla ni tira excepción: se convierte en el costo de referencia del producto y falsea toda la valuación de inventario y el margen calculado sobre él, en silencio. Y un ingreso duplicado infla el inventario sin dejar señal más que un movimiento de más en una tabla que nadie mira.
>
> Ninguna Open Question de `design.md` es bloqueante. Las 5 tienen recomendación; si el usuario no se pronuncia en el grupo 1, se implementa la recomendación.

## 1. Línea de base y confirmación de decisiones abiertas

- [x] 1.1 Verificar en la base real que existen exactamente dos filas en `unidades_negocio` (`1 Vivero`, `2 Herramientas`, ambas `activo=true`) y registrar el `costo_envio_porcentaje` de Herramientas — es un insumo directo de la fórmula de costo del grupo 5. *Confirmado: `id=1 Vivero (costo_envio_porcentaje=0.00)` e `id=2 Herramientas (costo_envio_porcentaje=5.00)`, ambas activas.*
- [x] 1.2 Registrar como línea de base la asignación actual de permisos por rol (tabla de unión real: `rol_permiso`), en particular cuántos permisos tiene hoy `JEFE` y cuántos `EMPLEADO_VIVERO`. Es la referencia contra la cual se verifica el grupo 4. *Confirmado: `ADMIN 2`=6, `EMPLEADO NORMAL`=4, `EMPLEADO VIVERO`=1, `EMPLEADO_VIVERO`=3, `ENCARGADA`=7, `JEFE`=11, `SEMBRADOR`=4. Tras el grupo 4, `JEFE` debe quedar en 13 (11+2) y `EMPLEADO_VIVERO` debe seguir en 3 — a verificar en el grupo 11 con el backend en vivo.*
- [x] 1.3 Elegir 2-3 productos de Herramientas para las pruebas del grupo 10 y anotar su `stock`, `costo_producto`, `descuento_proveedor`, `precio` y `porcentaje_ganancia` actuales, más el `costo_unitario` de su último `INGRESO`/`AJUSTE_INICIAL`. Sin esta línea de base no se puede demostrar que el ingreso hizo lo correcto. *Elegidos: `id=4 "Cinta aisladora"` (stock=6, costo_producto=2000, descuento_proveedor=0.80, precio=2624.83, %ganancia=26, último costo_unitario=2083.20), `id=5 "Cinta métrica"` (stock=2, costo_producto=5000, descuento_proveedor=1.00, precio=7016.63, %ganancia=35, último costo_unitario=5197.50), `id=12 "Destornillador"` (stock=1, costo_producto=10000, descuento_proveedor=0.80, precio=13000.21, %ganancia=24.81, último costo_unitario=10416.00).*
- [x] 1.4 Presentar al usuario las 5 Open Questions de `design.md` con su recomendación y registrar acá su decisión sobre cada una. Ninguna bloquea: si no se pronuncia, se implementa la recomendación y se anota que se tomó por defecto. *Las 5 se implementan por la recomendación documentada en `design.md`: (1) un pedido es a un solo proveedor; (2) no hay "segunda entrega" contra un pedido ya `PARCIAL`; (3) recibir de más se acepta; (4) NO se actualiza `Producto.costoProducto`/`precio` al confirmar; (5) el total en pesos del listado es calculado, no persistido.*
- [x] 1.5 Confirmar con el usuario los nombres de los permisos nuevos (`LEER_PEDIDOS` / `ESCRIBIR_PEDIDOS`) y que sólo van al rol `JEFE`, recordándole que él puede asignárselos a sus roles propios después desde `UsuariosAdmin.jsx` (`DataInitializer` no toca los roles creados a mano). *Confirmado. El usuario además pidió que el rol `ADMIN 2` (creado a mano, id=4 en la base real) también reciba ambos permisos — se otorgan vía la API real de roles en el grupo 11, NO se agrega `ADMIN 2` a `DataInitializer.java` (ese archivo sólo tiene autoridad sobre `JEFE`/`EMPLEADO_VIVERO`, precedente `bandejas-acceso-limitado`).*

## 2. Entidad y ABM de proveedores (backend)

- [x] 2.1 Crear `backend/src/main/java/com/vivero/gestion/models/Proveedor.java` con `id`, `nombre`, `telefono`, `contacto`, `@ManyToOne UnidadNegocio` y `deleted`, usando `@SQLDelete` + `@SQLRestriction("deleted = false")` y Lombok `@Data`, con `Cliente.java` como molde exacto.
- [x] 2.2 Crear `ProveedorRepository` con `findAllByUnidadNegocioId(Long)` y `findByIdAndUnidadNegocioId(Long, Long)`, mismas firmas que `ClienteRepository`.
- [x] 2.3 Crear `dto/ProveedorDTO.java` con `id`, `nombre`, `telefono`, `contacto`. Sin `unidadNegocio` — la asigna el servidor desde el contexto y no es dato del cliente.
- [x] 2.4 Crear `services/ProveedorService.java` + `services/impl/ProveedorServiceImpl.java` con `getAll`, `getById`, `create`, `update`, `delete`, todos `@Transactional` y filtrando por `UnidadNegocioContextHolder.getUnidadNegocioId()` con el patrón de `ClienteServiceImpl` (lista filtrada si hay unidad, `findByIdAndUnidadNegocioId` con excepción para el acceso por id).
- [x] 2.5 Validar en `create`/`update` que el nombre no sea nulo ni vacío, rechazando la operación si lo es.
- [x] 2.6 Crear `controllers/ProveedorController.java` con `@RequestMapping("/api/proveedores")` y las cinco operaciones de la tabla de la Decisión 13, con `@PreAuthorize` **por método** (`LEER_PEDIDOS` para lectura, `ESCRIBIR_PEDIDOS` para escritura).
- [x] 2.7 Verificar por lectura del código que el controller no toca ningún repositorio y que ningún método devuelve la entidad `Proveedor` — sólo `ProveedorDTO` (reglas duras: Controller → Service → Repository, DTOs siempre).

## 3. Entidades de pedido (backend)

- [x] 3.1 Crear `models/EstadoPedido.java` con exactamente `PENDIENTE, COMPLETO, PARCIAL, CANCELADO`.
- [x] 3.2 Crear `models/Pedido.java` con los campos de la tabla de la Decisión 1: `proveedor`, `unidadNegocio`, `usuario`, `fechaCreacion`, `fechaRecepcion`, `estado` (`@Enumerated(EnumType.STRING)`), `observaciones`, `detalles` (`@OneToMany(mappedBy="pedido", cascade=ALL, orphanRemoval=true)`) y `deleted` con baja lógica.
- [x] 3.3 Crear `models/PedidoDetalle.java` con `pedido`, `producto`, `cantidadPedida`, `costoUnitarioPactado` (`BigDecimal`, `precision=12, scale=2`) y `cantidadRecibida` **nullable** (`Integer`, no `int`).
- [x] 3.4 Verificar que `cantidadRecibida` NO tiene valor por defecto 0: `null` significa "sin confirmar" y `0` significa "confirmado, no llegó nada". Confundirlos rompe el requisito de remanente.
- [x] 3.5 Verificar que el remanente NO existe como columna en `PedidoDetalle` — es derivado y se calcula en el mapeo a DTO.
- [x] 3.6 Crear `PedidoRepository` (con soporte `Pageable` y filtros por `unidadNegocioId`, `estado` y `proveedorId`) y `PedidoDetalleRepository`.
- [x] 3.7 Levantar el backend y verificar en la base que Hibernate creó `proveedores`, `pedidos` y `pedido_detalles` con sus FKs, y que **ninguna tabla preexistente cambió de esquema** (`productos` y `movimientos_stock` intactas). Sin migración manual (`ddl-auto=update`). *(Build real con `docker compose up -d --build backend`, arrancó sin errores. Las 3 tablas nuevas existen; `productos` mantuvo sus 14 columnas y `movimientos_stock` sus 13, iguales a antes del change.)*

## 4. Permisos del circuito (backend)

- [x] 4.1 En `config/DataInitializer.java`, dar de alta `LEER_PEDIDOS` y `ESCRIBIR_PEDIDOS` a continuación de los permisos de bandejas.
- [x] 4.2 Agregar ambos a `permisosJefe`. *Nota: el usuario pidió además que el rol `ADMIN 2` (id=4 en la base real, creado a mano desde `UsuariosAdmin.jsx`, lo usa su cuñado para Herramientas) reciba `LEER_PEDIDOS`/`ESCRIBIR_PEDIDOS`. `DataInitializer.java` NO se toca para esto — ese archivo sólo tiene autoridad sobre `JEFE`/`EMPLEADO_VIVERO` (precedente `bandejas-acceso-limitado`); tocar un rol creado a mano ahí rompería ese patrón. El otorgamiento a `ADMIN 2` se hace vía la API real de roles (el mismo mecanismo que usaría el usuario desde `UsuariosAdmin.jsx`), en la verificación en vivo del grupo 11 — ver nota en 11.1.*
- [x] 4.3 Verificar que `permisosEmpleado` queda **exactamente** como está: `LEER_STOCK`, `ESCRIBIR_STOCK`, `ESCRIBIR_VENTAS`. Ni un permiso más.
- [x] 4.4 Levantar el backend y verificar en base real contra la línea de base de 1.2: existen los dos permisos nuevos, `JEFE` subió en exactamente 2, `EMPLEADO_VIVERO` no cambió, y ningún rol creado a mano desde `UsuariosAdmin.jsx` cambió. *(Verificado: `LEER_PEDIDOS`/`ESCRIBIR_PEDIDOS` existen (ids 12/13), `JEFE` 11→13, `EMPLEADO_VIVERO`=3 sin cambio, resto de roles sin cambio. Además, a pedido del usuario, se le dieron ambos permisos al rol `ADMIN 2` (el cuñado que maneja Herramientas) vía `PUT /api/roles/4`, el mismo mecanismo real de `UsuariosAdmin.jsx` — no vía `DataInitializer`. `ADMIN 2` quedó con sus 6 permisos originales + los 2 nuevos = 8.)*

## 5. Costo explícito en los movimientos de stock (backend) — CHECKPOINT LIVIANO

- [x] 5.1 **Checkpoint previo:** mostrar al usuario la fórmula exacta que va a quedar (`costoBase = explícito != null ? explícito : producto.getCostoProducto()`, y después `(costoBase − descuento%) + envío% sobre el resultado`) y confirmar que el descuento del producto y el envío de la unidad de negocio se siguen aplicando sobre el costo del pedido. Es la decisión de dónde sale la plata que se congela.
- [x] 5.2 Agregar a `services/MovimientoStockService.java` la sobrecarga `registrarMovimiento(Producto, Integer, TipoMovimientoStock, Usuario, BigDecimal costoBaseExplicito)`, dejando la firma de 4 parámetros exactamente como está.
- [x] 5.3 En `services/impl/MovimientoStockServiceImpl.java`, mover el cuerpo actual a la sobrecarga nueva y hacer que la firma de 4 parámetros **delegue** en ella con `null`. Debe quedar una sola implementación de la fórmula, no dos.
- [x] 5.4 En la rama `INGRESO`/`AJUSTE_INICIAL`, usar `costoBaseExplicito` cuando no es `null` y `producto.getCostoProducto()` cuando lo es. **No tocar la rama de egresos** (`EGRESO`/`VENTA`/`MERMA`), que busca el último ingreso y hoy funciona.
- [x] 5.5 Verificar que se siguen persistiendo los cuatro campos (`costoBase`, `descuentoPorcentaje`, `envioPorcentaje`, `costoUnitario`) y que `unidadNegocio` del movimiento se sigue tomando de `producto.getUnidadNegocio()`.
- [x] 5.6 **Verificación de no-regresión:** crear un producto y editarle el stock por el flujo de siempre (`POST` y `PUT /api/productos`), y comprobar en base real que los movimientos generados son idénticos a los de la línea de base de 1.3 — mismo tipo, misma cantidad, mismo `costo_unitario`. Los dos llamadores existentes están en `ProductoServiceImpl` y ninguno debe cambiar de comportamiento. *(POST crea producto nuevo con `AJUSTE_INICIAL` y costo calculado por la fórmula de siempre. PUT editando stock (6→9→6 sobre un producto real) generó `INGRESO`/`EGRESO` con `costo_unitario=2083.20`, idéntico a la fórmula sin el parámetro nuevo — la sobrecarga no afectó los dos llamadores existentes. Movimientos de prueba limpiados después.)*

## 6. Creación de pedidos y confirmación de recepción (backend) — CHECKPOINT OBLIGATORIO

> Este grupo es el corazón del change y el único que mueve inventario. Las tareas 6.6 a 6.12 no se escriben sin el checkpoint de 6.5.

- [x] 6.1 Crear los DTOs: `PedidoDTO` (con `proveedorId`/`proveedorNombre`, `fechaCreacion`, `fechaRecepcion`, `estado`, `usuarioNombre`, `observaciones`, lista de detalles y el total calculado si se confirmó la OQ5), `PedidoDetalleDTO` (con `productoId`/`productoNombre`, `cantidadPedida`, `cantidadRecibida`, `costoUnitarioPactado` y `cantidadPendiente` **calculado**) y `RecepcionPedidoDTO` (lista de `{detalleId, cantidadRecibida}`).
- [x] 6.2 Verificar que ningún DTO expone las entidades `Proveedor`, `Producto`, `Usuario` ni `UnidadNegocio` — sólo ids y nombres (regla dura).
- [x] 6.3 Crear `services/PedidoService.java` + `impl` con `crear`, `obtenerPorId`, `listar` (paginado), `actualizar`, `cancelar`, `eliminar` y `confirmarRecepcion`, todos `@Transactional` y filtrando por unidad de negocio con el patrón de `ClienteServiceImpl`.
- [x] 6.4 Implementar `crear`: estado `PENDIENTE`, `fechaCreacion` **del servidor** (no del payload), `unidadNegocio` y `usuario` del contexto, validando que hay al menos un ítem, que cada cantidad pedida es > 0 y que el proveedor existe y pertenece a la unidad activa. **Verificar explícitamente que no se toca el stock de ningún producto en este método.**
- [x] 6.5 **CHECKPOINT OBLIGATORIO, antes de escribir una línea de `confirmarRecepcion`:** presentar al usuario el pseudocódigo exacto de la secuencia —validar todo primero; después, por cada ítem con `cantidadRecibida > 0`: `producto.setStock(stock + recibida)` y `registrarMovimiento(producto, recibida, INGRESO, usuario, detalle.getCostoUnitarioPactado())`; después calcular el estado; después `fechaRecepcion`— y obtener confirmación explícita de la cantidad que se suma y del costo que se congela.
- [x] 6.6 Implementar las validaciones de `confirmarRecepcion` **antes de modificar nada**: el pedido existe, pertenece a la unidad activa y está en `PENDIENTE`; todos los `detalleId` del payload pertenecen a ese pedido; **todos** los ítems del pedido vienen en el payload; toda `cantidadRecibida` es entera y `>= 0`. Cualquier violación aborta sin efecto.
- [x] 6.7 Implementar el bucle de ingreso: por cada ítem con `cantidadRecibida > 0`, incrementar `producto.setStock(stockActual + cantidadRecibida)` sobre la entidad administrada y llamar la sobrecarga de `registrarMovimiento` con `INGRESO` y el `costoUnitarioPactado` del ítem.
- [x] 6.8 Verificar que los ítems con `cantidadRecibida == 0` **no** generan movimiento de stock ni tocan el producto — un `INGRESO` de cantidad 0 ensucia el historial y puede convertirse en el "último ingreso" que lee la `@Formula` de `costoUnitarioHistorico`.
- [x] 6.9 **Verificación crítica por lectura del código:** confirmar que `confirmarRecepcion` NO llama a `ProductoService.actualizarProducto()` ni a ningún método que lo invoque. Ese método ya genera su propio `MovimientoStock` al cambiar el stock (Context (c) de `design.md`): pasar por ahí produciría **doble movimiento** y con el costo equivocado.
- [x] 6.10 Verificar por lectura del código que `confirmarRecepcion` **no** escribe `producto.setCostoProducto(...)` ni `producto.setPrecio(...)` (Decisión 6): pisar el costo dispararía `calcularPrecioSiAplica()` y movería el precio de venta como efecto colateral.
- [x] 6.11 Implementar el cálculo del estado resultante: `COMPLETO` si **ningún** ítem quedó con `cantidadRecibida < cantidadPedida`; `PARCIAL` si al menos uno quedó corto. El estado lo calcula el servidor a partir de los números, nunca lo elige el payload. Setear `fechaRecepcion`.
- [x] 6.12 Verificar que todo el bucle, el cálculo de estado y el `fechaRecepcion` quedan dentro del **mismo** `@Transactional`, sin `REQUIRES_NEW`, sin `flush()` intermedio y sin llamadas a servicios externos entre medio.
- [x] 6.13 Implementar la matriz de transiciones de la Decisión 2 como **whitelist** (transiciones permitidas), no como lista de estados bloqueados: sólo `PENDIENTE → COMPLETO|PARCIAL|CANCELADO`. `actualizar`, `eliminar`, `cancelar` y una segunda `confirmarRecepcion` deben rechazarse sobre un pedido no `PENDIENTE`.
- [x] 6.14 Implementar la aceptación de `cantidadRecibida > cantidadPedida` (Decisión 10): ingresa la cantidad real, el pedido queda `COMPLETO` y el `cantidadPendiente` del DTO se informa como `0`, nunca negativo (usar `Math.max(0, pedida − recibida)`).
- [x] 6.15 **Checkpoint de cierre:** recorrer con el usuario el diff completo de `confirmarRecepcion` antes de dar el grupo por cerrado, en particular las dos líneas que suman stock y que congelan costo. *(Cerrado: el usuario revisó el diff completo línea por línea y confirmó "sí, cerrar y verificar".)*

## 7. Endpoints de pedidos (backend)

- [x] 7.1 Crear `controllers/PedidoController.java` con `@RequestMapping("/api/pedidos")` y los siete endpoints de la tabla de la Decisión 13, con `@PreAuthorize` por método.
- [x] 7.2 Implementar `GET /api/pedidos` **paginado** (`Pageable`, orden `fechaCreacion` descendente) con filtros opcionales `estado` y `proveedorId`. Regla dura: sin `findAll()` sin límite.
- [x] 7.3 Implementar `POST /api/pedidos/{id}/recepcion` como sub-recurso propio (no un `PUT` del pedido), recibiendo `RecepcionPedidoDTO`.
- [x] 7.4 Implementar `POST /api/pedidos/{id}/cancelar`, verificando que no genera ningún movimiento de stock.
- [x] 7.5 Verificar por lectura del código que el controller no toca repositorios y que ningún endpoint devuelve entidades JPA.
- [x] 7.6 Probar los endpoints crudos con la unidad de negocio Vivero (`X-Unidad-Negocio: 1`) y confirmar que el listado de pedidos y el de proveedores vienen vacíos, y que un `GET /api/pedidos/{id}` de un pedido de Herramientas es rechazado. *(Confirmado: `GET /api/proveedores` y `GET /api/pedidos` con unidad Vivero devuelven vacío; `GET /api/pedidos/{id}` de un pedido real de Herramientas devuelve error "no pertenece a la unidad" al pedirlo con unidad Vivero.)*

## 8. Cliente de API y ABM de proveedores (frontend)

- [x] 8.1 Crear `frontend/src/api/proveedores.api.js` y `frontend/src/api/pedidos.api.js` con el estilo de `clientes.api.js` / `bandejas.api.js`.
- [x] 8.2 Crear `frontend/src/pages/Proveedores.jsx`: listado con el patrón dual (`grid grid-cols-1 gap-4 md:hidden` para tarjetas + `hidden md:block` para tabla), alta, edición y baja.
- [x] 8.3 Crear `frontend/src/components/ProveedorForm.jsx` como modal con el shell fullscreen-mobile de `ProductoForm.jsx` (overlay `p-0 sm:p-4`, panel `w-full h-full sm:h-auto rounded-none sm:rounded-2xl`, header/body/footer `flex-none`/`flex-1 overflow-y-auto`/`flex-none`).
- [x] 8.4 Usar `useUIStore` para todo el feedback: `pushToast` para éxito/error y `askConfirm` para la baja, nombrando el proveedor. Nunca `alert`/`confirm` nativos.
- [x] 8.5 Verificar convenciones: componentes y archivos en PascalCase, `cursor-pointer` en todos los botones, íconos de `lucide-react`.

## 9. Pantallas de pedidos (frontend)

- [x] 9.1 Crear `frontend/src/pages/Pedidos.jsx`: listado paginado con patrón dual tabla/tarjetas, mostrando proveedor, fecha, estado y total, con filtros por estado y proveedor. Los pedidos `PARCIAL` deben distinguirse visualmente de los `COMPLETO`.
- [x] 9.2 Crear el flujo de creación (`PedidoForm.jsx` o página propia): selector de proveedor, agregado de ítems con producto, cantidad y costo unitario, y total en vivo. Inputs numéricos con `FormattedNumberInput`.
- [x] 9.3 Implementar el alta inline de producto dentro del armado del ítem, llamando al `POST /api/productos` **existente** con `stock = 0` (Decisión 3) y usando el id devuelto para el ítem. No crear endpoint nuevo.
- [x] 9.4 Verificar que el producto creado inline nace con `stock = 0` y no con la cantidad pedida — sumaría inventario que todavía no llegó.
- [x] 9.5 Crear `frontend/src/components/RecepcionPedidoModal.jsx`: por cada ítem, input de cantidad recibida **precargado con la cantidad pedida** y editable, más el remanente resultante actualizado en vivo mientras se edita.
- [x] 9.6 Implementar la confirmación explícita vía `askConfirm` antes de mandar la recepción, con el resumen de lo que va a ingresar; y una advertencia adicional que nombre la diferencia cuando algún ítem tenga cantidad recibida mayor a la pedida (Decisión 10).
- [x] 9.7 Verificar que el modal envía **todos** los ítems del pedido en el payload, no sólo los que el usuario tocó — el backend rechaza payloads incompletos (6.6).
- [x] 9.8 Mostrar en el detalle de un pedido confirmado las tres cantidades por ítem: pedida, recibida y pendiente.
- [x] 9.9 Verificar que un pedido `COMPLETO`, `PARCIAL` o `CANCELADO` no ofrece en la UI ninguna acción de edición, cancelación ni reconfirmación.
- [x] 9.10 Verificar que todos los listados y el modal son usables a 320px de ancho, sin desborde horizontal. *(Revisado por el usuario en el navegador — confirmado sin desborde horizontal.)*

## 10. Navegación, rutas y visibilidad por negocio (frontend)

- [x] 10.1 Agregar las rutas `/pedidos` y `/proveedores` en `frontend/src/App.jsx` dentro de un `ProtectedRoute requiredPermission="LEER_PEDIDOS"`, siguiendo el patrón de las rutas existentes.
- [x] 10.2 Agregar los items "Pedidos" y "Proveedores" a `navGroups` en `frontend/src/layouts/DashboardLayout.jsx` con `permission: 'LEER_PEDIDOS'` e íconos de `lucide-react` (ej. `ClipboardList` y `Truck`).
- [x] 10.3 Extender el filtro de visibilidad del layout para que esos dos items se muestren **sólo** cuando `isHerramientas` — es la condición inversa a la que hoy oculta Siembras/Insumos/Devolución de Bandejas, y hay que agregarla sin romper esa lógica existente.
- [x] 10.4 Verificar que con la unidad de negocio Vivero activa el menú no muestra ninguno de los dos items, y que Siembras, Insumos y Devolución de Bandejas siguen comportándose exactamente como antes en ambos negocios. *(Revisado por el usuario en el navegador — confirmado.)*
- [x] 10.5 Verificar que un usuario sin `LEER_PEDIDOS` que navega por URL a `/pedidos` es redirigido al dashboard. *(Revisado por el usuario en el navegador — confirmado.)*

## 11. Verificación funcional en base real

> El proyecto no tiene runner de tests en el frontend ni suite automatizada de backend, por eso este grupo es de verificación manual sobre la base real. Si se decidiera automatizar estas verificaciones, deben usar base real o Testcontainers, **nunca** mocks de base de datos (regla dura del proyecto).

> **Pendiente de este grupo, agregado por pedido del usuario (ver nota en 4.2):** otorgarle `LEER_PEDIDOS` y `ESCRIBIR_PEDIDOS` al rol `ADMIN 2` (id=4, creado a mano desde `UsuariosAdmin.jsx`) vía la API real de roles — el mismo mecanismo que usaría el usuario desde esa pantalla. No se hizo en el apply de código: `DataInitializer` no toca roles creados a mano.

- [x] 11.1 Crear un proveedor y un pedido de 3 ítems, y verificar en base que el pedido quedó `PENDIENTE`, con la unidad de negocio Herramientas, el usuario correcto, y que **el stock de los 3 productos no cambió** respecto de la línea de base de 1.3. *(Confirmado vía API real con header `X-Unidad-Negocio: 2`. Nota: el primer intento sin ese header quedó con `unidad_negocio_id` en NULL — no es un bug del código, es el mecanismo real del proyecto (filtro `X-Unidad-Negocio`), se limpió y se rehizo.)*
- [x] 11.2 Confirmar la recepción completa de un pedido de 1 ítem (10 pedidas, 10 recibidas) y verificar: stock del producto = anterior + 10; **exactamente un** `MovimientoStock` nuevo, de tipo `INGRESO`, cantidad 10; pedido en `COMPLETO`; `fecha_recepcion` poblada. *(Confirmado exacto: stock 2→12, un solo INGRESO cant=10, estado COMPLETO, fecha_recepcion poblada.)*
- [x] 11.3 Sobre ese mismo movimiento, verificar que `costo_unitario` coincide con el cálculo hecho a mano desde el `costoUnitarioPactado` del ítem —`(pactado − descuento%) + envío%` con los porcentajes de 1.1 y 1.3— y **no** con el `costo_producto` del producto. Es la verificación central del change. *(Confirmado en 3 ítems distintos: 2100→2187.36, 5300→5509.35, 10500→10936.80 — los tres coinciden exacto con la fórmula sobre el costo pactado del pedido, no sobre `costo_producto`.)*
- [x] 11.4 Verificar que tras esa confirmación el `costo_producto`, el `precio` y el `porcentaje_ganancia` del producto quedaron **idénticos** a la línea de base de 1.3, y que sólo cambiaron su `stock` y su `costoUnitarioHistorico` derivado. *(Confirmado: los 3 productos de prueba mantuvieron `costo_producto`/`precio`/`porcentaje_ganancia` idénticos a 1.3 después de confirmar recepciones.)*
- [x] 11.5 Confirmar una recepción parcial (10 pedidas, 7 recibidas) y verificar: stock +7, un solo `INGRESO` de 7, pedido en `PARCIAL`, y que el detalle informa pendiente = 3. *(Confirmado: stock 6→13, INGRESO cant=7, estado PARCIAL, `cantidadPendiente`=3.)*
- [x] 11.6 Confirmar una recepción con un ítem en 0 y verificar que ese producto no cambió de stock y que **no** se generó ningún movimiento para él, contando los movimientos totales generados por la operación. *(Confirmado: ítem con cantidadRecibida=0 no tocó el stock (2→2) ni generó movimiento — la operación completa generó exactamente 2 movimientos para 3 ítems, no 3.)*
- [x] 11.7 Confirmar una recepción con cantidad recibida mayor a la pedida (10 pedidas, 12 recibidas) y verificar: stock +12, movimiento por 12, pedido `COMPLETO`, pendiente informado como 0 y no negativo. *(Confirmado: stock 1→13, INGRESO cant=12, `cantidadPendiente`=0 — el pedido quedó PARCIAL en conjunto porque otro ítem del mismo pedido sí tuvo faltante, consistente con que el estado se calcula sobre el pedido completo, no ítem por ítem.)*
- [x] 11.8 Intentar confirmar dos veces el mismo pedido y verificar que la segunda es rechazada y que **no** se generó ningún movimiento adicional ni cambió el stock. *(Confirmado: segundo intento sobre un pedido ya PARCIAL rechazado con "No se puede confirmar la recepción de un pedido en estado PARCIAL", stock sin cambios.)*
- [x] 11.9 Intentar confirmar una recepción con un payload al que le falta un ítem, y otro con una cantidad negativa, y verificar en ambos casos que el stock de todos los productos del pedido quedó intacto y el pedido sigue `PENDIENTE`. *(Confirmados ambos casos, con los mensajes de error esperados y stock/estado intactos.)*
- [x] 11.10 Cancelar un pedido `PENDIENTE` y verificar que ningún producto cambió de stock y que no se generó ningún movimiento. *(Confirmado: `POST /cancelar` devuelve 204, pedido queda CANCELADO, stock sin cambios.)*
- [x] 11.11 Crear un producto inline desde el armado de un pedido y verificar en base que nació con `stock = 0`, con la unidad de negocio Herramientas y con su `AJUSTE_INICIAL` correspondiente; después confirmar la recepción y verificar que su stock queda en la cantidad recibida, no en el doble. *(Confirmado: producto nuevo nació con stock=0 y su `AJUSTE_INICIAL` (cant=0); tras confirmar recepción de 8 unidades, stock quedó en 8, no en 16.)*
- [x] 11.12 Cambiar la unidad de negocio activa a Vivero y verificar en la UI que las secciones Pedidos y Proveedores desaparecen del menú, y en la API que ambos listados vienen vacíos. *(Verificado por API con `X-Unidad-Negocio: 1` (listados vacíos) y la parte de UI revisada por el usuario en el navegador — confirmado.)*
- [x] 11.13 **Checkpoint final con el usuario:** presentarle la tabla comparativa antes/después de los productos de prueba (stock, costo configurado, precio, costo del último ingreso) y confirmar que el resultado es el que esperaba antes de dar el change por terminado. *(Cerrado: el usuario revisó el resultado y confirmó "ya podemos archivar".)*

## 12. Selector de producto por búsqueda en vez de `<select>` (frontend) — pedido del usuario post-apply

> El usuario probó el formulario de pedido y pidió reemplazar el `<select>` nativo de producto por línea por un buscador con filtro en vivo, por practicidad con catálogos grandes. Gobernanza LOW (UI pura, sin lógica de stock/costo nueva).

- [x] 12.1 En `frontend/src/components/PedidoForm.jsx`, reemplazar el `<select>` de producto de cada línea (líneas ~241-252) por un combobox de búsqueda: input de texto que al enfocarse/tipear muestra una lista filtrada de `productos` (case-insensitive, `nombre.includes(busqueda)`), siguiendo el mismo estilo visual que el buscador de `NuevaVenta.jsx` (icono `Search`, `input` con `pl-10`, lista de resultados debajo). *(Implementado como `ProductoSearchSelect`, componente de módulo definido antes de `PedidoForm` en el mismo archivo. Verificado por lectura de código, no en navegador — sin backend/navegador disponible en esta sesión.)*
- [x] 12.2 Cada línea del pedido mantiene su propio estado de búsqueda (texto tipeado) independiente de las demás líneas — no un solo buscador global para todas. *(`busqueda`/`editando` son `useState` internos de `ProductoSearchSelect`, instanciado una vez por línea vía `items.map`; cada instancia tiene su propio estado de React. Verificado por lectura de código.)*
- [x] 12.3 Al hacer click en un resultado, setear `productoId`/`productoNombre` de esa línea (reutilizando `seleccionarProducto`) y cerrar la lista de resultados, mostrando el nombre elegido en el input en vez de la búsqueda. *(`handleSeleccionar` llama a `onSelect(id)` — que en `PedidoForm` es `seleccionarProducto(it.lineaId, productoId)`, sin reescribirla — y luego `setEditando(false)`, lo que hace que el combobox pase a mostrar `productoNombre` en un botón. Verificado por lectura de código, no probado en navegador.)*
- [x] 12.4 Preservar la opción "+ Crear producto nuevo…" dentro del mismo flujo de búsqueda (ej. como último ítem de la lista de resultados, o un botón visible cuando no hay coincidencias) — no se pierde la función de alta inline (Decisión 3 de `design.md`). *(Aparece como último ítem de la lista desplegable, siempre que hay texto de búsqueda, tenga o no coincidencias; dispara `onSelect('__nuevo__')` que resuelve en el `seleccionarProducto` ya existente (no reescrito) y abre el sub-formulario inline sin cambios. Verificado por lectura de código.)*
- [x] 12.5 Permitir deseleccionar/cambiar el producto elegido de una línea ya completada (volver a abrir el buscador sobre esa línea). *(Cuando hay producto elegido, el combobox muestra un botón con el nombre + etiqueta "Cambiar"; el click reabre la búsqueda (`abrirBusqueda` → `setEditando(true)`). Verificado por lectura de código, no en navegador.)*
- [x] 12.6 Verificar que el resto de la línea (cantidad, costo unitario, borrar línea) no cambió de comportamiento. *(Sólo se tocó el primer `<div>` del grid `sm:grid-cols-[1fr_auto_auto_auto]`; los otros tres hijos — `FormattedNumberInput` de cantidad, `FormattedNumberInput` de costo unitario y el botón `Trash2` de borrar línea — quedaron byte-a-byte iguales. Verificado por diff/lectura de código.)*
- [x] 12.7 Verificar convenciones: `cursor-pointer` en los resultados clickeables, sin romper el layout a 320px de ancho. *(`cursor-pointer` presente en el botón "Cambiar", en cada resultado de la lista y en "+ Crear producto nuevo…"; el combobox ocupa el mismo primer `<div>` (columna `1fr`) que antes tenía el `<select>`, sin anchos fijos nuevos que puedan desbordar a 320px. Verificado por lectura de código, no medido en navegador real — sin navegador disponible en esta sesión.)*

## 13. Producto nuevo: creación diferida hasta la recepción (backend + frontend) — CHECKPOINT OBLIGATORIO, reemplaza la Decisión 3 original

> Pedido del usuario post-apply: hoy (Decisión 3 de `design.md`, grupo 9.3) un producto nuevo se crea de inmediato al armar el pedido, con `stock=0`, y ensucia el catálogo con "productos fantasma" mientras el pedido está en camino. Se reemplaza por completo: el producto nuevo NO se crea al armar el pedido — sólo se guardan sus datos mínimos (nombre, precio de venta) en la línea del pedido, y el `Producto` real recién se crea al confirmar la recepción, únicamente para los ítems que efectivamente llegaron (`cantidadRecibida > 0`). Si un ítem "pendiente de crear" se confirma con 0 unidades recibidas, no se crea nada.
>
> **Gobernanza MEDIA-ALTA, igual que el grupo 6 original** (esto ES el grupo 6, extendido). El checkpoint de la tarea 13.5 es obligatorio, mismo motivo que el original: la creación de un producto + su primer ingreso de stock queda anclada a un costo que, si está mal, envenena en silencio la valuación de inventario. **Confirmado por el usuario, no volver a preguntar**: (a) esto reemplaza por completo la Decisión 3 original, no coexisten las dos formas; (b) los datos mínimos que se piden en el pedido para un producto pendiente son nombre y precio de venta — el costo pactado ya existe por línea, como en cualquier ítem; (c) si la cantidad recibida termina en 0, no se crea el producto; (d) el mecanismo exacto (crear el producto en `stock=0` reutilizando el flujo existente de alta, y recién después aplicarle la misma lógica de ingreso que a cualquier producto existente, todo en la misma transacción única — si algo falla a mitad de camino se revierte TODO el pedido, incluidos productos ya creados y stock ya sumado de otras líneas de la misma confirmación) fue mostrado y confirmado explícitamente antes de escribir código.

- [x] 13.1 En `backend/src/main/java/com/vivero/gestion/models/PedidoDetalle.java`, hacer `producto` nullable (sin `nullable = false` si lo tenía) y agregar `productoNombreNuevo` (`String`, nullable) y `productoPrecioNuevo` (`BigDecimal`, `precision=12, scale=2`, nullable) — usados sólo mientras `producto` es `null`. *Verificado: el `@JoinColumn(name = "producto_id")` nunca tuvo `nullable = false` (default `true` de JPA); se dejó explícito `nullable = true` para que quede documentado. Se agregaron `productoNombreNuevo`/`productoPrecioNuevo` con getters/setters.*
- [x] 13.2 En `PedidoServiceImpl.crear()`, validar por ítem: viene `productoId` **o** vienen `productoNombreNuevo` + `productoPrecioNuevo`, nunca ambos ni ninguno. `costoUnitarioPactado` sigue siendo obligatorio en todos los casos, como ya lo es hoy. *Implementado en el helper `validarItemDetalle()`, reusado también por `actualizar()`. Precio del producto nuevo debe ser > 0.*
- [x] 13.3 Eliminar del flujo de creación de pedido cualquier llamada a `ProductoService.crear()` — ya no existe alta inline al armar el pedido. Verificar que `crear()` sigue sin tocar stock de ningún producto (la verificación de la tarea 6.4 original sigue valiendo). *Verificado por lectura: `crear()`/`actualizar()` nunca llamaron a `ProductoService` (sólo a `buscarProducto()` cuando `productoId` viene informado); no hay ninguna llamada nueva a agregar/quitar. `productoRepository.save`/stock siguen sin tocarse en `crear()`.*
- [x] 13.4 Actualizar `PedidoDetalleDTO` para exponer `productoNombreNuevo`/`productoPrecioNuevo` cuando `producto` es `null`, y seguir exponiendo `productoId`/`productoNombre` normalmente cuando no lo es. *Campos agregados al DTO; `mapDetalleToDTO()` sólo los completa cuando `detalle.getProducto() == null`.*
- [x] 13.5 **CHECKPOINT OBLIGATORIO, antes de tocar `confirmarRecepcion`:** ya confirmado por el usuario (ver nota del grupo) — registrar acá esa confirmación antes de escribir el código. *Confirmación ya obtenida por el usuario antes de este apply (ver briefing de la tarea): reemplazo total de la Decisión 3, datos mínimos nombre+precio, cantidad 0 no crea producto, y mecanismo exacto de `confirmarRecepcion` (crear vía `ProductoService.crearProducto()`, enlazar, luego bloque de ingreso único, todo en la misma transacción).*
- [x] 13.6 En `PedidoServiceImpl.confirmarRecepcion()`, para cada ítem con `producto == null` (pendiente) y `cantidadRecibida > 0`: crear el `Producto` reutilizando `ProductoService.crear()` (nombre y precio de la línea, `stock=0`, `unidadNegocio` de Herramientas — mismo flujo que ya existe y ya genera su `AJUSTE_INICIAL`), setear `detalle.setProducto(...)` con el producto recién creado, y a partir de ahí aplicar **exactamente** la misma lógica de ingreso que a un producto existente (sumar `cantidadRecibida` al stock + `registrarMovimiento(..., INGRESO, ..., costoUnitarioPactado)`), sin caminos de código separados. *Implementado: el método real se llama `ProductoService.crearProducto(ProductoDTO)` (no `crear()` — ese nombre no existe en la interfaz actual), reutilizado tal cual. `unidadNegocio` sale de `UnidadNegocioContextHolder` dentro de `crearProducto()`, que ya está en contexto Herramientas porque `UnidadNegocioFilter` lo fija por request. Un único bloque `if (recibida > 0) { if (producto == null) { crear+enlazar } ; stock+=recibida; registrarMovimiento(...) }` — no hay `if/else` separado para nuevo vs. existente.*
- [x] 13.7 Verificar que un ítem pendiente con `cantidadRecibida == 0` no crea ningún `Producto` ni genera ningún movimiento — la línea queda tal cual, sin producto asociado. *Verificado por lectura: la creación+ingreso está dentro de `if (recibida > 0)`; con `recibida == 0` sólo se ejecuta `detalle.setCantidadRecibida(0)`, sin tocar `producto`.*
- [x] 13.8 Verificar que toda la operación —creación de uno o más productos pendientes, más el ingreso de todas las líneas (pendientes y existentes)— sigue dentro del mismo `@Transactional` único, sin `REQUIRES_NEW` ni flush intermedio: si falla la creación del tercer producto pendiente de la lista, se revierte todo el pedido, incluidos los productos ya creados y el stock ya sumado de las líneas anteriores de esa misma confirmación. *Verificado por lectura: `confirmarRecepcion()` mantiene su único `@Transactional` (propagación por defecto). `ProductoServiceImpl.crearProducto()` también es `@Transactional` sin `propagation`, por lo que se une a la transacción existente (REQUIRED, no REQUIRES_NEW). No hay `flush()`/`saveAndFlush()` en ningún punto del loop — el `productoRepository.save()`/`movimientoStockService.registrarMovimiento()` quedan pendientes de commit hasta el final del método, así que una excepción a mitad del loop revierte todo (productos ya creados incluidos, por rollback de la transacción JPA).*
- [x] 13.9 En `frontend/src/components/PedidoForm.jsx`, cambiar el sub-formulario de "+ Crear producto nuevo…" (abierto desde `ProductoSearchSelect`, grupo 12) para que YA NO llame a `productosApi.create()`: sólo captura nombre y precio de venta y los guarda en la línea local (`productoNombreNuevo`/`productoPrecioNuevo`), sin `productoId`. La línea debe mostrarse visualmente distinta (ej. badge "Nuevo — se crea al confirmar") en vez de mostrar un nombre de producto existente. *Implementado: `confirmarProductoPendiente()` reemplaza a `handleCrearProductoInline()`, sin llamada a la API. `ProductoSearchSelect` recibe `productoNombreNuevo` y renderiza badge ámbar "Nuevo — se crea al confirmar" en vez del botón normal cuando la línea está pendiente.*
- [x] 13.10 En `frontend/src/components/RecepcionPedidoModal.jsx`, mostrar el nombre pendiente (`productoNombreNuevo`) para esas líneas en vez de `productoNombre`, y dejar claro en el texto de confirmación (`askConfirm`) que confirmar con cantidad > 0 va a dar de alta ese producto nuevo en el catálogo. *Implementado: helpers `nombreLinea()`/`esLineaPendiente()`, badge en la vista de línea, y el mensaje de `askConfirm` agrega un aviso explícito cuando hay líneas "(nuevo)" con cantidad > 0.*
- [x] 13.11 Verificar que el payload de `POST /api/pedidos` para una línea pendiente manda `productoNombreNuevo`/`productoPrecioNuevo` y no manda `productoId`, y que el payload de `POST /api/pedidos/{id}/recepcion` no cambia de forma (sigue siendo sólo `detalleId`/`cantidadRecibida` por ítem — el backend ya tiene todo lo que necesita desde el pedido guardado). *Verificado por lectura: `handleSubmit()` arma cada detalle con `it.productoId ? {productoId,...} : {productoNombreNuevo, productoPrecioNuevo,...}` — mutuamente excluyentes. `pedidosApi.confirmarRecepcion()` y `RecepcionItemDTO` no se tocaron: siguen siendo sólo `detalleId`/`cantidadRecibida`.*
- [x] 13.12 **Checkpoint de cierre:** mostrar el diff completo de `confirmarRecepcion` (la parte nueva de creación diferida) antes de dar el grupo por cerrado. *Fragmento nuevo devuelto en el resumen final de la tarea al usuario.*

## 14. Verificación funcional en base real — grupo 13

> **Desbloqueado:** el usuario autorizó explícitamente `docker compose up -d --build backend`. Build exitoso (Maven `BUILD SUCCESS`, 17.7s), el contenedor arrancó sano (`Started BackendApplication in 20.671 seconds`, Hibernate corrió las dos `ALTER TABLE` de `producto_nombre_nuevo`/`producto_precio_nuevo`, sin errores en el log). Verificación funcional hecha contra la base real, con proveedor `id=4 SHIMURA` y el producto real `id=5 "Cinta métrica"` (Herramientas). Datos de prueba limpiados al final (ver detalle en cada tarea).

- [x] 14.1 Crear un pedido con una línea a producto existente y una línea "pendiente de crear" (nombre + precio nuevos), y verificar en base que la línea pendiente no generó ningún `Producto` ni ningún movimiento de stock. *Confirmado: pedido `id=7` creado con detalle `id=12` (producto existente `id=5`, sin tocar su stock) y detalle `id=13` (`productoNombreNuevo="Producto Prueba G14 A"`, `productoId=null`). En base: cero filas en `productos` con ese nombre, `movimientos_stock` sin fila nueva (último id seguía siendo el previo a la prueba), producto `id=5` con `stock=2` sin cambio, pedido en `PENDIENTE`.*
- [x] 14.2 Confirmar la recepción con la línea pendiente en cantidad > 0, y verificar: se creó exactamente un `Producto` nuevo con el nombre/precio del pedido, `unidad_negocio_id` de Herramientas, `stock` final = cantidad recibida (no doble), y existen sus dos movimientos (`AJUSTE_INICIAL` en 0 seguido de `INGRESO` con el costo pactado del pedido). *Confirmado exacto: `POST /api/pedidos/7/recepcion` con detalle 12 (recibida=5) y detalle 13 (recibida=4, pendiente) creó `Producto id=16` ("Producto Prueba G14 A", precio=1500.00, `unidad_negocio_id=2`, `stock=4` — no 8). Sus movimientos: `AJUSTE_INICIAL` cantidad=0, `costo_base=0.00`, `costo_unitario=0.00`; luego `INGRESO` cantidad=4, `costo_base=800.00` (el `costoUnitarioPactado` del ítem), `descuento_porcentaje=0.00` (producto nuevo, sin descuento propio), `envio_porcentaje=5.00` (Herramientas), `costo_unitario=840.00` — fórmula exacta `(800 − 0%) + 5% = 840`. El detalle del producto existente (id=5) también se verificó en el mismo request: stock `2→7` (+5) y su `INGRESO` con `costo_unitario=5197.50` (`5000 − 1% descuento = 4950`, `+5% envío = 5197.50`), consistente con la fórmula del grupo 5/11.*
- [x] 14.3 Confirmar otra recepción con una línea pendiente en cantidad = 0, y verificar que no se creó ningún `Producto` nuevo ni ningún movimiento para esa línea. *Confirmado: pedido `id=8` con un único detalle pendiente (`id=14`, "Producto Prueba G14 B"). `POST /recepcion` con `cantidadRecibida=0` devolvió el detalle con `productoId=null` (no se enlazó ningún producto), pedido en `PARCIAL` (pendiente=3). En base: cero filas en `productos` con ese nombre, `movimientos_stock` sin fila nueva.*
- [x] 14.4 Confirmar una recepción con dos líneas pendientes a la vez, una de las cuales debería fallar la creación (ej. nombre vacío forzado a mano contra el endpoint crudo), y verificar que **ningún** producto de esa confirmación quedó creado ni ningún stock se movió — todo o nada. *Confirmado: pedido `id=9` con 3 líneas — detalle 15 (producto existente `id=5`, stock previo=7), detalle 16 (pendiente válido "Producto Prueba G14 D-ok"), detalle 17 (pendiente "Producto Prueba G14 D-fail", forzado a `producto_nombre_nuevo=NULL` por SQL directo para simular el caso, ya que `validarItemDetalle` bloquea un nombre vacío/nulo en la creación normal del pedido — bypass necesario para llegar al escenario de fallo dentro de `confirmarRecepcion`). `POST /recepcion` con las 3 cantidades en 2 devolvió `409 Conflict` (violación de `nombre NOT NULL` en `productos` al intentar `crearProducto()` con nombre nulo). Verificado en base tras el fallo: producto `id=5` con `stock=7` sin cambio (no quedó en 9), cero filas nuevas en `productos`, `movimientos_stock` sin ninguna fila nueva de las 3 líneas, pedido `id=9` siguió en `PENDIENTE`, y los 3 `pedido_detalles` quedaron con `cantidad_recibida=NULL` — ningún efecto parcial sobrevivió, confirma el rollback atómico del `@Transactional` único (tarea 13.8) incluso mezclando una línea de producto existente con líneas pendientes.*

**Limpieza de datos de prueba:** se revirtieron todos los efectos de las 4 pruebas mediante SQL directo dentro de una transacción: se borraron los movimientos de prueba (ids 60-62), se restauró el `stock` del producto real `id=5` ("Cinta métrica") de 7 a su valor previo de 2, se borraron los `pedido_detalles` y `pedidos` de prueba (ids 7, 8, 9) y el producto de prueba `id=16`. Verificado después: `movimientos_stock` volvió a 59 filas, `pedidos` volvió a 6, producto `id=5` con `stock=2`, y ninguna fila con nombre `%Prueba G14%` quedó en `productos`. No quedó ningún dato de prueba sin limpiar.
