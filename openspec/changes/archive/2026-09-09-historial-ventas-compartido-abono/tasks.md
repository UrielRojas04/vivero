## 0. Alcance y red de seguridad

> **Archivos que este change NO toca bajo ninguna circunstancia:** `RendicionColegaServiceImpl.java`, `RendicionColega*.java` (modelos, DTOs, controller), `StockAbonoServiceImpl.java`, `MovimientoStockAbono.java`, `Venta.java`, `Pago.java`, `VentaResponseDTO.java`, `CuentaAbonoFilter.java`, `CuentaAbonoContextHolder.java`, `CuentaAbono.java` y **todo el frontend**. Si una task parece pedir tocarlos, está mal interpretada: releer `design.md` (Non-Goals).
>
> **Advertencia sobre el baseline (Decisión 3 de `design.md`):** el primer test de `VentaServiceListarVentasAbonoTest` (`unidadAbonoConCuentaJefeSoloDevuelveVentasDeJefe`) afirma hoy lo contrario de lo que este change implementa. Su cambio de resultado **es el objetivo**, así que NO sirve como "baseline que debe quedar idéntico". El baseline real de este change son: el **segundo** test de ese mismo archivo (`unidadViveroNoFiltraPorCuentaAbonoAunConContextoResidual`) y los tests de rendición.

- [x] 0.1 Correr la suite backend completa contra Postgres real (`localhost:5433`, `DB_USER=admin DB_PASS=root JWT_SECRET=testsecrettestsecrettestsecrettestsecret`) y anotar el baseline (`N tests, M passing`). Si algo ya falla antes de tocar nada, reportarlo como fallo preexistente y **NO** arreglarlo. Nota de entorno conocida (heredada de changes anteriores): si el ApplicationContext no levanta por timezone, agregar `-DargLine="-Duser.timezone=UTC"` a `mvn test`.
  - Baseline: **201 tests, 198 passing, 3 failures preexistentes** (no tocados): `UnidadNegocioConfigTest.testModeloCostoSeededCorrectly`, `GastoServiceInsumosPorUnidadTest.listarGastosDeAbonoIncluyeSusPropiosInsumos`, `GastoServiceInsumosPorUnidadTest.listarGastosDeViveroSigueIncluyendoSusPropiosInsumos`. Ninguno relacionado con ventas/rendición/abono-cuenta.
- [x] 0.2 Confirmar que el baseline incluye en verde `VentaServiceListarVentasAbonoTest` (2 tests), `RendicionColegaLiquidacionModoColegaTest` y `RendicionColegaLiquidacionGastosManualesTest`, y anotar el número de tests de cada uno. Estos tres son la referencia de comparación exacta al cierre — con la excepción documentada del primer test de `VentaServiceListarVentasAbonoTest`.
  - `VentaServiceListarVentasAbonoTest`: 2/2 verde. `RendicionColegaLiquidacionModoColegaTest`: 2/2 verde. `RendicionColegaLiquidacionGastosManualesTest`: 2/2 verde.
- [x] 0.3 Verificar por `grep` que `findAllByUnidadNegocioIdAndCuentaAbonoOrderByFechaDesc` tiene exactamente dos apariciones en `backend/src` (su declaración en `VentaRepository` y su única llamada en `VentaServiceImpl`). Si aparece en algún otro lado, **detenerse y reportarlo**: el relevamiento del design quedó incompleto y el alcance cambia.
  - Confirmado: exactamente 2 apariciones (`VentaRepository.java:21` declaración, `VentaServiceImpl.java:344` única llamada).

## 1. Historial compartido: reescribir la especificación en código (RED)

- [x] 1.1 RED — En `backend/src/test/java/com/vivero/gestion/services/VentaServiceListarVentasAbonoTest.java`, reescribir el test `unidadAbonoConCuentaJefeSoloDevuelveVentasDeJefe` como `unidadAbonoDevuelveVentasDeAmbasCuentasConCualquieraActiva`: con una venta de `CuentaAbono.JEFE` y una de `CuentaAbono.COLEGA` en la base, listar con `CuentaAbono.JEFE` en contexto y afirmar que el resultado **contiene ambas**. Mantener el patrón del archivo (helper `crearVentaMinima`, limpieza en `@AfterEach` de `UnidadNegocioContextHolder`, `CuentaAbonoContextHolder` y las ventas creadas). **No** tocar el segundo test del archivo. Correr y confirmar que **falla** con el código actual.
  - Confirmado RED: `Tests run: 2, Failures: 1` — falla exactamente el test reescrito (`Expecting [557, 511] to contain [557, 558]`); el segundo test (Vivero) sigue verde, sin tocarlo.
- [x] 1.2 TRIANGULAR (RED) — En el mismo test, agregar la dirección inversa: con `CuentaAbono.COLEGA` en contexto, el historial también contiene las dos ventas. Correr y confirmar que también falla. (Ver `ClienteAgendaCompartidaAbonoTest` del change archivado `clientes-compartidos-abono` como referencia de estilo para las dos direcciones.)
  - Implementado como una segunda aserción dentro del mismo test (ambas direcciones cubiertas en un solo método, mismo patrón de datos). RED confirmado en la misma corrida de 1.1 (la aserción con JEFE activo ya falla primero).
- [x] 1.3 Actualizar el javadoc de clase del test: hoy describe el hallazgo #2 de la auditoría de `negocio-abono` y explica por qué el gate por unidad era necesario para el filtro por cuenta. Reescribirlo para que refleje el comportamiento nuevo (historial compartido) y para que quede claro que el segundo test sigue protegiendo a Vivero de un filtro por cuenta que nunca debió aplicarle.
- [x] 1.4 HALLAZGO — Alcance extendido durante 1.1: `ClienteAgendaCompartidaAbonoTest.ventasDeAbonoSobreClienteCompartidoSiguenParticionadasPorCuenta` (change archivado `clientes-compartidos-abono`, Grupo 4, líneas ~246-262) documentaba explícitamente que las ventas seguían particionadas por cuenta sobre un cliente compartido — exactamente la afirmación que este change invierte. Dejarlo intacto habría dejado una guarda de regresión falsa (verde con el código viejo, y su nombre habría seguido afirmando partición incluso después de 2.1). Decisión: reescribirlo con el mismo tratamiento que el test principal — renombrado a `ventasDeAbonoSobreClienteCompartidoTambienQuedanCompartidas`, aserción invertida cubriendo las dos direcciones (JEFE activo y COLEGA activo), comentario del bloque `Grupo 4` actualizado (ya no afirma partición; cross-referencia este change) y javadoc del método con la cita a `VentaServiceListarVentasAbonoTest`. RED confirmado antes de tocar `VentaServiceImpl`: `Tests run: 8, Failures: 1` en `ClienteAgendaCompartidaAbonoTest` — falla exactamente el test reescrito (`[559, 511]` no contiene `560`), los otros 7 tests del archivo (grupos 1-3) siguen verdes sin tocarlos.

## 2. Historial compartido: quitar la partición (GREEN)

- [x] 2.1 GREEN — En `backend/src/main/java/com/vivero/gestion/services/impl/VentaServiceImpl.java`, método `listarVentas()`: eliminar la rama `if (esUnidadAbono && cuentaAbono != null)` completa, dejando `ventas = ventaRepository.findAllByUnidadNegocioIdOrderByFechaDesc(unidadId);` cuando `unidadId != null` y el `else` con `findAllByOrderByFechaDesc()` intacto. Correr los tests de 1.1 y 1.2 hasta verde.
- [x] 2.2 REFACTOR — En el mismo método, eliminar el código que quedó muerto: la variable local `cuentaAbono` (la lectura de `CuentaAbonoContextHolder.getCuentaAbono()` **dentro de `listarVentas()`**), la variable local `esUnidadAbono` con su `unidadNegocioRepository.findById(unidadId).map(...)`, y el bloque de comentario que justificaba el gate por unidad (documenta una decisión que este change deroga).
  - Nota: 2.1 y 2.2 se hicieron en un solo edit (el reemplazo mínimo del método ya dejaba fuera todo el código muerto), no en dos pasos separados. El resultado final es idéntico al pedido: el método queda en 6 líneas (`unidadId != null` → `findAllByUnidadNegocioIdOrderByFechaDesc`; si no, `findAllByOrderByFechaDesc()`), sin la variable `cuentaAbono`, sin `esUnidadAbono` y sin el comentario que justificaba el gate.
- [x] 2.3 REFACTOR — Verificar que **NO** se eliminen: el campo `unidadNegocioRepository`, su import ni su parámetro de constructor (sigue usándose en `registrarVenta`, líneas ~102 y ~137), ni el import de `CuentaAbonoContextHolder` (sigue usándose en ~línea 141 para estampar la cuenta en el alta). Confirmar por `grep` que ambos siguen teniendo usos reales en el archivo antes de dar el refactor por cerrado.
  - Confirmado por grep: `unidadNegocioRepository` usado en líneas 102 y 137 (`registrarVenta`), `CuentaAbonoContextHolder` usado en línea 141. Ambos con usos reales fuera de `listarVentas()`.
- [x] 2.4 Compilar y correr `VentaServiceListarVentasAbonoTest` completo: los dos casos nuevos en verde y el test de Vivero (`unidadViveroNoFiltraPorCuentaAbonoAunConContextoResidual`) en verde **sin haberlo tocado**.
  - Confirmado: `Tests run: 2, Failures: 0`. También se corrió `ClienteAgendaCompartidaAbonoTest` completo como verificación extra (task 1.4): `Tests run: 8, Failures: 0` — el test reescrito pasó a verde sin tocarlo de nuevo.

## 3. Eliminar la query particionada (Decisión 2 de design.md)

- [x] 3.1 Eliminar de `backend/src/main/java/com/vivero/gestion/repositories/VentaRepository.java` el método `findAllByUnidadNegocioIdAndCuentaAbonoOrderByFechaDesc` (línea ~21), que queda sin llamadores tras 2.1.
- [x] 3.2 Verificar por `grep` que `sumarTotalVentasPorCuentaYPeriodo` **sigue presente** en `VentaRepository` y que sus llamadores en `RendicionColegaServiceImpl` (~líneas 183-184) están intactos. Es la query que alimenta la liquidación del colega: borrarla por confusión con la anterior rompería el cálculo de la rendición.
  - Confirmado: `sumarTotalVentasPorCuentaYPeriodo` intacta en `VentaRepository.java:88`, llamada desde `RendicionColegaServiceImpl.java:183-184` sin cambios.
- [x] 3.3 Compilar (`mvn test-compile`). Un error de compilación acá significa que quedó un consumidor sin relevar en 0.3: identificarlo y reportarlo antes de seguir, **no** silenciarlo.
  - `mvn test-compile` exitoso, sin errores. Confirma que 0.3 relevó correctamente los dos únicos usos (declaración + única llamada), ya eliminada esta última en 2.1.

## 4. Guarda de regresión: rendición, stock y otras unidades

- [x] 4.1 GUARD — Correr `RendicionColegaLiquidacionModoColegaTest` y `RendicionColegaLiquidacionGastosManualesTest` y confirmar **el mismo resultado exacto** que en el baseline de 0.2. Deben pasar en verde sin tocar una sola línea de código de rendición: si alguno falla, el change se desbordó de su alcance y hay que revisar 2.1/3.1 antes de continuar.
  - Confirmado: `RendicionColegaLiquidacionModoColegaTest` 2/2, `RendicionColegaLiquidacionGastosManualesTest` 2/2 — idéntico al baseline de 0.2. Cero código de rendición tocado.
- [x] 4.2 GUARD — Correr `RendicionColegaDireccionTest` y `RendicionColegaOrdenHistorialTest`, y los tests de ventas colaterales `VentaServiceObtenerPorIdTest`, `VentaResponseDocumentoTest`, `VentaDocumentoCasualTest` y `PrecioAjustadoVentaTest`. Todos deben quedar igual que en el baseline (ninguno consulta el historial filtrado por cuenta).
  - Confirmado, todos en verde: `RendicionColegaDireccionTest` 5/5, `RendicionColegaOrdenHistorialTest` 1/1, `VentaServiceObtenerPorIdTest` 3/3, `VentaResponseDocumentoTest` 4/4, `VentaDocumentoCasualTest` 4/4, `PrecioAjustadoVentaTest` 9/9. Cero fallas.
- [x] 4.3 Verificar por `grep` que `cuentaAbono` **sigue** presente y sin cambios en `Venta.java`, `Pago.java`, `MovimientoStockAbono.java`, `StockAbonoServiceImpl.java` y `RendicionColegaServiceImpl.java`, y en las líneas de `VentaServiceImpl` que estampan la cuenta al registrar la venta (~141-145, ~177, ~247, ~406). La atribución por cuenta se conserva; sólo cambió el criterio de consulta del listado.
  - Confirmado en los 5 archivos. En `VentaServiceImpl`: estampado en alta (141/145), paso a `stockAbonoService.registrarVenta` (177), estampado en pago (247) y en el segundo punto de creación de pago (389, la línea real del tercer punto mencionado en la task — el número ~406 era aproximado). Todos intactos.

## 5. Corregir las specs que documentaban la partición (Decisión 4 de design.md)

> Estas ediciones son sobre **specs principales ya sincronizadas** (`openspec/specs/`), no sobre los changes archivados. Los directorios de `openspec/changes/archive/` **no se tocan**: quedan como registro histórico de lo que se decidió en su momento.

- [x] 5.1 En `openspec/specs/historial-cobros-abono/spec.md`, requirement *"El Historial No Se Particiona por Cuenta de Abono"*: corregir la frase normativa que dice que la no-partición del historial de cobros "SHALL ser independiente de la partición que siguen aplicando **las ventas**, el stock y la rendición del colega", para que ya no afirme que las ventas siguen particionadas — la partición que permanece es la del **stock y la rendición**, y lo que permanece en las ventas es la **atribución** de cada venta a su cuenta, no la partición del listado.
- [x] 5.2 En el mismo archivo, reescribir el escenario *"La partición de ventas sigue vigente (guard de no regresión)"*: renombrarlo y reformular su `WHEN`/`THEN` para reflejar que el historial de ventas de Abono **también** es compartido entre ambas cuentas (cross-referenciar el change `historial-ventas-compartido-abono` en el texto para que quede rastreable de dónde salió la corrección).
  - Renombrado a "El historial de ventas de Abono también es compartido (corregido por `historial-ventas-compartido-abono`)".
- [x] 5.3 En `openspec/specs/backend-clientes/spec.md`, requirement *"Agenda de Clientes Compartida en Abono"*: corregir la frase "La atribución de ventas, pagos, movimientos de stock de Abono y rendición del colega a la cuenta operativa MUST permanecer intacta: cada cuenta sigue teniendo **sus propias ventas**, su propio stock y su propia rendición", para que la **atribución** siga afirmándose pero deje de implicar que el historial de ventas es privado de cada cuenta.
- [x] 5.4 En el mismo archivo, reescribir el escenario *"Ventas, stock y rendición siguen particionados por cuenta (guarda de regresión)"*: el `THEN` que dice "el listado devuelve únicamente las ventas de la cuenta activa" es lo que este change invierte. Reformularlo para que el guard siga cubriendo **stock y rendición** (que sí siguen por cuenta) y que la parte de ventas afirme el listado compartido, cross-referenciando `historial-ventas-compartido-abono`.
  - Renombrado a "Stock y rendición siguen particionados por cuenta; el historial de ventas ahora también es compartido (corregido por `historial-ventas-compartido-abono`)".
- [x] 5.5 Verificar por `grep` sobre `openspec/specs/` que no queda ninguna otra afirmación de que el historial de ventas de Abono está particionado por cuenta. Si aparece alguna no prevista, corregirla con el mismo criterio y dejarla anotada en el resumen.
  - Grep de `partici|particionad` sobre `openspec/specs/` da 4 archivos: los dos ya corregidos (5.1-5.4) más `costeo-productos/spec.md` y `configuracion-proveedores/spec.md`, ambos con matches falsos positivos de la palabra "participar" (proveedor no participa de la resolución de IVA/envío) — sin relación con ventas/Abono. Grep adicional de frases específicas (`propias ventas`, `ventas de esa cuenta`, `ventas de la cuenta activa`) sobre todo `openspec/specs/` no encontró ninguna otra ocurrencia. No hay hallazgos no previstos que corregir.

## 6. Cierre

- [x] 6.1 Correr la suite backend completa y comparar contra el baseline de 0.1: mismos tests verdes, cero regresiones, con la única diferencia esperada del test reescrito en 1.1/1.2. Anotar el conteo final.
  - Final: **201 tests, 198 passing, 3 failures** — exactamente las mismas 3 fallas preexistentes del baseline de 0.1 (`UnidadNegocioConfigTest.testModeloCostoSeededCorrectly`, `GastoServiceInsumosPorUnidadTest.listarGastosDeAbonoIncluyeSusPropiosInsumos`, `GastoServiceInsumosPorUnidadTest.listarGastosDeViveroSigueIncluyendoSusPropiosInsumos`), ninguna relacionada con ventas/rendición/abono-cuenta. Cero regresiones. Conteo total idéntico al baseline (201/198/3).
- [x] 6.2 Confirmar que **no se tocó ningún archivo del frontend**. `frontend/src/pages/HistorialVentas.jsx` consume `ventasApi.listarVentas()` tal cual y no necesita cambios (design.md, punto 6 del contexto). Si pareciera hacer falta tocar frontend, es un hallazgo a reportar, **no** a implementar sobre la marcha.
  - Confirmado: ningún archivo de `frontend/` fue tocado en este change. El repo tenía modificaciones de frontend preexistentes de otros changes en curso (`retiro-ganancia-abono`, `repique-devolucion-bandejas`), ajenas a este trabajo y no relacionadas con `HistorialVentas.jsx`.
- [ ] 6.3 Verificación manual en la UI (no hay test runner de frontend en este repo, así que esto es checklist, no test): con dos sesiones reales (Sergio y Pablo) en Abono, abrir `/ventas/historial` y confirmar que ambos ven la misma lista completa y que cada fila indica quién registró la venta.
- [ ] 6.4 Verificación manual en la UI: abrir la pantalla de rendición/liquidación del colega y confirmar que los totales por cuenta siguen mostrando la separación entre Sergio y Pablo, con los mismos números que antes del cambio. Es la comprobación de negocio del guard de la sección 4.
- [x] 6.5 Dejar registrado en el resumen final: qué archivos de backend se tocaron, el conteo de tests antes/después, y que las correcciones de spec de la sección 5 se hicieron a mano sobre `openspec/specs/` (Decisión 4) — para que quien archive este change sepa que esas dos capabilities ya están sincronizadas y no espere un delta que las cubra.
  - Registrado en el resumen final entregado al usuario (fuera de este archivo): archivos backend tocados (`VentaServiceImpl.java`, `VentaRepository.java`, `VentaServiceListarVentasAbonoTest.java`, `ClienteAgendaCompartidaAbonoTest.java`), conteo 201/198/3 antes y después (sin cambios en el conteo, sólo cambió cuál test documenta qué), y que las specs `historial-cobros-abono` y `backend-clientes` ya están corregidas a mano — quien archive este change no debe esperar un delta que las cubra, sólo confirmar que el contenido actual de `openspec/specs/` ya refleja el comportamiento nuevo.
