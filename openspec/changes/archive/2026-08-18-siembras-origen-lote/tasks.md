## 1. Migración de base de datos (bloqueante — requiere confirmación previa del usuario)

> El renombre `numeroLote` → `codigoLote` está sujeto a la confirmación pendiente en `design.md` (Open Question 1). No ejecutar este grupo sin esa confirmación. Si el usuario decide conservar el nombre `numeroLote`, omitir 1.2 y 1.5, y sustituir `codigoLote` por `numeroLote` en todas las tareas siguientes.

- [x] 1.1 Con el backend detenido, sacar respaldo de la tabla `siembras` y registrar el resultado de `SELECT COUNT(*) FROM siembras WHERE numero_lote IS NOT NULL;` para poder verificar el renombre después. *(Ejecutado: 3/3 filas con `numero_lote`. Backup vía `pg_dump --data-only --column-inserts`.)*
- [x] 1.2 Ejecutar `ALTER TABLE siembras RENAME COLUMN numero_lote TO codigo_lote;`. Este paso es obligatorio antes de levantar el backend con el código nuevo: `ddl-auto=update` no renombra columnas, crearía `codigo_lote` vacía y dejaría `numero_lote` huérfana con todos los datos adentro, sin emitir error. *(Ejecutado.)*
- [x] 1.3 Ejecutar `ALTER TABLE siembras ADD COLUMN IF NOT EXISTS numero_siembra VARCHAR(255);` sin `NOT NULL`. Las filas históricas no tienen ese dato y una columna `NOT NULL` sin default haría fallar el arranque de Hibernate. *(Ejecutado.)*
- [x] 1.4 Ejecutar `ALTER TABLE siembras ADD COLUMN IF NOT EXISTS tipo_origen VARCHAR(255);` seguido de `UPDATE siembras SET tipo_origen = 'SOBRE' WHERE tipo_origen IS NULL;`, asumiendo que todo lo cargado hasta hoy provino de un sobre porque `numeroLote` era obligatorio. *(Ejecutado: 3 filas backfileadas a SOBRE.)*
- [x] 1.5 Verificar que `SELECT COUNT(*) FROM siembras WHERE codigo_lote IS NOT NULL;` devuelve el mismo número anotado en 1.1, y que no quedó ninguna columna `numero_lote` en la tabla. *(Verificado: 3/3, coincide con baseline.)*

## 2. Modelo de dominio (backend)

- [x] 2.1 Crear `backend/src/main/java/com/vivero/gestion/models/TipoOrigenSiembra.java` como `public enum TipoOrigenSiembra { SOBRE, SUELTO }`, en el mismo paquete y con el mismo estilo que `EstadoSiembra.java`.
- [x] 2.2 En `backend/src/main/java/com/vivero/gestion/models/Siembra.java`, renombrar el campo `private String numeroLote;` a `private String codigoLote;` y agregarle un comentario que deje asentado que **no lleva restricción de unicidad**: varias siembras pueden compartir el mismo código de lote cuando de un sobre salen siembras para clientes distintos.
- [x] 2.3 En la misma entidad, agregar `private String numeroSiembra;` sin anotaciones de columna (nullable por defecto en base; la obligatoriedad se aplica en el servicio).
- [x] 2.4 En la misma entidad, agregar `@Enumerated(EnumType.STRING) private TipoOrigenSiembra tipoOrigen;` replicando el patrón ya usado por el campo `estado`. No asignarle valor por defecto en Java: el origen es una decisión explícita del usuario y el servicio lo exige.
- [x] 2.5 En `backend/src/main/java/com/vivero/gestion/dto/SiembraDTO.java`, renombrar `numeroLote` a `codigoLote` y agregar `private String numeroSiembra;` y `private TipoOrigenSiembra tipoOrigen;`, importando el enum desde `com.vivero.gestion.models`.

## 3. Servicio y validación (backend)

- [x] 3.1 En `backend/src/main/java/com/vivero/gestion/services/impl/SiembraServiceImpl.java`, crear el método privado `validarYNormalizarOrigen(SiembraDTO dto)` que aplique, en este orden: rechazar si `tipoOrigen` es nulo; rechazar si `numeroSiembra` es nulo o en blanco; rechazar si `tipoOrigen == SOBRE` y `codigoLote` es nulo o en blanco; y si `tipoOrigen == SUELTO`, forzar `codigoLote` a `null` descartando cualquier valor recibido.
- [x] 3.2 Lanzar los rechazos con el mismo mecanismo de error que ya usa la clase (`RuntimeException` con mensaje descriptivo, igual que `"Siembra no encontrada con ID: "`), con mensajes en español: `"El origen de la semilla es obligatorio"`, `"El número de siembra es obligatorio"`, `"El código de lote es obligatorio cuando el origen es SOBRE"`. No introducir Bean Validation (`@NotNull`, `@NotBlank`, `@Valid`): el proyecto no la usa en ninguna entidad.
- [x] 3.3 En `crearSiembra`, invocar `validarYNormalizarOrigen(dto)` antes de construir la entidad, y reemplazar `siembra.setNumeroLote(dto.getNumeroLote())` por `siembra.setCodigoLote(dto.getCodigoLote())`, sumando `siembra.setNumeroSiembra(dto.getNumeroSiembra())` y `siembra.setTipoOrigen(dto.getTipoOrigen())`.
- [x] 3.4 En `actualizarSiembra`, invocar `validarYNormalizarOrigen(dto)` antes de mutar la entidad y aplicar los mismos tres setters, de modo que cambiar el origen de `SOBRE` a `SUELTO` en una edición deje `codigoLote` en `null` y conserve `numeroSiembra`.
- [x] 3.5 En `mapToDTO`, reemplazar `dto.setNumeroLote(siembra.getNumeroLote())` por `dto.setCodigoLote(siembra.getCodigoLote())` y agregar `dto.setNumeroSiembra(...)` y `dto.setTipoOrigen(...)`. El mapeo es manual en los tres puntos: no omitir ninguno. *(También se corrigieron dos usos adicionales de `getNumeroLote()` en `pasarAStock`, no inventariados en design.md, para mantener la compilación.)*
- [x] 3.6 Verificar que `SiembraController` no requiere cambios: las rutas y verbos siguen iguales y sólo cambia la forma del `SiembraDTO`. No agregar `@Valid` a los `@RequestBody`. *(Verificado: sin ocurrencias de `numeroLote` ni `@Valid` en el controller.)*

## 4. Formulario de siembra (frontend)

- [x] 4.1 En `frontend/src/components/SiembraForm.jsx`, actualizar el `useState` inicial de `formData` y las dos ramas del `useEffect` (edición y alta): renombrar `numeroLote` a `codigoLote`, agregar `numeroSiembra: ''` y agregar `tipoOrigen`, que en alta arranca en `'SOBRE'` y en edición toma `siembra.tipoOrigen || 'SOBRE'`.
- [x] 4.2 Agregar el selector de origen sobre el bloque que hoy contiene el código de lote, con la etiqueta `Origen de la Semilla *` y dos botones `type="button"` dentro de un `grid grid-cols-1 sm:grid-cols-2 gap-3`, replicando el patrón del toggle "Tipo de Cheque" de `NuevoChequeModal.jsx`: activo `border-2 border-emerald-500 bg-emerald-50 text-emerald-700 font-bold`, inactivo `border-2 border-gray-200 bg-white text-gray-600 hover:bg-gray-50`, ambos con `rounded-xl transition-all cursor-pointer`. Textos: `Sobre` y `Suelto`.
- [x] 4.3 Hacer que el botón `Suelto` limpie `codigoLote` al seleccionarse (`setFormData(prev => ({ ...prev, tipoOrigen: 'SUELTO', codigoLote: '' }))`), para que un valor tipeado y luego oculto no viaje en el submit. El botón `Sobre` sólo cambia `tipoOrigen`.
- [x] 4.4 Convertir el campo actual "Número de Lote *" en `Código de Lote *` ligado a `formData.codigoLote`, y envolverlo en `{formData.tipoOrigen === 'SOBRE' && ( ... )}` para que se renderice únicamente con origen `SOBRE`, conservando `required` dentro de esa rama.
- [x] 4.5 Agregar el campo `Número de Siembra *` ligado a `formData.numeroSiembra`, siempre visible y con `required`, usando las mismas clases del input de código de lote (`w-full px-4 py-2 border border-gray-200 rounded-xl focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 transition-colors`).
- [x] 4.6 Reacomodar la grilla que hoy es `grid grid-cols-2 gap-4` con código de lote y cantidad, de modo que al ocultarse el código de lote en origen `SUELTO` la fila no quede con un hueco: el número de siembra debe ocupar el lugar liberado y `Cantidad Inicial (Bandejas)` conservar su `FormattedNumberInput` y el contador de semillas intacto. *(Se cambió además a `grid-cols-1 sm:grid-cols-2` para consistencia responsive con el resto del formulario.)*
- [x] 4.7 Verificar en un viewport de 320px que el toggle de origen y los campos de texto no desbordan, siguiendo el criterio responsive ya aplicado en los formularios del proyecto. *(Verificado por revisión de código: sin entorno de navegador disponible. El toggle y la grilla de campos usan `grid-cols-1 sm:grid-cols-2`, igual que el resto del formulario, por lo que en 320px caen a una columna sin desborde.)*

## 5. Consumidores del código de lote (frontend)

- [x] 5.1 En `frontend/src/pages/Siembras.jsx`, actualizar el filtro de búsqueda (`s.numeroLote && s.numeroLote.toLowerCase().includes(...)`) para que consulte `s.codigoLote` **y además** `s.numeroSiembra`, de modo que se pueda buscar una siembra por su número.
- [x] 5.2 En la tarjeta mobile de `Siembras.jsx`, reemplazar `Lote: {siembra.numeroLote}` por una presentación que muestre siempre el número de siembra y muestre el código de lote sólo cuando exista (`siembra.codigoLote`), sin dejar el texto `Lote:` colgando vacío en las siembras con origen `SUELTO`. *(Se agregó también el origen (Sobre/Suelto) junto al número de siembra, para cumplir el escenario "Visualización del origen en el listado" de la spec.)*
- [x] 5.3 Aplicar el mismo criterio en la fila de la tabla desktop de `Siembras.jsx` (`Lote: {siembra.numeroLote} | Bandeja: ...`), mostrando el origen junto al número de siembra y omitiendo el código de lote en las siembras sueltas.
- [x] 5.4 En `frontend/src/layouts/DashboardLayout.jsx`, adaptar el texto de alerta `{alerta.variedadPlanta?.nombre} (Lote {alerta.numeroLote})` para que use el número de siembra como identificador principal, dado que es el único dato presente en ambos orígenes.
- [x] 5.5 En `frontend/src/components/FinalizarSiembraModal.jsx`, actualizar la leyenda que hoy dice `(Lote: {siembra.numeroLote})` para que muestre el número de siembra y, si existe, el código de lote.
- [x] 5.6 En `frontend/src/components/PaseStockModal.jsx`, actualizar la línea `Lote: {siembra.numeroLote} • Dueño: {siembra.dueno}` con el mismo criterio: número de siembra siempre, código de lote sólo si existe.
- [x] 5.7 Tolerar `numeroSiembra` nulo en los cinco archivos anteriores mostrando un guion o texto equivalente, ya que las siembras cargadas antes de este change no tienen ese dato y no puede inventarse. *(Todos usan `siembra.numeroSiembra || '-'` / `alerta.numeroSiembra || '-'`.)*

## 6. Verificación y cierre

- [x] 6.1 Ejecutar un grep de `numeroLote` sobre `backend/src` y `frontend/src`: no debe quedar ninguna ocurrencia si se confirmó el renombre. Las quince ocurrencias originales están inventariadas en la tabla de la Decisión 3 de `design.md`. *(Verificado con grep case-insensitive: 0 ocurrencias en ambos árboles. Se detectaron y corrigieron 2 ocurrencias adicionales en `pasarAStock` de `SiembraServiceImpl.java`, no inventariadas en `design.md`.)*
- [x] 6.2 Verificar que en ningún punto se agregó `unique = true` ni un índice único sobre `codigo_lote`: el negocio necesita explícitamente que varias siembras compartan el mismo código de lote. *(Verificado con grep: 0 ocurrencias de `unique`/`uniqueConstraints` en `Siembra.java`.)*
- [x] 6.3 Probar manualmente el alta con origen `SOBRE`: el campo de código de lote aparece, es obligatorio, y el registro se persiste con `tipoOrigen`, `codigoLote` y `numeroSiembra` completos. *(Verificado por revisión de código: sin entorno de navegador disponible. `SiembraForm.jsx` renderiza el campo Código de Lote sólo cuando `tipoOrigen === 'SOBRE'` con `required`, y `validarYNormalizarOrigen` rechaza el alta si falta.)*
- [x] 6.4 Probar manualmente el alta con origen `SUELTO`: el campo de código de lote no se renderiza, el número de siembra sigue siendo obligatorio, y el registro queda persistido con `codigo_lote` en `NULL`. *(Verificado por revisión de código: sin entorno de navegador disponible. El botón "Suelto" limpia `codigoLote` en el estado y oculta el campo; `validarYNormalizarOrigen` fuerza `codigoLote` a `null` en el servidor sin importar lo recibido.)*
- [x] 6.5 Probar el escenario de un sobre repartido entre varios clientes: dar de alta tres siembras con el mismo código de lote, tres dueños distintos y tres números de siembra distintos, y confirmar que las tres se guardan sin error de duplicación. *(Verificado por revisión de código: sin entorno de navegador disponible. `codigoLote` no tiene restricción de unicidad en `Siembra.java` ni en el repositorio; cada fila es independiente por `id`.)*
- [x] 6.6 Probar la edición de una siembra existente cambiando el origen de `SOBRE` a `SUELTO`, y confirmar que el código de lote queda en `null` en base y que el número de siembra se conserva. *(Verificado por revisión de código: sin entorno de navegador disponible. `actualizarSiembra` invoca `validarYNormalizarOrigen(dto)` antes de mutar la entidad, que fuerza `codigoLote` a `null` cuando `tipoOrigen == SUELTO` y no toca `numeroSiembra`.)*
- [x] 6.7 Verificar que las siembras históricas (con `numeroSiembra` nulo) se listan sin romper la interfaz, y que al editarlas el servicio exige cargar el número de siembra. *(Verificado: 5.2/5.3/5.7 usan `siembra.numeroSiembra || '-'` y `siembra.codigoLote ? ... : ''`, tolerando null sin romper el render. `validarYNormalizarOrigen` rechaza `actualizarSiembra` si `numeroSiembra` es nulo o en blanco, obligando a completarlo antes de guardar una edición.)*

> El proyecto no tiene hoy infraestructura de tests automatizados más allá de `BackendApplicationTests` (carga de contexto), y no hay Testcontainers ni runner de tests en el frontend. Por eso el grupo 6 es de verificación manual. Si se decide sumar tests automatizados para estas reglas, deben usar base real o Testcontainers, nunca mocks de base de datos.

## 7. Fixes descubiertos al probar

- [x] 7.1 **Búsqueda por número de siembra ya funcionaba** (task 5.1) pero el placeholder del buscador no lo mencionaba, llevando a pensar que no estaba soportado. Actualizado el `placeholder` de `frontend/src/pages/Siembras.jsx` a "Buscar por variedad, número de siembra, lote o dueño...".
- [x] 7.2 **`numeroSiembra`/`codigoLote` no resaltaban en el diseño:** quedaban enterrados en una línea `text-xs text-gray-500` mezclados con origen y bandeja, tanto en la tarjeta mobile como en la fila de la tabla desktop de `Siembras.jsx`. Se separaron en badges propios (`Nº {numeroSiembra}` en índigo, siempre visible; `Lote {codigoLote}` en ámbar, sólo si existe), dejando origen + bandeja como línea secundaria debajo. Aplicado igual en mobile y desktop. Header de la columna desktop renombrado de "Variedad / Lote" a "Variedad / Identificación" para reflejar que ahora muestra dos identificadores, no uno.
- [x] 7.3 **Filtro exclusivo por número de siembra:** el buscador general mezclaba coincidencias de `codigoLote` y `numeroSiembra` en un único OR, generando confusión cuando ambos comparten el mismo valor (ej. lote "1" y siembra "1" de otro cliente). Se agregó un toggle "Todo" / "Sólo Nº Siembra" junto al buscador en `Siembras.jsx`; en modo exclusivo, `filteredSiembras` sólo compara contra `numeroSiembra`, ignorando `codigoLote`, `variedad` y `dueño`.
- [x] 7.4 **Tarjetas mobile con demasiada información:** se rediseñó la tarjeta de `Siembras.jsx` (sólo mobile, `sm:hidden`) como colapsable. Colapsada por defecto: badges de `Nº Siembra` y `Lote` primero (arriba de todo), variedad y estado debajo, con chevron indicando que es expandible. Al tocar la tarjeta se expande y muestra dueño, cantidad inicial, progreso estimado y los botones de acción (Stock/Editar/Eliminar), que antes estaban siempre visibles. Estado de expansión por tarjeta vía `Set` en `expandedIds`, sin afectar la vista de tabla desktop.
