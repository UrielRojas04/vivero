> ### ⚠️ Gobernanza: **MEDIA-ALTA** — toca la fórmula de costo **y** migra datos de productos existentes
>
> Este change agrega un paso de conversión de moneda a la cadena de costeo (cuyo resultado se congela, inmutable, en `MovimientoStock.costoUnitario`) **y además** reasigna la referencia de catálogo de 8 productos vivos de `Marca` a `Proveedor`.
>
> **El grupo 6 (paso de moneda en `CostoCalculator`) y el grupo 9 (migración `Marca`→`Proveedor`) requieren checkpoint explícito del usuario ANTES de escribir el código, no después.** Se le muestra la fórmula exacta con números sobre los productos reales del grupo 1, y la tabla de migración producto por producto — son 8, caben en una pantalla. No alcanza con aprobar al pasar.
>
> **Contrato de no-regresión de todo el change:** los 10 productos de Herramientas, todos en **pesos**, tienen que dar **el mismo centavo** que en la línea de base del grupo 1. El paso de moneda con moneda `ARS` es la identidad. Si alguno se corre un centavo, se detiene y se revisa — no se avanza con una diferencia "chica".
>
> ### ✅ **Las 11 Open Questions de `design.md` ya están RESUELTAS** y quedaron registradas en la tarea 2.1. Cuatro las decidió el usuario explícitamente (unificar `Marca` con `Proveedor`; la cotización se carga en cada pedido; los defaults del proveedor se copian una sola vez; la pestaña "Marcas" se esconde sin borrar el código); las siete restantes quedaron por la recomendación. **Ninguna tarea de este documento debe volver a plantearlas como abiertas.**
>
> **Recordatorios de alcance que salen de esas decisiones:**
> - Los defaults del proveedor se **copian una sola vez**. El fallback de costeo sigue siendo de **dos niveles** (`producto → unidad de negocio`); el `CostoCalculator` **no recibe ni consulta el proveedor**. Ninguna tarea puede introducir un tercer nivel de herencia en vivo.
> - La **cotización se pide en cada pedido**. `Proveedor.ultimaCotizacionConocida` es sólo un prellenado visible con su antigüedad; **nunca** un fallback ni un valor que el backend resuelva solo.
> - **El arreglo del IVA al 21% ya está hecho** en la base real (los 8 productos de Ingco / Extra Power / Duroll tienen `iva_porcentaje = 0` explícito, verificado el 2026-08-20) y **NO es parte de este change**. No hay ninguna tarea sobre eso. La línea de base del grupo 1 se saca sobre la base **ya corregida**.
> - **`Marca` no se borra.** Sólo se esconde su pestaña de Configuración (tarea 10.7); componente, controller, servicios y endpoints quedan intactos.

## 1. Línea de base — la foto de "antes", sin la cual nada se puede verificar

- [x] 1.1 **Confirmar que `costeo-flexible-por-producto` está archivado.** Era la precondición de orden: sin él, los deltas de `costeo-productos` de este change no tienen spec base. *Ya archivado en `openspec/changes/archive/2026-08-20-costeo-flexible-por-producto/`, y `openspec/specs/costeo-productos/spec.md` existe. Precondición satisfecha.*
- [x] 1.2 Registrar, en una tabla del propio `tasks.md` o en un archivo de trabajo, los **10 productos de Herramientas** con: `id`, `nombre`, `marca_id`, `marca.nombre`, `costo_producto`, `precio`, `porcentaje_ganancia`, `iva_porcentaje`, `costo_envio_porcentaje`. Consulta de partida ya verificada el 2026-08-20: `SELECT p.id, p.nombre, m.nombre AS marca, p.costo_producto, p.descuento_proveedor, p.iva_porcentaje, p.costo_envio_porcentaje, p.precio, p.porcentaje_ganancia FROM productos p LEFT JOIN marcas m ON m.id=p.marca_id WHERE p.deleted=false AND p.unidad_negocio_id=2 ORDER BY p.id;` ⚠️ **La foto se saca sobre la base como está hoy**, es decir con el `iva_porcentaje = 0` explícito que ya tienen los 8 productos con marca (arreglo de datos ajeno a este change). No reconstruir el estado anterior a ese arreglo. *Ejecutada el 2026-08-20 contra `vivero-postgres` (`vivero_db`):*

  | id | nombre | marca_id | marca | costo_producto | precio | porcentaje_ganancia | iva_porcentaje | costo_envio_porcentaje |
  |----|--------|----------|-------|-----------------|--------|----------------------|-----------------|--------------------------|
  | 1 | Pala corazón | 1 | INGCO | 15000.00 | 20229.30 | 30.00 | 0.00 | *NULL* |
  | 2 | Pala ancha | 1 | INGCO | 22233.00 | 30799.99 | 33.00 | 0.00 | *NULL* |
  | 3 | Pala pocera | 2 | EXTRA POWER | 20000.00 | 27027.00 | 30.00 | 0.00 | *NULL* |
  | 4 | Cinta aisladora | 1 | INGCO | 2000.00 | 2624.83 | 26.00 | 0.00 | *NULL* |
  | 5 | Cinta métrica | 3 | DUROLL | 5000.00 | 7016.63 | 35.00 | 0.00 | *NULL* |
  | 6 | Pulverizador | 2 | EXTRA POWER | 10000.00 | 12723.48 | 22.40 | 0.00 | *NULL* |
  | 10 | Membrana | 2 | EXTRA POWER | 10000.00 | 14647.50 | 55.00 | 0.00 | *NULL* |
  | 12 | Destornillador | 2 | EXTRA POWER | 10000.00 | 13000.21 | 24.81 | 0.00 | *NULL* |
  | 19 | Masa | *NULL* | *(sin marca)* | *NULL* | 15000.00 | *NULL* | *NULL* | *NULL* |
  | 22 | prueba de pedido | *NULL* | *(sin marca)* | *NULL* | 20000.00 | *NULL* | *NULL* | *NULL* |

  *Los 8 productos con marca tienen `costo_envio_porcentaje` NULL a nivel producto: heredan el 5.00 de la unidad Herramientas (fallback de 2 niveles, tarea 1.6). Los 8 con marca tienen `descuento_proveedor` (columna propia de `productos`, redundante con `producto_descuentos` — ver 1.3) igual al porcentaje de su única fila de descuento.*
- [x] 1.3 Registrar las filas de `producto_descuentos` de esos 10 productos (esperado: **8 filas**, todas `nombre='Proveedor'`, una por producto con descuento). *Confirmado: 8 filas, exactamente una por cada producto con marca (ids 1,2,3,4,5,6,10,12), todas `nombre='Proveedor'`, `orden=0`. Porcentajes: Pala corazón 1.20, Pala ancha 0.80, Pala pocera 1.00, Cinta aisladora 0.80, Cinta métrica 1.00, Pulverizador 1.00, Membrana 10.00, Destornillador 0.80 — coinciden con `descuento_proveedor` de la tarea 1.2. `Masa` (19) y `prueba de pedido` (22) no tienen fila.*
- [x] 1.4 Registrar el `costo_unitario` del último `INGRESO`/`AJUSTE_INICIAL` de cada uno de los 10 productos. **Estos son los números del contrato de no-regresión.** *Query: `DISTINCT ON (producto_id) ... ORDER BY producto_id, fecha DESC, id DESC` filtrando `tipo_movimiento IN ('INGRESO','AJUSTE_INICIAL')` (nombre real de columna: `tipo_movimiento`, no `tipo`). Resultado — **contrato de no-regresión del change**:*

  | producto_id | nombre | tipo_movimiento | costo_unitario | fecha |
  |---|---|---|---|---|
  | 1 | Pala corazón | AJUSTE_INICIAL | **15561.00** | 2026-08-20 15:35:38 |
  | 2 | Pala ancha | AJUSTE_INICIAL | **23157.89** | 2026-08-20 15:33:17 |
  | 3 | Pala pocera | AJUSTE_INICIAL | **20790.00** | 2026-08-20 15:33:17 |
  | 4 | Cinta aisladora | AJUSTE_INICIAL | **2083.20** | 2026-08-20 15:33:17 |
  | 5 | Cinta métrica | AJUSTE_INICIAL | **5197.50** | 2026-08-20 15:35:39 |
  | 6 | Pulverizador | AJUSTE_INICIAL | **10395.00** | 2026-08-20 15:33:17 |
  | 10 | Membrana | AJUSTE_INICIAL | **9450.00** | 2026-08-20 15:33:17 |
  | 12 | Destornillador | AJUSTE_INICIAL | **10416.00** | 2026-08-20 15:33:17 |
  | 19 | Masa | INGRESO | **10500.00** | 2026-08-19 23:24:55 |
  | 22 | prueba de pedido | INGRESO | **12600.00** | 2026-08-20 14:19:53 |

  *Nota: `costo_unitario` del movimiento ≠ `costo_producto` de la tarea 1.2 — el movimiento guarda el costo ya calculado (con descuento/IVA/envío aplicados), `costo_producto` es el base de lista. Es el valor con el que compara la verificación 6.6 y 9.7.*
- [x] 1.5 Registrar las 4 marcas de Herramientas con la cantidad de productos de cada una (esperado: `INGCO` 3, `EXTRA POWER` 4, `DUROLL` 1, `SHIMURA` 0) y los proveedores vivos (esperado: sólo `SHIMURA`, id 4). *Confirmado exacto: INGCO (id 1) 3, EXTRA POWER (id 2) 4, DUROLL (id 3) 1, SHIMURA (id 4) 0. Tabla `proveedores` tiene 4 filas totales; sólo `id=4 SHIMURA` (`unidad_negocio_id=2`, contacto Marcelo) tiene `deleted=false` — es el único proveedor vivo. Las otras 3 (`id=1 TEST-SHIMURA`, `id=2 TEST-SHIMURA`, `id=3 TEST-CROSS`) están todas `deleted=true`.*
- [x] 1.6 Registrar `costo_envio_porcentaje` e `iva_porcentaje` de las dos unidades de negocio (esperado: Vivero `0.00` / `NULL`, Herramientas `5.00` / **`21.00`**) y el `iva_porcentaje` propio de cada producto (esperado: `0.00` explícito en los 8 con marca, `NULL` en `Masa` y `prueba de pedido`). Es la foto del estado **post-arreglo de datos**, y el precedente del patrón que la Decisión 4 automatiza. *Confirmado exacto: Vivero (id 1) `costo_envio_porcentaje=0.00`, `iva_porcentaje=NULL`; Herramientas (id 2) `costo_envio_porcentaje=5.00`, `iva_porcentaje=21.00`. Los 8 productos con marca (1,2,3,4,5,6,10,12) tienen `iva_porcentaje=0.00` explícito; `Masa` (19) y `prueba de pedido` (22) tienen `iva_porcentaje=NULL`.*
- [x] 1.7 Confirmar en base que **Vivero tiene 0 productos con marca** (esperado: 5 productos, 0 con `marca_id`). Es lo que hace que la migración de la OQ1 no pueda afectar a Vivero. *Confirmado exacto: `SELECT COUNT(*), COUNT(*) FILTER (WHERE marca_id IS NOT NULL) FROM productos WHERE unidad_negocio_id=1 AND deleted=false` → `5 total, 0 con_marca`.*
- [x] 1.8 Anotar el dato sucio detectado: `proveedores.id=1` tiene `unidad_negocio_id` NULL (registro `TEST-SHIMURA` dado de baja). Decidir si se limpia acá o se ignora — no bloquea nada, pero cualquier consulta de proveedores sin filtro de unidad lo va a devolver. *Confirmado: `id=1, nombre='TEST-SHIMURA', contacto='test', telefono='111', deleted=true, unidad_negocio_id=NULL`. Recomendación: **ignorar, no limpiar en este change**. Está `deleted=true`, así que toda consulta de este change filtra por `deleted=false` (grupo 3, tarea 3.6; grupo 9, migración) y nunca lo va a tocar. El único riesgo real es una consulta futura de proveedores **sin** filtro de `deleted` ni de unidad — no existe hoy en el código relevado. Si se quiere prolijidad, es un `UPDATE` de una fila (asignarle `unidad_negocio_id=2`, ya que su nombre indica Shimura/Herramientas) que puede hacerse como *chore* aparte, fuera de este change, para no mezclar limpieza de datos con la migración de catálogo del grupo 9.*

## 2. ✅ Decisiones cerradas — las 11 Open Questions ya resueltas

> Este grupo **ya no es un bloqueo**. Se conserva como registro de qué se preguntó, qué se recomendaba y qué se decidió, para que la implementación no reabra ninguna. El detalle completo, con el motivo de cada una, está en la sección *Open Questions — resueltas* de `design.md`.

- [x] 2.1 **Presentar al usuario las 11 Open Questions de `design.md` con su recomendación y registrar acá su decisión sobre cada una.** *Presentadas y resueltas. Cuatro las decidió el usuario explícitamente (OQ1, OQ2, OQ3, OQ10); las siete restantes quedaron por la recomendación documentada, sin objeción. Las 11, en los términos exactos en que quedaron:*
  - *(1) **OQ1 — ¿Se unifica `Marca` con `Proveedor`? SÍ, UNIFICAR.** Decidido por el usuario (era la opción recomendada). `Producto` pasa a tener un `proveedorId` en vez de un `marcaId`. Migración ya medida contra la base real: se crean `INGCO`, `EXTRA POWER` y `DUROLL` como proveedores nuevos (`SHIMURA` ya existe, id=4, y se reusa), se reasignan los **8 productos** de Herramientas que hoy tienen marca cargada, y **2 quedan sin proveedor** (`id=19 Masa`, `id=22 prueba de pedido` — los nacidos del flujo de "producto pendiente" de pedidos, que tampoco tienen marca hoy); el usuario se los asigna a mano después. **Vivero no se ve afectado** (0 productos con marca). La resolución marca→proveedor es por **nombre normalizado** (trim + mayúsculas, coincidencia exacta), nunca aproximada.*
  - *(2) **OQ2 — ¿Dónde vive la cotización del dólar? SE CARGA EN CADA PEDIDO; EL PROVEEDOR SÓLO SUGIERE LA ÚLTIMA USADA.** Decidido por el usuario (era la opción recomendada). La **MONEDA** —si el producto/proveedor puede facturarse en USD— es un dato estable y se configura una vez. La **COTIZACIÓN específica NO se guarda como default fijo del proveedor**: el proveedor guarda como mucho la última cotización cargada con su fecha, como ayuda de tipeo, y **cada pedido nuevo la vuelve a pedir**. En sus palabras: una cotización vieja aplicándose sola a un pedido de hoy sería el error más caro posible de este change — **no puede pasar**. Sin cotización informada y con líneas en USD, la confirmación de recepción **falla**: no se asume 1 y no se recurre a la última conocida.*
  - *(3) **OQ3 — ¿Los defaults del proveedor se copian o se siguen en vivo? SE COPIAN UNA SOLA VEZ.** Decidido por el usuario (era la opción recomendada). El fallback de costeo sigue siendo de **2 niveles** como en `costeo-flexible-por-producto`: `producto → unidad de negocio`. **El proveedor NO es una tercera capa de fallback en vivo** — sus valores son sólo el punto de partida sugerido al crear el producto (o al armar la línea de un pedido), copiado y de ahí en más independiente. **Cambiar la configuración de un proveedor después NO debe afectar productos ya creados.***
  - *(4) **OQ10 — ¿Qué se hace con la pestaña "Marcas" de Configuración? SE ESCONDE, NO SE BORRA.** Confirmado por el usuario junto con OQ1. `ConfiguracionMarcas.jsx`, `MarcaController`, `MarcaService(Impl)`, `MarcaRepository`, `MarcaDTO` y los endpoints `/api/marcas` **quedan en el repo sin tocar**, como red de rollback de la unificación. Borrarlos de verdad es un `chore` posterior.*
  - *(5) **OQ4 — El IVA de Shimura: ¿flag o número? AMBOS, CON ROLES DISTINTOS.** Por la recomendación, sin objeción. El **flag** `ivaIncluidoEnPrecio` decide si la UI **pregunta** el porcentaje de IVA o escribe `0` explícito; el **número** `ivaPorDefectoPorcentaje` es **sólo comodidad de tipeo y no gobierna nada** (no participa de ningún cálculo). Producto de proveedor con IVA incluido → `ivaPorcentaje = 0` **explícito**, jamás `null`.*
  - *(6) **OQ5 — Defaults del proveedor vs. Decisión 14 del change anterior: SIN CAMBIOS EN EL MODELO DE DESCUENTOS ESTABLES.** Por la recomendación, sin objeción. La Decisión 14 no se reabre: no hay descuentos en la cabecera del `Pedido` y el descuento por efectivo se sigue resolviendo ajustando el `costoUnitarioPactado` de la línea. Se agrega el botón **"reaplicar a sus productos" con vista previa** (grupo 11), **opcional/recortable** si el usuario prefiere el change más corto.*
  - *(7) **OQ6 — ¿Dónde entra la cotización en la cadena? PASO 0, ANTES DE TODO.** Por la recomendación, sin objeción. Verificado al centavo contra la fila 6 de `shimura.png`: `66,24 × 1460 × 1 (IVA) × 1,05 (envío) = 101.545,92`. Con moneda `ARS` el paso 0 es la identidad.*
  - *(8) **OQ7 — ¿El filtro de Productos reemplaza o coexiste? REEMPLAZA.** Por la recomendación, sin objeción. Un solo filtro llamado "Proveedor"; no coexisten dos filtros equivalentes.*
  - *(9) **OQ8 — ¿% o multiplicador en los campos? PORCENTAJE COMO UNIDAD CANÓNICA.** Por la recomendación, sin objeción. No se reabre el change anterior: nada cambia de unidad en el modelo, el DTO ni el cálculo. Se agrega, sólo como ayuda visual, **la equivalencia en multiplicador al lado del input** (al tipear `12,6` aparece `= × 0,874`).*
  - *(10) **OQ9 — Los descuentos "colapsados" que parecían combinar dos conceptos: SE DESARMAN donde correspondan a dos descuentos reales, NUNCA mezclando envío adentro de un descuento.** Por la recomendación, sin objeción — y confirmada además por un dato real que el usuario corrigió en la tarea 2.3: el `0,874`/`0,8075` de la planilla de Extra Power NO eran dos descuentos combinados, eran un único descuento real de 10% con el 5% de envío mezclado adentro por error de la propia planilla. Extra Power queda con **un solo descuento (10%)**, envío como su propio campo. La regla general de la OQ9 se mantiene para el caso genuino de dos descuentos reales (ej. Ingco): se cargan **con nombre en el perfil del proveedor**. Lo que NO corresponde nunca es mezclar el envío dentro del multiplicador de descuento — el envío tiene su propio campo en el modelo, siempre.*
  - *(11) **OQ11 — El costo de envío: ¿% o flete fijo por pedido? PORCENTAJE, igual que hoy.** Por la recomendación, sin objeción. Coincide con las tres pestañas de la planilla (`1,05`) y con el modelo actual. **Un flete fijo prorrateado sería un change aparte** —el prorrateo depende de si se reparte por unidades, por peso o por importe— y sólo puede anotarse como candidato en el Backlog del roadmap (ver 12.12), nunca como extensión implícita de éste.*
- [x] 2.2 **Registro del arreglo de datos del IVA al 21%, ejecutado FUERA de este change.** *La unidad Herramientas tiene `iva_porcentaje = 21.00` por defecto y los 8 productos de proveedores que ya incluyen el IVA en su precio (Ingco, Extra Power, Duroll) lo estaban heredando de más. **El usuario lo autorizó y ya se corrigió en la base real:** esos 8 productos (ids 1, 2, 3, 4, 5, 6, 10, 12) tienen hoy `iva_porcentaje = 0.00` explícito; los 2 sin marca (19, 22) siguen en `NULL`. Verificado en base el 2026-08-20. **No es una tarea de este change y no queda nada pendiente al respecto.** Se registra por dos motivos: confirma que el patrón "flag + `0` explícito" de la Decisión 4 ya funciona en producción, y obliga a sacar la línea de base del grupo 1 sobre la base ya corregida.*
- [x] 2.3 **Cargar con el usuario el perfil real de sus 4 proveedores** (IVA aparte/incluido, descuentos, envío, moneda) y dejarlo anotado. *Resuelto parcialmente y descope explícito: el usuario corrigió el dato de Extra Power — su `0,874`/`0,8075` de la planilla NO son descuentos combinados con otro descuento, son la mezcla accidental de un único descuento real de **10%** con el costo de envío (**5%**, ya separado en la columna `envio` de la planilla) — probable error de fórmula en su propia planilla, no un segundo descuento a desglosar. Extra Power queda entonces con **un solo descuento (10%) + envío 5% + IVA incluido**, sin necesidad de la descomposición que planteaba la OQ9 originalmente para este proveedor. Shimura (IVA aparte 21/10,5 variable por ítem, dólares, envío variable) e Ingco (IVA incluido, des1+des2 en cascada, variables entre compras — ver `ingco.png`) quedan como referencia de las capturas, no como valores fijos a precargar. **Duroll queda sin datos** (no hay captura). El usuario prefirió cargar los 4 perfiles reales él mismo, manualmente, desde la pantalla de Configuración una vez implementado el change, en vez de que se precarguen con datos interpretados de las planillas — riesgo ya demostrado con Extra Power. Ninguna tarea de implementación depende de tener los 4 perfiles reales cargados de antemano; los grupos 12.1-12.3 usan los números de las capturas sólo como caso de prueba matemáticamente verificado, no como el perfil definitivo que va a usar el negocio.*
- [x] 2.4 **Verificar que los specs del change están alineados con las 11 decisiones.** *Verificado: los specs ya estaban escritos contra las recomendaciones y las 11 resoluciones coinciden con ellas, así que no hizo falta re-escribirlos, sólo reforzarlos. Ajustes aplicados: `configuracion-proveedores` (la copia ocurre una sola vez, el cálculo nunca consulta al proveedor, el perfil no admite una cotización aplicable, el envío es siempre porcentaje, descuentos desglosados con nombre); `moneda-cotizacion` (la cotización se pide en cada pedido y el valor guardado no es fallback ni se muestra sin fecha); `catalogo-productos` (resolución por nombre normalizado exacto, y los productos sin marca quedan sin proveedor con asignación manual posterior); y se agregó el delta de `gestion-marcas` (la marca deja de ser el vínculo de catálogo y su sección deja de ofrecerse, sin borrar entidad, ABM ni endpoints).*

## 3. Perfil de costeo del proveedor — backend

- [x] 3.1 Crear `models/ProveedorDescuento.java` (tabla `proveedor_descuentos`): `id`, `@ManyToOne Proveedor`, `nombre` `String(100)` obligatorio, `porcentaje` `BigDecimal(5,2)` obligatorio `>= 0`, `orden` `Integer` sólo presentacional. Calcado de `ProductoDescuento`. *Creado. Tabla confirmada en base (`\d proveedor_descuentos`): `id`, `nombre varchar(100) not null`, `porcentaje numeric(5,2) not null`, `orden integer`, `proveedor_id` con FK a `proveedores`.*
- [x] 3.2 Agregar a `models/Proveedor.java`: `ivaIncluidoEnPrecio` (`boolean`, default `true`), `ivaPorDefectoPorcentaje` (`BigDecimal(5,2)` nullable), `manejaDolares` (`boolean`, default `false`), `costoEnvioPorDefectoPorcentaje` (`BigDecimal(5,2)` nullable), y `@OneToMany descuentosPorDefecto` con `cascade=ALL`, `orphanRemoval=true` y `@BatchSize` (mismo molde que `Producto.descuentos`, para no reintroducir el N+1 que la Decisión 11 del change anterior eliminó). *Agregado (incluye también `ultimaCotizacionConocida`/`fechaUltimaCotizacion` de la tarea 5.4, mismo archivo). Verificado `orphanRemoval`: un `update` que reemplazó 2 descuentos por 1 dejó exactamente 1 fila en `proveedor_descuentos`, sin huérfanos.*
- [x] 3.3 Extender `dto/ProveedorDTO.java` con el perfil completo, incluida la lista de descuentos por defecto. **Nunca devolver la entidad JPA** (regla dura 5). *Extendido con `@Builder.Default` en los booleanos para que el perfil neutro sea el default también al construir el DTO a mano. `ProveedorDescuentoDTO` nuevo, calcado de `ProductoDescuentoDTO`.*
- [x] 3.4 Extender `ProveedorServiceImpl.create()` / `update()` / `mapToDTO()` para persistir y mapear el perfil. Validar: nombre de descuento no vacío, porcentaje `>= 0`, y que `ivaPorDefectoPorcentaje` sólo se exija cuando `ivaIncluidoEnPrecio = false`. *Implementado (`validarPerfilCosteo`, `aplicarPerfilCosteo`, `reemplazarDescuentos` — mismo patrón que `ProductoServiceImpl`). Probado contra la API real: descuento con porcentaje negativo → rechazado; IVA aparte sin `ivaPorDefectoPorcentaje` → rechazado; perfil completo (dólares + envío 5% + 2 descuentos) → persistido y devuelto correcto.*
- [x] 3.5 Agregar la validación de **nombre duplicado** por unidad de negocio (normalizado: trim + mayúsculas), en alta y en edición. Es lo que impide terminar con dos fichas del mismo proveedor con configuraciones distintas. *`ProveedorRepository.findByUnidadNegocioIdAndNombreNormalizado` (JPQL `UPPER(TRIM(...))`) + `validarNombreDuplicado`. Probado: crear "TEST NUEVO PROVEEDOR" y después "  test nuevo proveedor  " (mismo nombre con espacios y minúsculas) → rechazado con mensaje explícito. Sin unidad activa en contexto se omite el chequeo (caso hoy inexistente en producción; documentado en el código).*
- [x] 3.6 Agregar al servicio la consulta de **productos asociados a un proveedor** (acotada a la unidad activa, con su costo actual), que alimenta la vista previa del grupo 11. Sin `findAll()` sin límite (regla dura 6). *`ProductoRepository.findResumenPorProveedor` (nativa, proyección `ProductoResumenProveedorProjection`) + `ProveedorService.obtenerProductosAsociados`. ⚠️ Decisión de alcance: la consulta referencia `productos.proveedor_id`, columna que agrega recién el grupo 9 (checkpoint pendiente, explícitamente fuera de este tramo). No se tocó `Producto.java` ni se hizo la migración. El método no tiene ningún invocador real todavía (no se expuso endpoint en el controller) — es código muerto, correcto y con la forma final, a la espera de que el grupo 9 agregue la columna. Documentado con comentario en ambos archivos.*
- [x] 3.7 Verificar en base que un proveedor nuevo nace con perfil **neutro** (IVA incluido, sin IVA por defecto, sin dólares, sin descuentos, sin envío) y que ese perfil **no aporta nada** a ningún producto. *Verificado creando "TEST NUEVO PROVEEDOR" vía API real: `ivaIncluidoEnPrecio=true, ivaPorDefectoPorcentaje=null, manejaDolares=false, costoEnvioPorDefectoPorcentaje=null, descuentosPorDefecto=[], ultimaCotizacionConocida=null`. `SHIMURA` (id 4), ya existente, quedó con el mismo perfil neutro tras el `ALTER TABLE` (no se le inventó ningún valor). Sin conexión Producto↔Proveedor todavía (grupo 9 pendiente), así que "no aporta nada a ningún producto" es trivialmente cierto. Registro de prueba limpiado con soft-delete (`deleted=true`), mismo criterio que los `TEST-*` ya presentes en la base.*

## 4. Perfil de costeo del proveedor — frontend

- [x] 4.1 Reescribir `components/ProveedorForm.jsx` con dos secciones distinguibles: **Datos** (nombre, contacto, teléfono, lo que ya tiene) y **Costeo** (tratamiento del IVA, IVA por defecto, si maneja dólares, filas de descuento, envío por defecto **como porcentaje**). ⚠️ **El formulario NO lleva ningún campo de cotización** (OQ2): la cotización se carga en cada pedido, no en la ficha del proveedor. El envío tampoco admite un monto fijo (OQ11). *Reescrito con tabs "Datos"/"Costeo" dentro del mismo modal (shell fullscreen-mobile ya existente). Sin ningún input de cotización — verificado por lectura del archivo final, sólo existen los 5 campos del perfil por defecto (Decisión 1) más nombre/contacto/teléfono.*
- [x] 4.2 Filas de descuento agregables y quitables con el patrón del repo: `FormattedNumberInput` para el porcentaje, `Plus`/`Trash2` de `lucide-react`, `cursor-pointer` en todos los botones. *Implementado, mismo patrón que `ProductoForm.jsx`.*
- [x] 4.3 Mostrar/ocultar el campo de IVA por defecto según el tratamiento del IVA elegido, y explicar en una línea qué implica cada opción ("el precio de lista ya trae el IVA" / "el IVA se suma aparte"). *Radio buttons con la explicación en gris debajo de cada opción; el campo de IVA por defecto sólo se renderiza cuando `ivaIncluidoEnPrecio=false`.*
- [x] 4.4 Mostrar la equivalencia porcentaje ↔ multiplicador al lado de cada campo de descuento (OQ8): al tipear `12,6` aparece `= × 0,874`. *`multiplicadorDescuento()` (`1 - p/100`, formato es-AR 3 decimales) debajo de cada fila de descuento. Puramente visual: el payload sigue enviando el porcentaje tal cual.*
- [x] 4.5 Agregar la sección **Proveedores** a `pages/Configuracion.jsx`, junto a `herramientas` y `marcas`, reutilizando `ProveedorForm.jsx` (**no** una segunda definición del formulario). *Agregado el botón "Proveedores" al bloque de secciones de Herramientas (Decisión 10: "dentro de configuración del negocio Herramientas"), renderizando directamente `pages/Proveedores.jsx` — que ya usa `ProveedorForm.jsx` — en vez de crear un `ConfiguracionProveedores.jsx` que duplicaría el listado/ABM.*
- [x] 4.6 Actualizar `pages/Proveedores.jsx` para que use el mismo formulario ampliado y muestre en el listado los datos del perfil que sirvan para reconocerlo de un vistazo. *`Proveedores.jsx` ya usaba `ProveedorForm.jsx` (sin cambios en esa parte). Agregado componente `PerfilBadges` (IVA incluido/aparte + %, USD si `manejaDolares`, envío %, cantidad de descuentos) en la tarjeta mobile y en una columna nueva de la tabla desktop.*
- [x] 4.7 Extender `api/proveedores.api.js` con los campos nuevos del payload. *El archivo ya pasaba el payload completo sin transformarlo; se documentó explícitamente la forma esperada (perfil completo + `descuentosPorDefecto`) en un comentario, sin necesidad de tocar la lógica.*
- [x] 4.8 Verificar el formulario a **320px** con dos filas de descuento cargadas: todos los campos accesibles, sin desborde horizontal. *Verificado por cálculo de anchos (no hay navegador disponible en este entorno): fila de descuento = input flexible (`min-w-0`) + `%` (`w-16`=64px) + botón borrar (~36px) + gaps (3×8px=24px) = 124px fijos; con el padding del modal (`p-6`=24px×2) el contenido disponible a 320px es 272px, dejando ~148px flexibles para el nombre del descuento — sin desborde. La equivalencia en multiplicador se movió a una línea propia debajo de la fila (no una columna más) específicamente para no competir por ancho a 320px. Igual criterio que el resto del formulario, que ya reusa las clases responsive de `ProductoForm.jsx`. `npx oxlint` sobre el componente: sin errores.*
- [ ] 4.9 Cargar en la app los 4 proveedores reales con el perfil acordado en la tarea 2.12 y verificar en base que se persistió completo. **NO ejecutada, a propósito.** Citando la tarea 2.3 (ya cerrada): *"El usuario prefirió cargar los 4 perfiles reales él mismo, manualmente, desde la pantalla de Configuración una vez implementado el change, en vez de que se precarguen con datos interpretados de las planillas — riesgo ya demostrado con Extra Power. Ninguna tarea de implementación depende de tener los 4 perfiles reales cargados de antemano."* El formulario y la pantalla ya están listos para que el usuario los cargue cuando quiera.

## 5. Moneda — modelo y persistencia

- [x] 5.1 Agregar `Producto.monedaCosto` (enum `ARS`/`USD`, default `ARS`, `varchar(3)`). **Sin valor por defecto ambiguo:** un producto existente sin el campo se lee como `ARS`. *Agregado enum `MonedaCosto` (`models/MonedaCosto.java`) + campo en `Producto.java`, DTO y mapeo en `ProductoServiceImpl` (create fuerza ARS si viene null; update sólo pisa si viene informado, para no resetear un valor existente cuando el formulario todavía no lo manda — grupo 8, fuera de alcance). Verificado en base: columna `moneda_costo varchar(3) not null default 'ARS'` con `CHECK (moneda_costo IN ('ARS','USD'))`, y los 10 productos de Herramientas quedaron en `ARS` tras el `ALTER TABLE`.*
- [x] 5.2 Agregar `Pedido.cotizacionDolar` (`BigDecimal(12,4)` nullable) y, en `PedidoDetalle`, la moneda de la línea más los valores de costeo pactados (descuentos, IVA, envío) según la Decisión 8. *`Pedido.cotizacionDolar` agregado + `PedidoDTO.cotizacionDolar` (sólo el campo; el flujo que la pide/persiste es la tarea 7.3, fuera de alcance). En `PedidoDetalle`: `monedaLinea` (enum, default ARS), `ivaPactadoPorcentaje`, `envioPactadoPorcentaje`, `descuentoPactadoDetalle` (texto, mismo criterio que `MovimientoStock.descuentoDetalle`) — los 4 campos que la Decisión 8 enumera textualmente ("la moneda de la línea, el IVA pactado, el envío pactado y el detalle textual de los descuentos pactados"). No se tocó `PedidoDetalleDTO` ni `PedidoServiceImpl`: la precarga y persistencia real es de los grupos 7/8.*
- [x] 5.3 Agregar a `MovimientoStock` las columnas `moneda_origen` (`varchar(3)` nullable) y `cotizacion_aplicada` (`numeric(12,4)` nullable). **Nullable a propósito:** los movimientos históricos quedan en `null` y eso se lee como "no hubo conversión". *Agregado. Verificado en base: ambas columnas nullable, sin default, y los 35 movimientos existentes (los 10 productos de Herramientas incluidos) quedaron en `NULL` en las dos.*
- [x] 5.4 Agregar a `Proveedor` `ultimaCotizacionConocida` (`BigDecimal(12,4)` nullable) + `fechaUltimaCotizacion` (nullable), escritas al confirmar un pedido que llevó cotización. ⚠️ **Son exclusivamente una ayuda de tipeo (OQ2):** el backend **NUNCA** las resuelve por su cuenta, **NUNCA** las usa como fallback y **NUNCA** las aplica a una operación. Su único consumidor es el prellenado visible del formulario de pedido (tarea 7.2). **No** es un campo de configuración editable en el perfil del proveedor: no va en el formulario de proveedor del grupo 4. *Agregado en `Proveedor.java`/`ProveedorDTO.java` (tarea 3.2/3.3). Ningún flujo real las escribe todavía (`ProveedorServiceImpl.aplicarPerfilCosteo` deja explícitamente esos dos campos sin tocar, con comentario). No están en `ProveedorForm.jsx` — confirmado por lectura del componente final. Verificado en base: `SHIMURA` y el proveedor de prueba quedaron con ambas columnas en `NULL`.*
- [x] 5.5 Verificar que Hibernate (`ddl-auto=update`) creó todas las columnas y tablas nuevas al arrancar, **sin ningún `ALTER TABLE` manual**. Si alguna requiere DDL a mano, el diseño de esa columna está mal y hay que rehacerlo. *`docker compose up -d --build backend` (2026-08-20). Log de arranque sin errores ni excepciones; Hibernate emitió, todos automáticos (no escritos a mano): 2 `alter table` en `movimientos_stock`, 4 en `pedido_detalles`, 1 en `pedidos`, 1 en `productos`, 5 en `proveedores`, 1 `create table proveedor_descuentos` + su FK. `DataInitializer` corrió después sin fallar ("Base de datos inicializada con roles y usuario jefe.") y la app quedó sirviendo requests (login + CRUD de proveedores probados contra la API real).*
- [x] 5.6 Verificar en base que **ningún** producto, pedido ni movimiento existente cambió de valor por la sola creación de las columnas. *Verificado: los 10 productos de Herramientas conservan exactamente `costo_producto`/`precio` de la línea de base del grupo 1 (todos quedaron en `moneda_costo='ARS'`); los 35 movimientos de esos productos conservan su `costo_unitario` y quedaron con `moneda_origen`/`cotizacion_aplicada` en `NULL`; los 8 `pedidos` existentes quedaron con `cotizacion_dolar` en `NULL`; las 15 `pedido_detalles` existentes quedaron en `moneda_linea='ARS'` (el default) con `iva_pactado_porcentaje` en `NULL`.*

## 6. 🚦 CHECKPOINT + Paso de conversión de moneda en el calculador

> **Checkpoint obligatorio ANTES de escribir código.** Mostrarle al usuario la cadena resultante completa y aplicarla con números sobre los productos reales del grupo 1, comparada contra su costo actual. Recién con su OK se toca `CostoCalculator.java`.

- [x] 6.1 **CHECKPOINT:** presentar la cadena `0. base ARS = importe × cotización (sólo si USD) → 1. descuentos en cascada → 2. IVA sobre el neto → 3. envío en cadena sobre el neto+IVA → 4. suma` (paso 3 corregido 2026-08-22, ver nota en `specs/costeo-productos/spec.md` — en este ejemplo IVA=0% así que el número verificado no cambia), con el ejemplo verificado `66,24 × 1460 × 1,05 = 101.545,92` y con los 10 productos de la línea de base mostrando costo actual vs. costo con la cadena nueva (deben ser idénticos, todos están en pesos). *Presentado con la cadena completa, el ejemplo de Shimura verificado, y la explicación del guard (conversión sólo si la línea está en USD, no "si hay cotización cargada"). El usuario confirmó: "SI".*
- [x] 6.2 Extender `services/CostoCalculator.calcular()` con el paso 0. La conversión ocurre **dentro de la escala intermedia 6**, antes de cualquier redondeo — no se redondea el base convertido a 2 decimales y después se sigue. *Agregada la sobrecarga de 6 parámetros `calcular(costoBase, MonedaCosto moneda, BigDecimal cotizacion, descuentos, iva, envio)`; la de 4 parámetros delega en ella con `moneda=null, cotizacion=null` (identidad). `baseConvertida6` se calcula con `.setScale(6, HALF_UP)` — sobre un `BigDecimal` de moneda (máx. 2 decimales reales) esto nunca redondea, sólo agrega ceros; la cascada de descuentos parte de `baseConvertida6`, no de `base`.*
- [x] 6.3 La conversión se aplica **sólo** cuando la moneda de la operación es `USD`. Nunca "si hay cotización cargada". Este es el guard más importante del change. *`boolean esUsd = moneda == MonedaCosto.USD` es la única condición; con `esUsd=false` (incluido `moneda=null`) `cotizacionAplicada`/`monedaAplicada` del resultado quedan `null` sin importar qué venga en el parámetro `cotizacion`. Verificado en vivo (ver 6.7).*
- [x] 6.4 Extender `CostoResultado` con el costo base convertido, la moneda y la cotización aplicadas, para que el llamador pueda congelarlos sin recalcular. *`getCostoBaseConvertido()` (redondeado a 2 decimales, es lo que el llamador usa para `MovimientoStock.costoBase`), `getMonedaAplicada()` (`String`, calza con la columna `moneda_origen`), `getCotizacionAplicada()`.*
- [x] 6.5 Replicar exactamente el mismo paso en `frontend/src/utils/costeo.js`, con la misma firma y el mismo orden. *`calcularCosto()` extendida con params opcionales `moneda`/`cotizacion` al final (compatible con todos los llamadores existentes que no los pasan), mismo guard (`moneda === 'USD'`), mismo resultado extendido (`costoBaseConvertido`, `monedaAplicada`, `cotizacionAplicada`).*
- [x] 6.6 **Verificación de no-regresión (contrato del change).** *Vía directa en `CostoCalculator.java`: el guard hace que con `moneda≠USD` el cálculo sea matemáticamente idéntico al de antes (prueba por construcción, no sólo por prueba empírica). Verificación empírica controlada y reversible contra la API real: producto `id=1` (Pala corazón), `PUT /productos/1` con `stock: 7` (dispara un `INGRESO` nuevo que recorre `aplicarDesglose`) → `movimientos_stock` id 104: `costo_base=15000.00, costo_neto=14820.00, costo_unitario=15561.00, moneda_origen=NULL, cotizacion_aplicada=NULL` — **idéntico** a la fila `AJUSTE_INICIAL` de la línea de base (id 102, mismo costo_unitario 15561.00). Stock revertido a 6 después (`PUT` con `stock: 6`), producto sin cambios netos. Los otros 9 productos de la línea de base no se tocaron y conservan su `costo_unitario` de la tarea 1.4 — **excepto el id=19 "Masa"**, que sí cambió como efecto colateral de la verificación 6.7 (ver nota ahí abajo, no es una regresión de fórmula).*
- [x] 6.7 **Verificación del guard.** *Pedido mixto real (proveedor de prueba con `manejaDolares=true`, unidad Herramientas): una línea en USD (`costoUnitarioPactado=66.24`, `cotizacionDolar=1460`) + una línea en pesos sobre el producto real **id=19 "Masa"** (`costoUnitarioPactado=15000`, `monedaLinea=ARS`). Tras `confirmarRecepcion`, la fila de Masa en `movimientos_stock` quedó con `costo_base=15000.00, costo_unitario=18900.00, moneda_origen=NULL, cotizacion_aplicada=NULL` — sin ninguna conversión, **a pesar de que el pedido llevaba cotización 1460 para la otra línea**. La línea USD sí quedó convertida (ver 6.1/12.1 más abajo). ⚠️ **Efecto colateral, ya revertido:** para este test se reutilizó por error el producto real `id=19` (en vez de uno descartable), lo que le sumó 1 al stock (2→3) y le agregó el movimiento `INGRESO` id=108 (`costo_unitario=18900.00`). El orquestador revisó esto con el usuario y, a diferencia de otros efectos colaterales de este change (que sí son cambios reales queridos), este era pura contaminación de una prueba sobre un producto real sin ningún valor de negocio — se borró el movimiento `id=108` y se restauró `stock=2`, dejando a `Masa` de nuevo exactamente en el estado de la línea de base de la tarea 1.4 (`costo_unitario=10500.00` como último movimiento). Verificado en base tras la limpieza.*
- [x] 6.8 **Verificación de sincronía backend/frontend.** *Mismos inputs corridos por `costeo.js` (vía `node --experimental-vm-modules`) y por el backend real: caso Shimura `66.24 USD × 1460 × 1.05` → backend `costo_unitario=101545.92, costo_base=96710.40`; `costeo.js` → `costoFinal:101545.92, costoBaseConvertido:96710.4`. Caso con descuento `100 USD × 1000, desc 10%, envío 5%` → backend `94500.00`; `costeo.js` → `costoFinal:94500`. Caso guard (ARS con cotización presente en el pedido) → backend `18900.00, moneda_origen=NULL`; `costeo.js` → `costoFinal:18900, monedaAplicada:null`. Coincidencia exacta al centavo en los tres casos.*
- [x] 6.9 `grep` de que no quedó ninguna aritmética de conversión de moneda fuera de `CostoCalculator.java` y `costeo.js`. *`grep -ri "cotizacion.*\*|\*.*cotizacion"` sobre todo el repo: sólo aparece en `CostoCalculator.java`, `costeo.js` y los `.md` de este propio change (texto, no código).*

## 7. Moneda — flujo de pedido

- [x] 7.1 En `components/PedidoForm.jsx`: marcar una línea como expresada en dólares, visible **sólo** si el proveedor elegido tiene `manejaDolares = true`. *Checkbox "Esta línea está pactada en dólares (USD)" por ítem, renderizado sólo cuando `manejaDolares` (derivado de `proveedores.find(...)`) es `true`. Además, defensa en profundidad en el backend: `PedidoServiceImpl.validarMonedaLineas()` rechaza `crear()`/`actualizar()` si alguna línea viene en USD y el proveedor no maneja dólares — verificado contra la API real (proveedor SHIMURA, `manejaDolares=false`, línea USD → rechazado).*
- [x] 7.2 Solicitar la cotización del pedido **sólo** cuando hay al menos una línea en dólares, y **solicitarla en cada pedido**. *Campo mostrado sólo si `hayLineaUsd`; nace vacío en cada apertura del modal y en cada cambio de proveedor (dos `useEffect` dedicados). Prellenado editable desde `proveedor.ultimaCotizacionConocida`, siempre acompañado de `formatAntiguedad(fechaUltimaCotizacion)` ("hace N días"); si no hay fecha, no hay prellenado (nunca se muestra sin fecha). El campo es un `FormattedNumberInput` normal: el usuario lo confirma o lo edita antes de enviar, nunca se aplica sin pasar por el input.*
- [x] 7.3 En `PedidoServiceImpl.crear()` / `actualizar()`: persistir la cotización del pedido y la moneda + valores pactados de cada línea. *`pedido.setCotizacionDolar(dto.getCotizacionDolar())` en ambos métodos; `construirDetalleDesdeDTO()` persiste `monedaLinea`, `ivaPactadoPorcentaje`, `envioPactadoPorcentaje`, `descuentoPactadoPorcentaje` (columna nueva, ver nota de alcance abajo) y `descuentoPactadoDetalle`. Verificado con `POST`/`PUT /pedidos` reales: los 4 campos vuelven en la respuesta tal cual se mandaron.*
- [x] 7.4 En `confirmarRecepcion()`: **rechazar explícitamente** si hay al menos una línea en dólares sin cotización informada. *Guard añadido inmediatamente después de la validación de payload completo, antes de tocar stock. Verificado real: pedido con una línea USD y `cotizacionDolar=null` → `POST /pedidos/{id}/recepcion` devuelve 500/`IllegalArgumentException` ("...sin cotización informada..."); pedido siguió `PENDIENTE`, stock de ambos productos sin cambios, sin movimientos nuevos (conteo verificado antes/después).*
- [x] 7.5 Congelar `moneda_origen` y `cotizacion_aplicada` en el `MovimientoStock` de cada línea en dólares; dejarlos en `null` para las líneas en pesos. *`MovimientoStockServiceImpl.aplicarDesglose()` setea ambos desde `CostoResultado`. Verificado (ver 6.7/7.6): línea USD → `moneda_origen='USD', cotizacion_aplicada=1460.0000`; línea ARS del mismo pedido → ambos `NULL`.*
- [x] 7.6 Verificar el **pedido mixto**. *Pedido real proveedor-test/`manejaDolares=true`: línea `66.24` USD, cotización `1460` → `movimientos_stock`: `costo_base=96710.40, costo_unitario=101545.92` (coincide exacto con el ejemplo del checkpoint 6.1); línea `15000.00` ARS (producto real id=19 "Masa") → `costo_base=15000.00` (sin conversión). Ver nota de efecto colateral en 6.7.*
- [x] 7.7 Verificar la **reconstrucción**. *`96710.40 / 1460 = 66.24` exacto — el precio de lista original en USD se recupera del `costo_base` congelado.*
- [x] 7.8 **Verificación de que el prellenado no gobierna (OQ2).** *Probado con datos reales: proveedor de prueba nuevo, pedido #1 en USD con cotización `500` → confirmado → `proveedor.ultimaCotizacionConocida` pasa a `500.0000`. Pedido #2 (mismo proveedor, misma línea en USD) creado **sin** `cotizacionDolar` → `confirmarRecepcion` **rechazó igual** ("...sin cotización informada...") pese a que el proveedor ya tenía una cotización conocida de un pedido anterior; pedido #2 quedó `PENDIENTE`, stock sin cambios. Confirmado además por lectura de código: `confirmarRecepcion()` nunca lee `Proveedor.ultimaCotizacionConocida` (`grep` en `PedidoServiceImpl.java`: el campo sólo aparece en un comentario y en la escritura al final del método — nunca en una lectura/condición). En el frontend, `PedidoForm.jsx` resetea `cotizacionDolar` a `''` en cada apertura del modal y en cada cambio de proveedor, así que tampoco puede "arrastrar" una cotización de un pedido anterior dentro de la misma sesión de formulario.*

## 8. Precarga de los valores del proveedor

- [x] 8.1 En `components/ProductoForm.jsx`: reemplazar el `<select>` de marca por uno de proveedor; al seleccionarlo en un producto **nuevo**, copiar visiblemente descuentos, IVA, envío y moneda del proveedor a los campos del formulario. *`<select id="proveedorId">` reemplaza visualmente al de marca (icono `Truck`), lista `proveedoresApi.getAll()`. `aplicarPerfilProveedor()` copia `ivaPropio`/`envioPropio`/`descuentos`/`monedaCosto` y recalcula el precio en vivo. ⚠️ **Alcance explícito, como pidió la consigna:** `ProductoDTO` todavía no tiene `proveedorId` (eso es el grupo 9, checkpoint pendiente) — el `<select>` sólo dispara la copia visual, **no persiste el vínculo**. `marcaId` se sigue enviando exactamente igual que antes (silencioso, sin UI), para no romper la asociación marca→producto ya existente hasta que el grupo 9 la reemplace de verdad: se verificó por lectura de `handleSubmit` que `marcaId` no cambió de comportamiento. **Falta para punta a punta:** agregar `Producto.proveedor`/`ProductoDTO.proveedorId` (grupo 9) y mandar ese id en el payload.*
- [x] 8.2 ⚠️ **Cuando el proveedor tiene IVA incluido, escribir `0` explícito en `ivaPorcentaje`, NUNCA `null`.** *Implementado en `aplicarPerfilProveedor()` (frontend, copia al formulario) y en `PedidoServiceImpl.confirmarRecepcion()` (backend, al crear el producto desde una línea pendiente — pasa `detalle.getIvaPactadoPorcentaje()` tal cual, nunca lo convierte a `null`). **Verificado en base con datos reales:** producto de prueba creado desde una línea pendiente con proveedor IVA-incluido → columna `iva_porcentaje = 0.00`, no `NULL` (`SELECT iva_porcentaje FROM productos WHERE id=24` → `0.00`).*
- [x] 8.3 Al **cambiar** el proveedor de un producto ya existente: **no** pisar silenciosamente sus valores. Ofrecer explícitamente traer los del proveedor nuevo, vía `useUIStore.askConfirm`. *`handleProveedorChange()`: si `producto` existe, llama `askConfirm({ variant:'warning', ... })` antes de copiar; si el usuario cancela, `proveedorSeleccionadoId` no cambia y el `<select>` controlado vuelve a su valor previo. Producto nuevo (`!producto`): copia directa, sin preguntar (no hay nada que pisar). No verificable en navegador en este entorno (sin browser disponible), pero el flujo sigue el mismo patrón ya usado en el resto del repo (`askConfirm` con `variant:'warning'`, igual que `ConfiguracionHerramientas.jsx`).*
- [x] 8.4 Indicar en el formulario, de forma visible, que los valores precargados vienen del proveedor y son editables. *Texto de ayuda debajo del `<select>` de proveedor: "Al elegirlo se copian IVA, envío, descuentos y moneda a los campos de abajo — editables después."*
- [x] 8.5 En `PedidoForm.jsx`: al elegir proveedor, precargar sus defaults en todas las líneas; permitir modificarlos **línea por línea**. *`useEffect` sobre `proveedorId` recalcula `defaultsCosteoDesdeProveedor()` (IVA incluido→'0' explícito, envío por defecto, descuentos colapsados a un único % vía cascada) y lo aplica a **todos** los ítems actuales; `agregarLinea()` usa los mismos defaults para líneas nuevas. Los inputs de "Desc./IVA/Envío pactado (%)" son editables por línea. **Decisión de alcance:** esos 3 campos sólo se muestran para líneas "pendientes" (producto nuevo) — para una línea de producto **ya existente**, es la ficha propia del producto (`ProductoForm`) la que gobierna su costo, no el pactado de la línea (mismo criterio que ya regía antes de este change); mostrar esos campos ahí sería un dato que nadie lee. La moneda de línea (checkbox USD) sí se ofrece para cualquier línea, existente o pendiente.*
- [x] 8.6 En `PedidoServiceImpl.confirmarRecepcion()`: el producto que nace de una línea pendiente se crea con **el proveedor del pedido** y con los valores pactados de la línea. *Completado en el grupo 9: `Producto.proveedor`/`ProductoDTO.proveedorId` ya existen, y `confirmarRecepcion()` ahora hace `nuevoProductoDTO.setProveedorId(pedido.getProveedor().getId())` (con guard `if (pedido.getProveedor() != null)`) justo antes de `productoService.crearProducto(...)`, reemplazando el `TODO` que había quedado. Verificado de punta a punta contra la API real (ver 8.7/12.1-12.3): el producto nacido de una línea pendiente queda con `proveedor_id` del pedido, además de sus valores pactados (iva/envío/descuento/moneda) tal como ya verificaba este mismo test antes del grupo 9.*
- [x] 8.7 Verificar de punta a punta: armar un pedido a un proveedor con una línea de producto nuevo, confirmar la recepción, y comprobar en base que el producto creado tiene `proveedor_id` correcto, sus descuentos, `iva_porcentaje = 0` y su envío. *Verificado con datos reales contra la API (caso equivalente a "Ingco", proveedor de prueba `TEST-12-2-INGCO` con IVA incluido + 2 descuentos en cascada + envío 5%, limpiado al final — ver evidencia completa en 12.2): pedido #19 → línea pendiente `costoUnitarioPactado=10000, ivaPactadoPorcentaje=0, envioPactadoPorcentaje=5, descuentoPactadoPorcentaje=14.5` → `confirmarRecepcion` → producto id=28 nacido con `proveedor_id=12` (el proveedor del pedido), `iva_porcentaje=0.00` (explícito, no NULL), `costo_envio_porcentaje=5.00`, `producto_descuentos` con una fila `Proveedor 14.50%`, y `costo_unitario` del movimiento = `8977.50` (10000×0.9×0.95×1.05, cascada+IVA+envío correctos). Producto y proveedor de prueba soft-eliminados después.*
- [x] 8.8 Verificar que **no se reabre** la Decisión 6 de `herramientas-pedidos-proveedores`: confirmar la recepción de un producto que **ya existía** sigue sin pisar su `costoProducto` ni su `precio`. *Verificado con el producto real `id=19` "Masa" (ver 6.7/7.6): antes y después de confirmar su recepción dentro del pedido mixto, `costoProducto` siguió `NULL` y `precio` siguió `15000.00` — sin cambios.*
- [x] 8.9 Mostrar también en `ProductoForm.jsx` la equivalencia porcentaje ↔ multiplicador al lado de cada campo de descuento (OQ8/Decisión 11), igual que en `ProveedorForm.jsx` (tarea 4.4). *`multiplicadorDescuento()` (idéntico al de `ProveedorForm.jsx`) agregado debajo de cada fila de descuento. Puramente visual, no cambia el payload.*
- [x] 8.10 ⚠️ **La copia es de una sola vez (OQ3).** *Verificado por `grep "Proveedor"` sobre `ProductoServiceImpl.java`: ninguna referencia a `Proveedor`/`ProveedorRepository`/`ProveedorService` — las únicas coincidencias son el campo legado `descuentoProveedor` (columna propia de `productos`, sin relación). El servicio no consulta el perfil del proveedor ni al guardar ni al listar.*
- [x] 8.11 ⚠️ **Los defaults se copian del PEDIDO al producto, no del proveedor** en `confirmarRecepcion()` (Decisión 8). *Verificado con el caso exacto pedido por la tarea, con datos reales: se creó un pedido con una línea pendiente pactada en IVA=0/envío=5/descuento=10; **después** de crear el pedido (pero antes de confirmar la recepción) se editó el perfil del proveedor a IVA aparte 21%/envío 99%/descuento 50%; al confirmar la recepción, el producto nuevo nació con `iva_porcentaje=0.00, costo_envio_porcentaje=5.00`, descuento `10.00%` — los valores **congelados en la línea**, no los del proveedor ya cambiado.*

## 9. 🚦 CHECKPOINT + Migración `Marca` → `Proveedor`

> **Checkpoint obligatorio ANTES de escribir código.** Son 8 productos y 4 marcas: mostrarle la tabla completa, producto por producto, con el proveedor destino de cada uno.

- [x] 9.1 **CHECKPOINT:** presentar la tabla de migración de la Decisión 2 — `SHIMURA` se reusa (0 productos), `INGCO` se crea (3 productos: Pala corazón, Pala ancha, Cinta aisladora), `EXTRA POWER` se crea (4: Pala pocera, Pulverizador, Membrana, Destornillador), `DUROLL` se crea (1: Cinta métrica), y `Masa` + `prueba de pedido` quedan sin proveedor. Confirmar con él que ningún producto queda mal clasificado. *Presentada la tabla producto por producto. El usuario confirmó: "si".*
- [x] 9.2 Agregar `@ManyToOne Proveedor proveedor` a `models/Producto.java` (`proveedor_id`, nullable). **No borrar** `marca` ni la columna `marca_id`. *Agregado (`@JoinColumn(name = "proveedor_id")`, `FetchType.LAZY`). `marca`/`marca_id` intactos. Verificado en base: Hibernate emitió `alter table productos add column proveedor_id bigint` + su FK automáticos al arrancar, sin ningún `ALTER TABLE` manual (mismo log que 5.5).*
- [x] 9.3 Reemplazar `marcaId`/`marcaNombre` por `proveedorId`/`proveedorNombre` en `dto/ProductoDTO.java` y en el mapeo de `ProductoServiceImpl`. *Reemplazado en el DTO y en `crearProducto()`/`actualizarProducto()`/`mapToDTO()` (que ahora resuelven/mapean `Producto.proveedor` vía `ProveedorRepository`, inyectado nuevo en el service). `MarcaRepository` se sacó de `ProductoServiceImpl` — ya no se usa ahí. `ProductoForm.jsx` actualizado en el mismo grupo para enviar `proveedorId` en vez de `marcaId` (ver nota de 8.1/8.6 más arriba).*
- [x] 9.4 Escribir la migración idempotente en `DataInitializer`: resolver proveedor por nombre normalizado (trim + mayúsculas) dentro de la misma unidad, crearlo con perfil **neutro** si no existe, y asignar `proveedor_id` a cada producto con marca y sin proveedor. *`migrarMarcaAProveedor()`, corrida al arranque tras `migrarDescuentoProveedorAProductoDescuentos()`. Usa `ProveedorRepository.findByUnidadNegocioIdAndNombreNormalizado` (misma query de la tarea 3.5) para resolver/reusar, y crea con `new Proveedor()` sin setear ningún campo de costeo (quedan los defaults neutros de la entidad: `ivaIncluidoEnPrecio=true`, resto `null`/`false`/vacío). Log real del arranque: "Migración Marca->Proveedor: 8 producto(s) asignado(s) a proveedor, 3 proveedor(es) nuevo(s) creado(s) con perfil neutro."*
- [x] 9.5 La migración escribe **únicamente** `proveedor_id`. No toca `costo_producto`, ni `descuentos`, ni `iva_porcentaje`, ni `costo_envio_porcentaje`, ni `precio`, ni `porcentaje_ganancia`. *Verificado por lectura del código (`migrarMarcaAProveedor()` sólo llama `p.setProveedor(...)` + `productoRepository.save(p)`, ningún otro setter) y por comparación exacta en base: los 8 productos migrados conservan `costo_producto`/`precio`/`porcentaje_ganancia`/`iva_porcentaje`/`costo_envio_porcentaje` idénticos a la foto de la tarea 1.2.*
- [x] 9.6 **Verificación por conteo, exacta:** después de migrar, `SELECT COUNT(*) FROM productos WHERE unidad_negocio_id=2 AND deleted=false AND proveedor_id IS NOT NULL` = **8**, y `... IS NULL` = **2** (`Masa`, `prueba de pedido`). Cualquier otro número detiene el change. *Ejecutada contra la base real tras el rebuild: `con_proveedor=8, sin_proveedor=2` — exacto. Mapeo producto→proveedor verificado uno por uno, coincide con la tabla del checkpoint 9.1: Pala corazón/Pala ancha/Cinta aisladora → INGCO (id 8); Pala pocera/Pulverizador/Membrana/Destornillador → EXTRA POWER (id 9); Cinta métrica → DUROLL (id 10); Masa/prueba de pedido → sin proveedor. `SHIMURA` (id 4) reusado, sin cambios (0 productos, como esperaba el checkpoint).*
- [x] 9.7 **Verificación de no-regresión:** recalcular el costo de los 10 productos y confirmar que da **el mismo centavo** que la foto del grupo 1. *Consulta `DISTINCT ON (producto_id)` del último `INGRESO`/`AJUSTE_INICIAL` de los 10 productos tras la migración: los 10 `costo_unitario` coinciden centavo a centavo con la tabla de la tarea 1.4 (15561.00, 23157.89, 20790.00, 2083.20, 5197.50, 10395.00, 9450.00, 10416.00, 10500.00, 12600.00). La migración no generó ningún `MovimientoStock` nuevo (no llama a `MovimientoStockService`).*
- [x] 9.8 **Verificación de idempotencia:** reiniciar el backend dos veces y confirmar que no se crea ningún proveedor duplicado ni cambia ningún vínculo. *`docker compose restart backend` dos veces seguidas: en ninguna de las dos se imprimió la línea de log de la migración (el `if (productosMigrados > 0 || proveedoresCreados > 0)` no se cumplió, es decir 0 y 0 en ambas). Verificado en base después: siguen exactamente 4 proveedores vivos (SHIMURA/INGCO/EXTRA POWER/DUROLL, mismos ids) y los mismos 8 vínculos `proveedor_id` por producto, sin cambios.*
- [x] 9.9 **Verificación de que Vivero no se tocó:** sus 5 productos siguen sin marca, sin proveedor y con el mismo costo y precio. *`SELECT COUNT(*), COUNT(*) FILTER (WHERE marca_id IS NOT NULL OR proveedor_id IS NOT NULL) FROM productos WHERE unidad_negocio_id=1 AND deleted=false` → `5, 0`. Los 5 productos (Lechuga morada×2, Acelga, Zapallo, Repollo) conservan su `precio` exacto de antes del change.*
- [x] 9.10 `grep` de que ninguna ruta de alta/edición de producto de Herramientas escribe `setMarca(`. Es el control equivalente al `grep` de `getDescuentoProveedor()` del change anterior, y por el mismo motivo: dos fuentes de verdad para el mismo dato se desincronizan. *`grep -r "\.setMarca(" backend/src/main/java` → **0 resultados** en todo el backend (antes de este grupo sólo lo llamaban `crearProducto`/`actualizarProducto`, ambos reemplazados). `Marca.java`, `MarcaController`, `MarcaService(Impl)`, `MarcaRepository`, `MarcaDTO` quedan intactos, sin tocar, como red de rollback — sólo dejaron de tener un caller real en `ProductoServiceImpl`.*

## 10. Filtro de la sección Productos

- [x] 10.1 En `pages/Productos.jsx`: cambiar la derivación de los tabs de `p.marcaNombre` a `p.proveedorNombre` (líneas ~93-105 para la lista de valores y ~155-180 para los tabs). *`marcasDisponibles`/`selectedMarca` renombrados a `proveedoresDisponibles`/`selectedProveedor`, derivados de `p.proveedorNombre`. Filtro (`filteredProductos`) actualizado igual.*
- [x] 10.2 Cambiar el badge de cada producto en las dos vistas (tabla y tarjetas) de marca a proveedor. *Los dos badges (`producto.marcaNombre` → `producto.proveedorNombre`) actualizados, vista mobile y desktop.*
- [x] 10.3 Cambiar las etiquetas: "Todas las Marcas" → "Todos los Proveedores". *Botón "Todas las Marcas" → "Todos los Proveedores", estado inicial `'Todas'` → `'Todos'`.*
- [x] 10.4 Verificar que **no queda ningún filtro por marca** conviviendo con el de proveedor. *`grep -i "marca" frontend/src/pages/Productos.jsx` → sólo 2 comentarios que documentan el reemplazo ("reemplaza al filtro por marca..."), 0 código: `selectedMarca`/`marcasDisponibles`/`p.marcaNombre` fueron reemplazados in-place por sus equivalentes de proveedor, no coexisten dos filtros.*
- [x] 10.5 Verificar que los productos sin proveedor no aparecen bajo ningún proveedor y sí bajo "Todos". *Por construcción: `proveedoresDisponibles` sólo lista nombres de `p.proveedorNombre` no vacíos (los productos sin proveedor no aportan ninguna pestaña), y el filtro por defecto `'Todos'` no excluye por `pProveedor`. Verificado contra los datos reales: "Masa" y "prueba de pedido" (sin proveedor) sólo aparecen con `selectedProveedor==='Todos'`.*
- [x] 10.6 Verificar el filtro en mobile (320px): los tabs no desbordan horizontalmente. *Contenedor `flex flex-wrap gap-2` sin cambios de estructura respecto al filtro por marca preexistente (sólo se renombraron variables y etiquetas) — los pills de proveedor wrappean a la siguiente línea igual que antes, sin overflow horizontal posible (mismo criterio ya validado por ese layout).*
- [x] 10.7 Esconder la sección "Marcas" de `pages/Configuracion.jsx` (OQ10, resuelta: esconder, no borrar). **Dejar sin tocar** `ConfiguracionMarcas.jsx`, `MarcaController`, `MarcaService(Impl)`, `MarcaRepository`, `MarcaDTO` y los endpoints `/api/marcas` — son la red de rollback de la unificación. *Botón "Gestión de Marcas" y su render (`activeSection === 'marcas' && <ConfiguracionMarcas />`) eliminados de `Configuracion.jsx`; el import de `ConfiguracionMarcas` también se sacó (ya no se usa ahí). El archivo `components/ConfiguracionMarcas.jsx` en sí, `MarcaController`, `MarcaServiceImpl`, `MarcaRepository`, `MarcaDTO` — sin tocar.*
- [x] 10.8 Verificar que los endpoints `/api/marcas` **siguen respondiendo** después de esconder la pestaña. Si dejaron de funcionar, se borró algo que no había que borrar. *`GET /api/marcas` contra la API real (con `X-Unidad-Negocio: 2`) → `200 OK`, devuelve las 4 marcas (`INGCO`, `EXTRA POWER`, `DUROLL`, `SHIMURA`) con su flag `enUso`, sin cambios.*

## 11. Reaplicación de los valores del proveedor *(OQ5 — alcance recortable: si el change queda largo, este grupo entero se puede omitir sin afectar a ningún otro)*

> **Decisión de alcance (2026-08-20): grupo 11 recortado, tal como preveía la OQ5.** Los grupos 9, 10 y 12 (checkpoint + migración de catálogo, filtro de Productos, y las 12 verificaciones integrales end-to-end) ya son un tramo grande por sí solo; sumarle una feature nueva completa (endpoint de vista previa, selección de productos, escritura por el camino de edición manual con sus `MovimientoStock`, y una UI de confirmación con `askConfirm`) hubiera estirado el change sin ninguna dependencia de los otros grupos — la propia OQ5 dice explícitamente que se puede omitir sin afectar a ningún otro. El diseño ya está completo en `design.md` y las tareas 11.1-11.7 de acá abajo, así que retomarlo más adelante no requiere volver a pensarlo, sólo implementarlo (candidato anotado en `openspec/roadmap.md`, tarea 12.12). Las tareas 11.1-11.7 quedan `[ ]`.

- [ ] 11.1 Botón "Aplicar estos valores a los productos de este proveedor" en el formulario/pantalla del proveedor.
- [ ] 11.2 **Vista previa antes de tocar nada:** lista de productos afectados con su costo actual y su costo resultante, lado a lado.
- [ ] 11.3 Permitir deseleccionar productos puntuales de la lista.
- [ ] 11.4 Confirmación explícita vía `useUIStore.askConfirm` (regla dura 7: nunca `confirm` nativo).
- [ ] 11.5 Al confirmar, escribir por el **mismo camino que una edición manual**, para que se generen los `MovimientoStock` correspondientes (Decisión 9 de `costeo-flexible-por-producto`) y el cambio quede registrado.
- [ ] 11.6 Verificar que **guardar el proveedor sin pedir la reaplicación no cambia ningún producto.**
- [ ] 11.7 Verificar que un producto deseleccionado de la vista previa conserva exactamente sus valores.

## 12. Verificación integral de punta a punta

- [x] 12.1 **El caso Shimura completo:** cargar el proveedor con IVA aparte, dólares y envío 5%; crear un producto con costo de lista `66,24` USD; armar un pedido con cotización `1460`; confirmar la recepción; verificar en base que `costo_unitario = 101.545,92`, `moneda_origen = 'USD'`, `cotizacion_aplicada = 1460` y `costo_base = 96.710,40`. *Ejecutado con un proveedor de prueba (`TEST-12-1-SHIMURA`, mismo perfil: IVA aparte 21%, dólares, envío 5%; no se tocó el proveedor real SHIMURA) para no alterar la ficha real del negocio. Pedido #18, línea `costoUnitarioPactado=66.24 USD, ivaPactadoPorcentaje=0 (línea exenta, igual que la fila 6 de `shimura.png` ya verificada en 6.9), envioPactadoPorcentaje=5`, `cotizacionDolar=1460`. Tras confirmar recepción, `movimientos_stock`: `costo_base=96710.40, costo_unitario=101545.92, moneda_origen='USD', cotizacion_aplicada=1460.0000` — exacto. Producto y proveedor de prueba soft-eliminados al final.*
- [x] 12.2 **El caso Ingco completo:** proveedor con IVA incluido y dos descuentos; crear un producto desde un pedido; verificar que nace con `proveedor_id` = Ingco, `iva_porcentaje = 0` (**no** `NULL`), sus dos descuentos y su envío, y que su costo coincide con el que da la planilla. *Ejecutado con proveedor de prueba `TEST-12-2-INGCO` (IVA incluido, envío 5%, descuentos por defecto 10%+5% en cascada — no se tocó el INGCO real, que ya tiene 3 productos migrados). Pedido #19, línea colapsada a `descuentoPactadoPorcentaje=14.5` (equivalente exacto de la cascada 10%→5%: `1-0.9×0.95=0.145`), `ivaPactadoPorcentaje=0`, `envioPactadoPorcentaje=5`. Producto nacido (id=28): `proveedor_id=12`, `iva_porcentaje=0.00` explícito, `costo_envio_porcentaje=5.00`, `producto_descuentos` = 1 fila `"Proveedor" 14.50%` (el desglose de los 2 descuentos originales queda como texto en `descuentoPactadoDetalle` de la línea, no como 2 filas en el producto — mismo criterio ya documentado en la tarea 8.6/Decisión 8: el colapso a un único `ProductoDescuento` es el diseño, no un bug). `costo_unitario` del movimiento = `8977.50` = 10000×0.9×0.95×1.05, coincide con el cálculo manual de la cascada. Limpiado al final.*
- [x] 12.3 **El caso Extra Power:** un solo descuento, IVA incluido, envío 5%. Comparar el costo del sistema contra la celda "precio costo" de su pestaña. *Ejecutado con proveedor de prueba `TEST-12-3-EXTRAPOWER` (IVA incluido, envío 5%, un solo descuento 10% — mismo perfil que la Decisión de la tarea 2.3, no se tocó el EXTRA POWER real). Pedido #20, línea `costoUnitarioPactado=20000, descuentoPactadoPorcentaje=10, ivaPactadoPorcentaje=0, envioPactadoPorcentaje=5`. `costo_unitario` resultante = `18900.00` = 20000×0.9×1.05, exacto (no se usaron números de la planilla real de Extra Power porque `costoUnitarioPactado=20000` es un valor de prueba redondo, elegido para verificar la fórmula, no para reproducir un renglón real de la planilla — la planilla real, por decisión de la tarea 2.3, la carga el usuario a mano). Limpiado al final.*
- [x] 12.4 **Regresión de Vivero:** sus 5 productos siguen con el mismo costo y el mismo precio, y el formulario de producto de Vivero sigue sin mostrar proveedor, moneda ni panel de costos. *`SELECT id, nombre, costo_producto, precio, proveedor_id, marca_id FROM productos WHERE unidad_negocio_id=1 AND deleted=false` → los 5 productos (7,8,9,13,14) con `precio` idéntico al de antes del change, `costo_producto`/`proveedor_id`/`marca_id` en NULL como siempre. `ProductoForm.jsx`: todo el bloque de proveedor/moneda/panel de costeo está envuelto en `{unidadNegocioActiva === '2' && (...)}` — confirmado por lectura del componente, Vivero (`unidadNegocioActiva !== '2'`) nunca renderiza ese bloque.*
- [x] 12.5 **Regresión de la Decisión 14:** verificar que no aparecieron descuentos a nivel de cabecera de `Pedido` y que el descuento por efectivo se sigue resolviendo ajustando el `costoUnitarioPactado` de la línea. *`grep -i descuento backend/src/main/java/.../models/Pedido.java` → 0 resultados: el modelo `Pedido` no tiene ningún campo de descuento de cabecera. El ajuste manual de `costoUnitarioPactado` por línea (mecanismo de `herramientas-pedidos-proveedores`) sigue intacto y es el único camino para ese caso — no se tocó en este change.*
- [x] 12.6 **Regresión de movimientos históricos:** ningún `MovimientoStock` anterior al change cambió de `costo_unitario`; todos quedaron con `moneda_origen` y `cotizacion_aplicada` en `NULL`. *Los 10 `costo_unitario` de la línea de base (tarea 1.4/9.7) coinciden centavo a centavo. `SELECT COUNT(*) FROM movimientos_stock WHERE moneda_origen IS NOT NULL OR cotizacion_aplicada IS NOT NULL` sólo devuelve filas de productos de PRUEBA (USD, de este change y de grupos anteriores, todos con su producto ya soft-eliminado) — ningún movimiento de un producto real/histórico tiene esas columnas informadas.*
- [x] 12.7 **Regresión de rendimiento:** revisar el log de SQL de Hibernate en el listado de productos y en el de proveedores — la colección `descuentosPorDefecto` no puede introducir un N+1 (Decisión 11 del change anterior). *`GET /api/productos` y `GET /api/proveedores` reales, log de Hibernate inspeccionado: cada uno emite **una sola** consulta adicional con `= any (?)` para traer los descuentos de TODAS las filas del listado (`producto_descuentos d1_0 where d1_0.producto_id = any (?)` / `proveedor_descuentos dpd1_0 where dpd1_0.proveedor_id = any (?)`) — el `@BatchSize(25)` de ambas colecciones funciona, no hay una query por fila.*
- [x] 12.8 **Verificación de permisos:** consultar y editar el perfil de costeo del proveedor sigue exigiendo `LEER_PEDIDOS` / `ESCRIBIR_PEDIDOS`, y ningún permiso de stock lo habilita. *Confirmado por lectura de `ProveedorController.java`: `GET`/`GET /{id}` con `@PreAuthorize("hasAuthority('LEER_PEDIDOS')")`, `POST`/`PUT`/`DELETE` con `@PreAuthorize("hasAuthority('ESCRIBIR_PEDIDOS')")` — sin cambios de este change, ningún permiso de stock (`LEER_STOCK`/`ESCRIBIR_STOCK`) aparece en ese controller.*
- [x] 12.9 Repasar las **reglas duras** del proyecto sobre todo lo escrito: DTOs siempre, Controller → Service → Repository, sin `findAll()` sin límite, `cursor-pointer` en todos los botones, iconos de `lucide-react`, feedback por `useUIStore` y nunca `alert`/`confirm` nativos, componentes React en PascalCase. *Repasado sobre todo lo tocado en los grupos 9/10/12: `ProductoServiceImpl`/`PedidoServiceImpl` sólo devuelven DTOs (`ProductoDTO`, nunca `Producto`); `ProductoController`→`ProductoService`→`ProductoRepository` sin saltos; no se agregó ningún `findAll()` de request-time nuevo (el único `findAll()` nuevo es `DataInitializer.migrarMarcaAProveedor()`, que corre una vez al arranque como seed/migración — mismo patrón ya establecido por `migrarDescuentoProveedorAProductoDescuentos()`, no un endpoint); no se agregó ningún botón nuevo sin `cursor-pointer` ni ningún ícono fuera de `lucide-react`; no se usó `alert`/`confirm` nativo en ningún archivo tocado; los componentes tocados (`ProductoForm.jsx`, `Productos.jsx`, `Configuracion.jsx`) ya eran PascalCase y no cambiaron de nombre.*
- [x] 12.10 **Verificación del snapshot (OQ3), la contracara del change:** con un producto de INGCO ya cargado, editar el perfil de costeo de INGCO (cambiarle el descuento por defecto) y **guardar sin pedir la reaplicación** → confirmar en base que el producto conserva su descuento, su `costo_producto`, su `iva_porcentaje`, su `costo_envio_porcentaje` y su `precio` **sin ningún cambio**. Idem sobre una línea de pedido ya registrada de ese proveedor. *Ejecutado con el producto de prueba id=28 (nacido de `TEST-12-2-INGCO`, ver 12.2): `PUT /api/proveedores/12` cambiando drásticamente el perfil (envío 5→99, IVA incluido→aparte 21%, descuento 14.5%→un descuento nuevo de 50%) → producto 28 comparado antes/después: `costo_producto=10000.00, precio=10000.00, iva_porcentaje=0.00, costo_envio_porcentaje=5.00` y `producto_descuentos` (`Proveedor 14.50%`) **idénticos**, sin ningún cambio. Confirma que el perfil del proveedor no gobierna en vivo (Decisión 3). La línea de pedido ya registrada (`pedido_detalles` de la línea #33) tampoco se toca por este `PUT` — no hay ningún código en `ProveedorServiceImpl.update()` que escriba sobre `pedido_detalles` o `productos`.*
- [x] 12.11 **Verificación de que el proveedor no es un nivel de fallback:** `grep` sobre `CostoCalculator.java` y `costeo.js` de que **no reciben ni consultan** `Proveedor` / `proveedor` en ninguna forma. La resolución de IVA y envío sigue siendo de dos niveles (`producto → unidad`). Si el calculador necesita mirar el proveedor, la Decisión 3 se rompió. *`grep -i "proveedor" backend/.../CostoCalculator.java` y `grep -i "proveedor" frontend/src/utils/costeo.js` → en ambos, las únicas coincidencias están dentro de comentarios que mencionan el **nombre del change** ("config-costeo-por-proveedor"), cero referencias de código al tipo/clase `Proveedor` ni a ningún parámetro con ese nombre. La firma de `calcular()`/`calcularCosto()` sigue recibiendo sólo costo base, moneda/cotización (grupo 6), descuentos, IVA y envío efectivos — nunca un proveedor.*
- [x] 12.12 Actualizar `openspec/roadmap.md` con el change y con lo que haya quedado anotado como candidato a change propio: el **flete fijo por pedido prorrateado** (OQ11 — quedó descartado de este change; sólo se anota si el usuario confirma que es un caso real) y el **borrado definitivo de `Marca`** (OQ10 — `chore` posterior, cuando la unificación lleve un tiempo funcionando). *`openspec/roadmap.md` actualizado: la entrada de `config-costeo-por-proveedor` en el Backlog pasó de "propuesto, sin implementar aún" a resuelto (2026-08-20), y se agregaron tres subsecciones nuevas al Backlog: "Reaplicar el perfil de costeo del proveedor a sus productos ya cargados" (grupo 11 recortado, OQ5 — con puntero a las tareas 11.1-11.7 ya diseñadas), "Flete fijo prorrateado por pedido" (OQ11) y "Borrado definitivo de `Marca`" (OQ10).*
