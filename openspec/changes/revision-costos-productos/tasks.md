> ### ⚠️ Gobernanza: **MEDIA** — no toca la fórmula de costeo, pero sí mueve precios de venta
>
> Este change **no modifica `CostoCalculator` en ninguna línea** (contrato de no-regresión) y **no reabre la Decisión 6 de `herramientas-pedidos-proveedores`**: confirmar un pedido sigue sin pisar `costoProducto`/`precio`. Sólo lee un valor ya congelado y ofrece copiarlo por el mismo camino que hoy usa una edición manual en `ProductoForm.jsx`. Por eso está un escalón por debajo de `costeo-flexible-por-producto` y `config-costeo-por-proveedor` (ambos MEDIA-ALTA) y **no requiere checkpoint de fórmula**.
>
> **Sí requiere un checkpoint liviano: el grupo 4, ANTES de cablear la acción "Actualizar".** Se le muestra al usuario el criterio de detección **corriendo sobre los productos reales del grupo 1** —qué filas aparecen, cuáles no y por qué— y se confirma que coincide con lo que él espera ver. Sigue siendo precio de venta al público: una fila de más que el jefe aprieta sin pensar le mueve un precio.
>
> ### ✅ **Las 4 Open Questions de `design.md` ya están resueltas** y quedaron registradas en la tarea 6.1. La **OQ1** —la única genuinamente abierta— la decidió el usuario explícitamente: **"Descartar" persiste el `id` del movimiento ya revisado**, en una sola columna nullable `productos.movimiento_revision_descartado_id` (opción C, ver Decisión 11). Las **OQ2, OQ3 y OQ4** quedaron por la recomendación documentada, sin objeción. **Ninguna tarea de este documento debe volver a plantearlas como abiertas, y ninguna puede implementar el descarte como estado local de React ni como par de columnas de costo — las opciones A y B están descartadas.**
>
> **Recordatorios de alcance que salen de las decisiones de `design.md`:**
> - Se compara `costo_producto` contra **`costo_base`** (las dos bases), **nunca** contra `costo_unitario`. Comparar contra el costo final daría 11 filas de 11 y el panel dejaría de significar algo (Decisión 1).
> - "Actualizar" **reusa `PUT /api/productos/{id}`**. Ninguna tarea puede crear una segunda ruta de escritura de costo/precio (Decisión 5).
> - "Actualizar" copia **sólo el costo base**. Nunca el descuento, el IVA ni el envío congelados del movimiento (Decisión 6).
> - Un producto con `monedaCosto = USD` **nunca** recibe un importe en pesos en su `costoProducto` (Decisión 4). Es el único error de dinero real que este change puede producir.
> - **Regla dura 1:** no buildear sin pedido explícito. **Regla dura 2:** no commitear sin pedido explícito.

## 1. Línea de base — la foto de "antes", y la fabricación del caso de prueba

- [x] 1.1 **Verificar que los deltas de `frontend-productos` no colisionan.** *Verificado 2026-08-21. Requisitos en `revision-costos-productos/specs/frontend-productos/spec.md`: "Panel de Revisión de Costos en la Sección Productos", "Contraste Visible en Cada Fila del Panel", "Acciones de Un Click en el Panel", "Degradación del Panel ante un Backend sin el Endpoint" — los cuatro nuevos. Requisitos en `config-costeo-por-proveedor/specs/frontend-productos/spec.md`: "Filtrado por Proveedor en Herramientas" (el renombre de "Filtrado por Marca..."), "Proveedor en el Formulario de Producto", "Moneda del Costo en el Formulario de Producto", "Equivalencia entre Porcentaje y Multiplicador...". Cero nombres repetidos. Los dos deltas se aplican en cualquier orden (R6).*
- [x] 1.2 **Registrar la línea de base de Herramientas.** Query verificada el 2026-08-21 contra `vivero-postgres` / `vivero_db` (usuario `admin`):
  ```sql
  SELECT p.id, p.nombre, pr.nombre AS proveedor, p.moneda_costo,
         p.costo_producto, p.porcentaje_ganancia, p.precio,
         m.id AS mov_id, m.costo_base, m.costo_unitario, m.moneda_origen,
         m.cotizacion_aplicada, m.tipo_movimiento, m.fecha
  FROM productos p
  LEFT JOIN proveedores pr ON pr.id = p.proveedor_id
  LEFT JOIN LATERAL (
      SELECT ms.* FROM movimientos_stock ms
      WHERE ms.producto_id = p.id AND ms.deleted = false
        AND ms.tipo_movimiento IN ('INGRESO','AJUSTE_INICIAL')
      ORDER BY ms.fecha DESC, ms.id DESC LIMIT 1
  ) m ON true
  WHERE p.deleted = false AND p.unidad_negocio_id = 2
  ORDER BY p.id;
  ```
  Resultado esperado según la medición del 2026-08-21: **11 productos, los 11 con `costo_producto == costo_base` exacto, los 11 con `moneda_costo = 'ARS'`**. Anotar la tabla completa en esta tarea. Si el resultado difiere, **detenerse y reportar**: la base cambió desde la medición y el resto del change se diseñó sobre esos números.
  *Re-verificado 2026-08-21, antes de tocar nada: 11 filas, las 11 con `costo_producto == costo_base` exacto, las 11 en ARS. Coincide con lo esperado.*

  | id | nombre | proveedor | costo_producto | costo_base | costo_unitario | precio |
  |---|---|---|---|---|---|---|
  | 1 | Pala corazón | INGCO | 15000.00 | 15000.00 | 15561.00 | 20229.30 |
  | 2 | Pala ancha | INGCO | 22233.00 | 22233.00 | 23157.89 | 30799.99 |
  | 3 | Pala pocera | EXTRA POWER | 20000.00 | 20000.00 | 20790.00 | 27027.00 |
  | 4 | Cinta aisladora | INGCO | 2000.00 | 2000.00 | 2083.20 | 2624.83 |
  | 5 | Cinta métrica | DUROLL | 5000.00 | 5000.00 | 5197.50 | 7016.63 |
  | 6 | Pulverizador | EXTRA POWER | 10000.00 | 10000.00 | 10395.00 | 12723.48 |
  | 10 | Membrana | EXTRA POWER | 10000.00 | 10000.00 | 9450.00 | 14647.50 |
  | 12 | Destornillador | EXTRA POWER | 10000.00 | 10000.00 | 10416.00 | 13000.21 |
  | 30 | destornillador phillips 1/4 x8" tramontina | SHIMURA | 1972.00 | 1972.00 | 2484.72 | 3230.14 |
  | 31 | taladro de banco SHIMURA 13mm 350w | SHIMURA | 96710.40 | 96710.40 | 101545.92 | 132009.70 |
  | 32 | pulverizadora fumigadora shimura 2t mochila de curar 25lts | SHIMURA | 131764.00 | 131764.00 | 166022.64 | 215829.43 |

  *(Estos 11 productos, la fila 5/Cinta métrica se mueve deliberadamente en la tarea 1.6 al fabricar el caso de prueba; el resto queda intacto.)*
- [x] 1.3 **Registrar que la lista arranca vacía y por qué.** Confirmar con la query de la Decisión 1 que hoy da **0 filas**. Dejar anotado que eso **no es un error**: es la prueba de que el jefe ya hizo a mano el trabajo que el panel automatiza (ver la historia del producto `id=31` en `proposal.md`). Consecuencia operativa: **el estado vacío es el estado normal del día 1**, y hay que diseñarlo (tarea 8.2), no tratarlo como caso de borde.
  *Verificado 2026-08-21: la query de la Decisión 1 (JOIN LATERAL + comparación COALESCE + guard >0) dio **0 filas** contra la base real, antes de fabricar ningún caso. No es un error: es la evidencia de que hoy `costo_producto == costo_base` en los 11 productos porque el jefe ya corrigió todo a mano (historia del producto 31). Es el estado normal del día 1 — ver tarea 8.2.*
- [x] 1.4 **Contar los movimientos de Herramientas con `costo_base = 0.00` o `NULL`.** `SELECT count(*) FROM movimientos_stock WHERE deleted=false AND unidad_negocio_id=2 AND tipo_movimiento IN ('INGRESO','AJUSTE_INICIAL') AND COALESCE(costo_base,0)=0;` Son los candidatos a falso positivo que el guard de la Decisión 1d neutraliza (R5). Anotar el número **antes** de escribir la query.
  *Verificado 2026-08-21: **4** movimientos de Herramientas con `costo_base = 0.00`/`NULL`. Son los candidatos a falso positivo que neutraliza el guard `(costo_producto > 0 OR costo_base > 0)` de la Decisión 1d — verificado en la query de 3.1/3.8: ninguno de esos 4 aparece en el resultado del endpoint.*
- [x] 1.5 **Verificar la ausencia de empates exactos de `fecha`.** `SELECT producto_id, fecha, count(*) FROM movimientos_stock WHERE deleted=false AND tipo_movimiento IN ('INGRESO','AJUSTE_INICIAL') GROUP BY 1,2 HAVING count(*)>1;` Esperado: **0 filas** (los movimientos 123/124 del producto 31 difieren en 6 ms). Y `SELECT count(*) FROM movimientos_stock WHERE deleted=false AND fecha IS NULL;` → esperado **0**. Si aparece algún empate o alguna fecha nula, **es el disparador de R2**: anotarlo, no arreglarlo acá, y llevarlo a la tarea 7.4.
  *Verificado 2026-08-21: **0** empates exactos de `fecha` por producto, **0** movimientos con `fecha IS NULL`. Coincide con lo esperado — R2 no se dispara. No hace falta llevar nada a la tarea 7.4.*
- [x] 1.6 **Fabricar el caso de prueba por el flujo real** (R4). No editar filas de la base a mano. Armar un pedido a proveedor en Herramientas con un ítem sobre un producto existente, con un `costoUnitarioPactado` **distinto** del `costoProducto` de la ficha (ej. `Cinta métrica`, ficha `5000.00`, pedir a `6200.00`), y confirmar la recepción. Verificar después con la query de 1.2 que ese producto quedó con `costo_producto = 5000.00` y `costo_base = 6200.00`. **Éste es el caso vivo contra el que se verifica todo el resto del change.** Anotar el `producto_id` y el `mov_id` resultantes.
  *Fabricado 2026-08-21 por el flujo real, sin tocar filas a mano: `POST /api/pedidos` (proveedor DUROLL, id 10) con un ítem sobre `productoId=5` (Cinta métrica, ficha `5000.00`) a `costoUnitarioPactado=6200.00` → pedido `id=29`, detalle `id=47` → `POST /api/pedidos/29/recepcion` con `cantidadRecibida=10` → pedido pasó a `COMPLETO`. Verificado contra la base: **`producto_id=5`, `mov_id=141`, `costo_producto=5000.00`, `costo_base=6200.00`, `costo_unitario=6444.90`, `tipo_movimiento=INGRESO`**. Coincide exactamente con lo esperado. Éste es el caso vivo del resto del change.*
- [x] 1.7 **Fabricar un segundo caso, por edición manual** (para probar que el criterio no distingue el origen — Decisión 1): confirmar que editar el stock de otro producto genera un `AJUSTE_INICIAL`/`INGRESO` cuyo `costo_base` coincide con la ficha, y por lo tanto **no** produce una fila. Documentar por qué: `actualizarProducto()` registra el movimiento con `costoBase = producto.getCostoProducto()`, así que una edición manual "resuelve" la diferencia sola.
  *Fabricado 2026-08-21: `PUT /api/productos/1` (Pala corazón) con `stock` 6→8, resto de campos idénticos (mismo `costoProducto=15000.00`). Generó movimiento `id=142`, `tipo_movimiento=INGRESO`, `costo_base=15000.00 == costo_producto=15000.00`. **No** produjo diferencia, como se esperaba: `actualizarProducto()` registra el movimiento con `costoBase = producto.getCostoProducto()` de la ficha en ese mismo instante, así que edición manual y ficha quedan sincronizadas solas.*

## 2. Contrato de no-regresión — lo que este change NO puede tocar

- [x] 2.1 Registrar el hash o la fecha de última modificación de `backend/src/main/java/com/vivero/gestion/services/CostoCalculator.java`. **Al cerrar el change tiene que ser el mismo** (tarea 9.1). Cero cambios en la fórmula.
  *Registrado 2026-08-21, ANTES de escribir ninguna línea del grupo 3: último commit `f4739e6` (2026-08-20 12:36:31 -0300), `sha256 = b6fc6e1ed4e6dd08df4cbfc2117a20d4ab966ecc7ad9b2469c68252fa2d19e15`. Nota: el archivo tiene un diff sin commitear contra `HEAD` (agrega la sobrecarga con conversión de moneda, `MonedaCosto`/`cotizacion`) — es trabajo **preexistente** de `config-costeo-por-proveedor` (91/99 tareas, sin archivar), no de este change. El hash de arriba es el estado real del archivo en disco al empezar el grupo 3; verificado al cierre del grupo 3 (después de 3.1-3.8) que sigue siendo **exactamente el mismo** hash — cero líneas tocadas por este change.*
- [x] 2.2 Registrar el `costo_unitario` del último ingreso de los 11 productos de Herramientas (columna `m.costo_unitario` de la query 1.2). Son los valores que lee `VentaServiceImpl:118` al vender. **Ninguno puede moverse por efecto de este change salvo en los productos sobre los que el usuario apriete "Actualizar" a propósito** (R1).
  *Registrado 2026-08-21 (tabla completa en la tarea 1.2): id 1→15561.00, id 2→23157.89, id 3→20790.00, id 4→2083.20, id 5→5197.50, id 6→10395.00, id 10→9450.00, id 12→10416.00, id 30→2484.72, id 31→101545.92, id 32→166022.64. El producto `id=5` se movió a `costo_unitario=6444.90` como consecuencia **deliberada** de la fabricación del caso de prueba (tarea 1.6, no de "Actualizar" — la acción "Actualizar" es del grupo 5, no implementado en esta pasada). Los otros 10 no se tocaron.*
- [x] 2.3 Dejar asentado en el propio `tasks.md` que la Decisión 6 de `herramientas-pedidos-proveedores` **sigue vigente y no se reabre**: `PedidoServiceImpl.confirmarRecepcion()` no se modifica en ninguna línea. Verificable con `git diff` al cierre (tarea 9.2).
  *Asentado 2026-08-21: `PedidoServiceImpl.confirmarRecepcion()` sigue registrando el `INGRESO` con `costoBaseExplicito = detalle.getCostoUnitarioPactado()` y sin tocar `Producto.costoProducto`/`precio` — verificado con el caso fabricado en 1.6 (el `PUT`/escritura de "Actualizar" NO se ejecutó sobre el producto 5; su `costoProducto` sigue en `5000.00` después de confirmar la recepción). `git status` muestra `PedidoServiceImpl.java` con un diff preexistente de `config-costeo-por-proveedor` (soporte USD, sin commitear) — **ninguna línea de ese diff la agregó este change**; este grupo no tocó el archivo.*

## 3. Backend — la consulta de detección

- [x] 3.1 Escribir la query nativa en `repositories/ProductoRepository.java` siguiendo la Decisión 1 al pie de la letra: `JOIN LATERAL` con `ORDER BY ms.fecha DESC, ms.id DESC LIMIT 1`, filtro por `unidad_negocio_id` y `deleted = false` en **las dos** tablas, comparación con `COALESCE(...) <> COALESCE(...)`, guard de "al menos uno de los dos > 0", el filtro del descarte `AND (p.movimiento_revision_descartado_id IS NULL OR p.movimiento_revision_descartado_id <> m.id)` (Decisión 11), `ORDER BY m.fecha DESC, p.nombre` y **`LIMIT` explícito** (regla dura 6). Devolver una `Projection` de Spring Data, no la entidad. ⚠️ **La columna del filtro la crea la tarea 6.2 — implementarla primero**, o la query nativa falla contra la base. El filtro se escribe una sola vez, acá; la tarea 6.3 no lo vuelve a agregar.
  *Implementado 2026-08-21 en `ProductoRepository.findRevisionCostos(unidadId, limite)`, `LIMIT=50` explícito. ⚠️ **La columna `movimiento_revision_descartado_id` se adelantó a `Producto.java`** (normalmente tarea 6.2 del grupo 6, no implementado en esta pasada): es la única forma de que esta query compile y corra, tal como advertía la propia tarea. Sólo se adelantó la columna — SIN el endpoint de escritura ("Descartar") ni ninguna otra tarea del grupo 6. `RevisionCostoProjection` (nuevo archivo) es la Projection de Spring Data que devuelve; nunca la entidad `Producto`. La query extiende la comparación de la Decisión 1 con la regla de moneda de la Decisión 4 (ver 3.6) en el mismo `WHERE`, con un `CASE` para el valor comparable y la cotización.*
- [x] 3.2 Documentar en el propio comentario de la query **por qué el desempate por `id` está ahí** y por qué la `@Formula` `Producto.costoUnitarioHistorico` no lo tiene (R2 / OQ3). Es la clase de detalle que dentro de seis meses parece un error si no está explicado.
  *Documentado 2026-08-21 en el bloque de comentarios `(a)`-`(f)` sobre `findRevisionCostos()` en `ProductoRepository.java`: punto `(b)` explica el desempate `fecha DESC, id DESC` (movimientos 123/124 del producto 31 a 6ms) y por qué la `@Formula` no lo tiene (inconsistencia real pero hoy inofensiva, 0 empates/0 nulas verificado 2026-08-21 en 1.5; tocarla subiría la gobernanza del change — OQ3/R2, candidato a chore en el roadmap, tarea 9.6 fuera de esta pasada).*
- [x] 3.3 Definir el DTO de la fila del panel (regla dura 5 — nunca la entidad): `productoId`, `nombre`, `proveedorNombre`, `fechaUltimoIngreso`, `costoFicha`, `costoUltimoIngreso`, `costoUnitarioActual`, `costoUnitarioResultante` (OQ2), `precioActual`, `precioResultante`, `monedaCosto`, `cotizacionAplicada` y **`movimientoId`** — este último es **obligatorio**: es el `id` que la acción de descartar sella en el producto (Decisión 11), sin él el grupo 6 no se puede implementar. Todos nullables donde el dato puede faltar; **nunca** un `0` que se confunda con un costo real (tarea 8.3).
  *Implementado 2026-08-21: `dto/RevisionCostoProductoDTO.java`, con los 12 campos pedidos, todos con wrapper types (`BigDecimal`/`Long`/`LocalDateTime`/`String`) — ninguno primitivo, así que ninguno puede colapsar a `0` por defecto. `movimientoId` presente y obligatorio, listo para que el grupo 6 lo use.*
- [x] 3.4 Implementar el método de lectura en `ProductoService` / `ProductoServiceImpl` con `@Transactional(readOnly = true)`, resolviendo la unidad con `UnidadNegocioContextHolder.getUnidadNegocioId()` igual que `obtenerTodosLosProductos()` (Decisión 9). **Sin lógica de negocio nueva sobre el costo**: sólo armado del DTO.
  *Implementado 2026-08-21: `ProductoService.listarRevisionCostos()` + `ProductoServiceImpl.listarRevisionCostos()`, `@Transactional(readOnly = true)`, mismo `UnidadNegocioContextHolder.getUnidadNegocioId()` que `obtenerTodosLosProductos()` — `null` → lista vacía, sin excepción. El armado del DTO por fila vive en el método privado `armarFilaRevisionCosto()` (tarea 3.5), que sólo invoca `CostoCalculator`, no reimplementa nada.*
- [x] 3.5 Calcular `precioResultante` reusando **`CostoCalculator.resolverEfectivo()` + `CostoCalculator.calcular()`** con exactamente los mismos argumentos que `ProductoServiceImpl.calcularPrecioSiAplica()` (descuentos del producto, IVA y envío efectivos con fallback a la unidad de negocio), y aplicando el margen igual que ahí (Decisión 3). **Reproducir el guard**: si `porcentajeGanancia` es null o `<= 0`, o el costo nuevo es `<= 0`, `precioResultante = precioActual`. ⚠️ **No copiar la aritmética a mano** — invocar el mismo calculador, no una quinta copia de la fórmula. Exponer en el DTO **los dos costos unitarios finales** (el actual, calculado sobre `costoProducto`, y el resultante, calculado sobre el costo del ingreso): ya se calculan para llegar al precio, y son lo que la fila muestra como línea secundaria (OQ2, tarea 7.4).
  *Implementado 2026-08-21 en `armarFilaRevisionCosto()`: `CostoCalculator.resolverEfectivo()` para IVA/envío efectivos (fallback producto → unidad de negocio) y `CostoCalculator.calcular()` dos veces — una vez sobre `costoProducto` (→ `costoUnitarioActual`) y otra sobre `costoUltimoIngresoComparable` (→ `costoUnitarioResultante`), exactamente los mismos descuentos/IVA/envío en las dos. Guard reproducido tal cual: `porcentajeGanancia > 0 AND costoNuevoBase > 0`; si no se cumple, `precioResultante = precioActual`. Verificado con el caso vivo (producto 5): `costoUnitarioActual=5197.50` (igual al `costo_unitario` de la línea de base 1.2), `costoUnitarioResultante=6444.90`, `precioResultante=8700.62` — cero aritmética copiada a mano.*
- [x] 3.6 Implementar la regla de moneda de la **Decisión 4**: `ARS`/null → comparación directa; `USD` con `moneda_origen = 'USD'` y `cotizacion_aplicada > 0` → comparar `costo_base / cotizacion_aplicada` y exponer la cotización; `USD` sin conversión registrada → **excluir el producto** y dejar un `log.warn` con el `productoId` para que el caso sea diagnosticable. ⚠️ **Ésta es la única tarea del change que puede producir un error de dinero** (escribir pesos en un campo en dólares = ×1460). Escribirla con el guard explícito, no inferido.
  *Implementado 2026-08-21 con guard explícito en el `CASE` de la query (`p.moneda_costo = 'USD' AND m.moneda_origen = 'USD' AND m.cotizacion_aplicada > 0`, nunca inferido): ARS/null compara `costo_producto` vs `costo_base` directo; USD con conversión válida compara vs `costo_base / cotizacion_aplicada` (misma escala que `costoProducto`) y expone `cotizacionAplicada`; USD sin conversión válida **no matchea ninguna rama del `WHERE`** → excluido por construcción. Diagnóstico: `findProductosUsdSinConversionRegistrada()` (nueva query) + `log.warn` en `ProductoServiceImpl.listarRevisionCostos()` con el `productoId`. Hoy 0 productos USD en Herramientas (verificado 1.2), así que esta rama no tiene caso vivo para probar — cubierta por la tarea 8.9 del grupo 8 (no implementada en esta pasada).*
- [x] 3.7 Exponer el endpoint `GET` nuevo en `ProductoController` con `@PreAuthorize("hasAuthority('LEER_STOCK')")` (Decisión 8). El controller **no llama al repositorio** (regla dura 6).
  *Implementado 2026-08-21: `GET /api/productos/revision-costos` en `ProductoController`, `@PreAuthorize("hasAuthority('LEER_STOCK')")`, delega en `productoService.listarRevisionCostos()` — el controller no importa `ProductoRepository`.*
- [x] 3.8 Verificar contra la base que el endpoint devuelve **exactamente 1 fila**: la del producto fabricado en 1.6. Y que los otros 10 productos **no** aparecen. Si aparece alguno más, es un falso positivo y hay que entender por qué antes de seguir.
  *Verificado 2026-08-21 tras rebuild (`docker compose up -d --build backend`) y login real (`jefe@vivero.com`): `GET /api/productos/revision-costos` con `X-Unidad-Negocio: 2` devuelve **exactamente 1 fila** — `productoId=5, nombre="Cinta métrica", proveedorNombre="DUROLL", costoFicha=5000.00, costoUltimoIngreso=6200.00, costoUnitarioActual=5197.50, costoUnitarioResultante=6444.90, precioActual=7016.63, precioResultante=8700.62, monedaCosto="ARS", cotizacionAplicada=null, movimientoId=141`. Los otros 10 productos de la línea de base **no** aparecen (coinciden al centavo, ver tabla de 1.2). Cero falsos positivos.*

## 4. 🚦 CHECKPOINT — el criterio de detección, con productos reales, antes de cablear la escritura

- [ ] 4.1 **Mostrarle al usuario la salida real del endpoint** sobre la base con el caso fabricado: la fila que aparece, con costo viejo, costo nuevo, precio viejo y precio nuevo, y **la lista de los 10 productos que NO aparecen con el motivo de cada uno** (coinciden al centavo). Confirmar que es lo que espera ver.
- [ ] 4.2 Mostrarle, con los números concretos del producto `id=31` de la historia real, **qué le pasa al `costo_unitario` cuando se aprieta "Actualizar"** (R1): el ingreso del pedido dio `121855.10` y el ajuste manual posterior dio `101545.92` — **un 17% de diferencia**, porque el movimiento nuevo se calcula con los descuentos/IVA/envío de la **ficha**, no con los del pedido (Decisión 6). Confirmar que entiende que ése es el comportamiento que ya tiene hoy al editar a mano, y que el panel sólo lo hace de un click.
- [x] 4.3 Presentar las **OQ2, OQ3 y OQ4** de `design.md` con su recomendación y cerrarlas en esta misma conversación. Anotar las respuestas acá. *Cerradas por la recomendación documentada, sin objeción del usuario. El registro completo de las cuatro decisiones —incluida la OQ1— está en la tarea 6.1; no volver a plantearlas como abiertas en el checkpoint.*
- [ ] 4.4 **No avanzar al grupo 5 sin el OK explícito.** Si el usuario quiere cambiar el criterio (ej. incluir los productos sin costo en la ficha, o excluirlos), se ajusta el grupo 3 y se vuelve a este checkpoint — no se sigue con una diferencia de criterio pendiente.

## 5. Backend / Frontend — la acción "Actualizar"

- [ ] 5.1 Implementar la acción en el frontend tomando **el objeto producto completo** que ya está en el estado de `Productos.jsx` (viene de `GET /api/productos`, que mapea todos los campos incluida la lista de descuentos), reemplazando **únicamente `costoProducto`**, y haciendo el mismo `PUT /api/productos/{id}` que hace `ProductoForm.jsx` (Decisión 5). **Ningún endpoint de escritura nuevo.**
- [ ] 5.2 Agregar el método correspondiente en `frontend/src/api/productos.api.js`. `ProductoForm.jsx` **no se toca**.
- [ ] 5.3 Refrescar la grilla del catálogo y la lista del panel después de un `PUT` exitoso, con el `fetchProductos()` que ya existe.
- [ ] 5.4 Feedback con `useUIStore` (regla dura 7): `pushToast('success', ...)` indicando **el producto y el cambio de precio concreto** ("Cinta métrica: precio actualizado de $7.016,63 a $8.700,63"), no un "guardado" genérico. Ante `403`, `denyAccess(...)` con el mismo texto que usan `handleCreateOrUpdate`/`handleDelete`. Ante otro error, `pushToast('error', getErrorMessage(err, ...))` y **la fila se queda en el panel**.
- [ ] 5.5 Verificar que el `precioResultante` que mostraba la fila es **exactamente** el `precio` persistido después del `PUT` (requisito de la spec, Decisión 3). Si difiere aunque sea un centavo, el cálculo del 3.5 no está usando los mismos argumentos que `calcularPrecioSiAplica()` — corregirlo ahí, no ajustar la comparación.
- [ ] 5.6 Verificar que después de "Actualizar" el producto **desaparece de la lista**: el `PUT` genera un movimiento nuevo cuyo `costoBase = costoProducto` nuevo, así que la diferencia se cierra sola. Confirmarlo con la query de 1.2, no sólo mirando la UI.
- [ ] 5.7 **Registrar el efecto sobre `costoUnitarioHistorico`** (R1): anotar el `costo_unitario` del último ingreso del producto **antes** y **después** de la acción. Es el número que van a congelar las ventas futuras. Si se movió más de lo que el usuario aprobó en 4.2, detenerse y reportar.
- [ ] 5.8 **Verificación anti-R3 — el diff completo.** Tomar el producto del caso 1.6, guardar el `SELECT *` de su fila en `productos` y de sus filas en `producto_descuentos` **antes** de la acción, ejecutar "Actualizar", y volver a compararlo. **Sólo `costo_producto` y `precio` pueden haber cambiado.** `iva_porcentaje`, `costo_envio_porcentaje`, `porcentaje_ganancia`, `proveedor_id`, `moneda_costo`, `stock`, `lote`, `dueno`, `descripcion` y la lista completa de descuentos MUST quedar idénticos. Ésta es la verificación más importante del grupo — `actualizarProducto()` pisa IVA/envío con null y **borra y reinserta** los descuentos.

## 6. La acción "Descartar" — opción C, decidida

> Las cuatro Open Questions están cerradas (tarea 6.1). El mecanismo es el de la **Decisión 11**: persistir el `id` del movimiento ya revisado. **Este grupo ya no está bloqueado.** La tarea 6.2 es prerrequisito de la línea del filtro de la tarea 3.1.

- [x] 6.1 **Registro de las 4 Open Questions de `design.md` y su resolución.** *Cerradas. La OQ1 la decidió el usuario explícitamente; las otras tres quedaron por la recomendación documentada, sin objeción. Las cuatro decisiones, en los términos exactos en que quedaron:*
  - *(1) **OQ1 — qué persiste "Descartar": OPCIÓN C, el `id` del movimiento ya revisado.** Decidido por el usuario, sobre las alternativas de no persistir nada (A) o persistir el par de costos (B). Una sola columna nullable `productos.movimiento_revision_descartado_id BIGINT`, sin FK y sin default; "Descartar" sella ahí el `id` del `INGRESO`/`AJUSTE_INICIAL` que el usuario acaba de mirar, y la query suma `AND (p.movimiento_revision_descartado_id IS NULL OR p.movimiento_revision_descartado_id <> m.id)`. Semántica: "ya revisé este ingreso". **Consecuencia aceptada a sabiendas:** si llega un ingreso **nuevo al mismo costo ya descartado**, la fila **vuelve a aparecer** — el marcador identifica el ingreso, no el importe. **El marcador se auto-limpia**: al editar el producto se genera un movimiento nuevo y el `id` guardado deja de ser el del último ingreso. **Las opciones A y B quedan descartadas: ninguna tarea puede implementar el descarte como estado local de React ni como par de columnas de costo.** Ver Decisión 11 de `design.md`.*
  - *(2) **OQ2 — mostrar también el costo unitario final: SÍ, como línea secundaria.** Por la recomendación, sin objeción. La fila muestra `costo unitario actual → costo unitario resultante` en tipografía más chica, debajo del contraste de costos base. Sale gratis (el backend ya calcula ese valor para el `precioResultante`) y explica por qué el precio nuevo es el que es. Es puramente presentacional: tareas 3.3, 3.5 y 7.4.*
  - *(3) **OQ3 — el desempate faltante de la `@Formula` `costoUnitarioHistorico`: NO se toca en este change.** Por la recomendación, sin objeción. Es una inconsistencia real (`fecha DESC` sin `id DESC`) pero hoy inofensiva (0 empates exactos y 0 fechas nulas, verificado el 2026-08-21), y esa `@Formula` es lectura financiera viva —Finanzas y cada venta—: tocarla subiría la gobernanza del change de MEDIA a MEDIA-ALTA. Queda como candidato a **chore propio** en `openspec/roadmap.md` (tarea 9.6), documentado en el comentario de la query (tarea 3.2). Sigue siendo un Non-Goal explícito.*
  - *(4) **OQ4 — rol con `LEER_STOCK` y sin `ESCRIBIR_STOCK` (`EMPLEADO VIVERO`): ve el panel y los botones, y recibe el 403 manejado con `denyAccess`.** Por la recomendación, sin objeción. Es el mismo patrón que ya usan `handleCreateOrUpdate` y `handleDelete` en `Productos.jsx`. **No se ocultan los botones preventivamente** — sería un gating distinto del que la página ya tiene. Tareas 5.4 y 8.8.*
- [ ] 6.2 Agregar en `models/Producto.java` la columna nullable **`movimientoRevisionDescartadoId`** (`Long`, `@Column(name = "movimiento_revision_descartado_id")`), **sin FK declarada y sin valor por defecto** — es un marcador de revisión, no una relación del dominio, y `null` significa "nunca se descartó nada" (Decisión 11 y R8). `ddl-auto=update` la crea al arrancar — **sin `ALTER TABLE` a mano** (mismo criterio que los dos changes anteriores). Verificar la columna en la base con `\d productos` después de levantar el backend, y confirmar que quedó en `NULL` en las 16 filas existentes (sin backfill). ⚠️ **Es el único cambio permitido en `Producto.java`: la `@Formula` `costoUnitarioHistorico` no se toca** (OQ3).
- [ ] 6.3 Implementar el endpoint de escritura del descarte en `ProductoController` → `ProductoService`/`ProductoServiceImpl`, con `@PreAuthorize("hasAuthority('ESCRIBIR_STOCK')")` y `@Transactional`. Recibe el `productoId` y el `movimientoId` que el panel mostró en esa fila (el del DTO de la 3.3), y **sella únicamente** `movimientoRevisionDescartadoId`. Es la **única** escritura nueva que este change agrega. El filtro en la query ya se escribió en la 3.1 — no duplicarlo. El controller **no llama al repositorio** (regla dura 6).
- [ ] 6.4 Cablear la acción en el frontend: el botón de descartar de la fila llama al endpoint nuevo (método en `productos.api.js`), y ante éxito **saca la fila del panel sin recargar el catálogo** —no cambió ningún dato de la grilla—. Ante `403`, `denyAccess(...)`; ante otro error, `pushToast('error', ...)` y **la fila se queda** en el panel (OQ4, regla dura 7).
- [ ] 6.5 Verificar el requisito duro de la spec, en sus **dos mitades**: (a) descartar → **recargar la página** → la fila **sigue sin aparecer** (es lo que la opción A no daba); (b) después **confirmar un pedido nuevo** sobre ese producto a un costo distinto → **la fila vuelve a aparecer**, con el costo del ingreso nuevo. El paso (b) es el que valida que "descartar" no se volvió un "resuelto para siempre".
- [ ] 6.6 Verificar que descartar **no modifica ningún dato de negocio del producto**: mismo `SELECT *` de su fila en `productos` y de sus filas en `producto_descuentos` antes y después, con **`movimiento_revision_descartado_id` como única diferencia**. `costo_producto`, `precio`, `porcentaje_ganancia`, `iva_porcentaje`, `costo_envio_porcentaje`, `stock`, `proveedor_id` y los descuentos MUST quedar idénticos.
- [ ] 6.7 Verificar la **auto-limpieza** del marcador (Decisión 11): tomar un producto con el descarte sellado, editarlo desde `ProductoForm.jsx` (o apretar "Actualizar") y confirmar que el movimiento nuevo pasa a ser el último ingreso, con lo cual el `id` guardado deja de coincidir y el marcador queda **inerte** — sin necesidad de borrarlo ni de ninguna rutina de limpieza.

## 7. Frontend — el panel

- [ ] 7.1 Crear el componente del panel en `frontend/src/components/`, en **PascalCase** (regla dura 7), y montarlo en `pages/Productos.jsx` **arriba de la grilla**, condicionado a `unidadNegocioActiva === '2'` **y** a que haya al menos una fila (Decisión 2). Cuando no hay filas, el panel **no ocupa espacio**.
- [ ] 7.2 Cada fila: nombre, proveedor (o la indicación de que no tiene), fecha del último ingreso, `costo ficha → costo último ingreso`, `precio actual → precio resultante`. Formateo de moneda con la convención que ya usa `Productos.jsx` (`toLocaleString('es-AR', { minimumFractionDigits: 2 })`).
- [ ] 7.3 Los dos botones con `cursor-pointer` e iconos de `lucide-react`. **Sin modal de confirmación** (Decisión 7): la decisión se toma mirando el precio resultante, que ya está en la fila. **Sin `alert`/`confirm` nativos.**
- [ ] 7.4 Agregar la línea secundaria `costo unitario actual → costo unitario resultante` (OQ2, resuelta por sí), en tipografía más chica, debajo del contraste de bases. Es lo que explica por qué el precio nuevo es el que es. Los dos valores vienen del DTO (tarea 3.5): **el frontend no los recalcula.**
- [ ] 7.5 Manejar el fallo de la consulta ocultando el panel, **sin** bloquear la sección Productos (requisito de degradación de la spec). El catálogo tiene que cargar y operar igual.
- [ ] 7.6 Responsive: el panel tiene que verse en mobile con el mismo criterio de tarjetas que ya usa la grilla (`grid ... sm:hidden` / `hidden sm:block`), coherente con `ui-responsive`.
- [ ] 7.7 *(Si la lista llega al `LIMIT`)* Mostrar explícitamente que está recortada ("se muestran los N ingresos más recientes"), en vez de fingir que ésa es toda la lista (Decisión 10).

## 8. Casos de borde y comportamiento visual

- [ ] 8.1 Verificar el aislamiento por unidad de negocio: con Vivero activo, el panel **no se renderiza** y el endpoint devuelve lista vacía para `unidad_negocio_id = 1` (Decisión 9).
- [ ] 8.2 Verificar el **estado vacío**, que es el estado normal del día 1 (tarea 1.3): con 0 filas, la sección Productos se ve **exactamente como hoy**, sin ningún cartel de "todo al día" que el usuario aprenda a ignorar.
- [ ] 8.3 Verificar el caso "ficha sin costo cargado": la fila lo indica explícitamente en lugar de mostrar un `$0,00` que parezca un costo real.
- [ ] 8.4 Verificar el caso "producto sin proveedor": la fila lo indica en lugar de dejar el espacio vacío.
- [ ] 8.5 Verificar que un producto cuyo movimiento más reciente es una `VENTA` o un `EGRESO` **no** aparece por eso: el criterio mira el último `INGRESO`/`AJUSTE_INICIAL`, y los egresos copian el desglose del ingreso anterior (`MovimientoStockServiceImpl`, rama `else`).
- [ ] 8.6 Verificar que un producto **sin ningún** `INGRESO`/`AJUSTE_INICIAL` no aparece (el `JOIN LATERAL` lo excluye por construcción).
- [ ] 8.7 Verificar el caso de igualdad con distinta escala: `15000.00` vs `15000.000` **no** es una diferencia (comparación `numeric` por valor, Decisión 1c).
- [ ] 8.8 Verificar el `403`: iniciar sesión con un rol que tenga `LEER_STOCK` y no `ESCRIBIR_STOCK` (existe: **`EMPLEADO VIVERO`**, verificado en base el 2026-08-21) y confirmar que la lectura funciona, que **los botones se ven igual** (no se ocultan preventivamente — OQ4 resuelta) y que **las dos** escrituras —"Actualizar" y "Descartar"— devuelven `403` manejado con `denyAccess`, dejando la fila en el panel.
- [ ] 8.9 *(Sólo si algún día hay un producto en USD — hoy hay 0)* Verificar los tres caminos de la Decisión 4. Mientras no exista ninguno, dejar el caso cubierto por un test de la lógica de comparación con datos construidos, no por prueba manual.

## 9. Verificación integral y cierre

- [ ] 9.1 **Contrato de no-regresión:** `git diff` sobre `backend/src/main/java/com/vivero/gestion/services/CostoCalculator.java` → **cero líneas**. Si cambió algo, se rompió el alcance declarado del change.
- [ ] 9.2 `git diff` sobre `PedidoServiceImpl.java` → **cero líneas** (Decisión 6 de `herramientas-pedidos-proveedores` intacta, tarea 2.3). `git diff` sobre `ProductoForm.jsx` → **cero líneas**.
- [ ] 9.3 Verificar que `calcularPrecioSiAplica()` y `actualizarProducto()` **no fueron modificados**: el change los reusa, no los toca. `git diff` sobre `models/Producto.java` → **sólo** el campo `movimientoRevisionDescartadoId` con su getter/setter (tarea 6.2); la `@Formula` `costoUnitarioHistorico` **sin una sola línea de diferencia** (OQ3).
- [ ] 9.4 Volver a correr la query de la línea de base (1.2) sobre los 11 productos y confirmar que **ninguno** se movió salvo los que el usuario actualizó a propósito durante las pruebas.
- [ ] 9.5 Recorrido de punta a punta con el usuario: confirmar un pedido a un costo nuevo → el producto aparece en el panel → mirar el precio resultante → "Actualizar" → verificar el precio en la grilla → confirmar otro pedido a otro costo → el producto **vuelve** a aparecer → "Descartar" → recargar la página y confirmar que **sigue sin aparecer** → confirmar un pedido más → confirmar que **vuelve a aparecer** (Decisión 11 / OQ1).
- [ ] 9.6 Anotar en `openspec/roadmap.md` el hallazgo de la **OQ3/R2** (el desempate faltante en la `@Formula` `Producto.costoUnitarioHistorico`) como candidato a *chore* propio, con el motivo por el que no se arregló acá: es lectura financiera viva (Finanzas y ventas) y tocarla subiría la gobernanza del change. Incluir la línea exacta de `Producto.java` y el contraste con `findResumenPorProveedor`.
- [ ] 9.7 Anotar en `openspec/roadmap.md` la relación con `config-costeo-por-proveedor` (todavía sin archivar) y con la Decisión 6 de `herramientas-pedidos-proveedores`, para que quien lea el roadmap entienda que este change **completa** esa decisión en vez de contradecirla.
- [ ] 9.8 Confirmar que **no se ejecutó ningún build ni ningún commit** sin pedido explícito del usuario (reglas duras 1 y 2).
